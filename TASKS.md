# Pitch Simulator Vertical Slice Tasks

This checklist mirrors `docs/plans/2026-07-14-pitch-simulator-vertical-slice.md`. Each task is checked only after its task-specific verification checkpoint succeeds.

## P0 - Domain foundation

- [x] Task 1: Unity repository foundation and repeatable test runner
- [x] Task 2: Assembly boundaries and core game states
- [x] Task 3: Dialogue DTOs and structural validation
- [x] Task 4: Runtime dialogue graph, flags and branches
- [x] Task 5: Scoring, confidence and result feedback
- [x] Task 6: Timer and accessibility settings
- [x] Task 7: LMS contracts, serialization and mock bridge
- [x] Task 8: Localization catalog and save-data versioning
- [x] Task 9: Smart School Garden content and JSON import
- [x] Task 10: Session controller orchestration

## P1 - Playable experience and integrations

- [x] Task 11: Unity composition, scenes and uGUI shell
- [x] Task 12: Response interaction, timer and confidence presentation
- [x] Task 13: Original pixel-art presentation and judge reactions
- [x] Task 14: Browser-safe audio hooks
- [x] Task 15: WebGL JavaScript bridge and local LMS harness
- [x] Task 16: Complete results, review and retry flow

## P2 - Delivery and acceptance

- [x] Task 17: Required project documentation and AI handoffs
- [x] Task 18: WebGL build automation and development build
- [x] Task 19: Local HTTP and browser smoke tests
- [x] Task 20: Final acceptance audit

## Verification evidence

- 2026-07-14 Task 1: Unity `6000.5.3f1` batch import exited `0` and ended with `Exiting batchmode successfully now!`; the full `artifacts/logs/unity-import.log` scan found `0` matches for `error CS\d+|Compilation failed|Unhandled Exception`.
- 2026-07-14 Task 1: `Packages/manifest.json` retains the approved `com.unity.test-framework` `1.5.1` and `com.unity.ugui` `2.0.0` pins with no multiplayer, Addressables, Input System, networking or tween package. Unity 6000.5 resolved its built-in lock entries to test framework `1.7.0` and uGUI `2.5.0`.
- 2026-07-14 Task 1: both PowerShell wrappers parsed with `0` syntax errors; required ignore rules were exercised, while `Assets/**/*.meta`, `Packages/packages-lock.json` and `ProjectSettings/` remained unignored.
- 2026-07-14 Task 1: `tools/Run-UnityTests.ps1 -Platform EditMode` completed end-to-end, created `artifacts/test-results/editmode.xml`, and reported `0/0` passed with `0` failures before any test assembly exists.
- 2026-07-14 Task 1 log note: Unity recovered from transient licensing-client handshake/access-token diagnostics, resolved its assigned Personal licence, and completed successfully; two `Curl error 42: Callback aborted` lines occurred during clean shutdown.
- 2026-07-14 Task 2 RED: the focused `GameStateMachineTests` Edit Mode run produced no XML and stopped at the expected missing-production-types compiler boundary (`CS0234` for `Agrovator.PitchSimulator.Core`) before the Core types existed.
- 2026-07-14 Task 2 focused GREEN: `GameStateMachineTests` reported `5/5` passed with `0` failures and the complete 421-line Unity log contained `0` matches for `error CS\d+|Compilation failed|Unhandled Exception`.
- 2026-07-14 Task 2 all Edit Mode GREEN: the canonical wrapper reported `5/5` passed with `0` failures, skips or inconclusive tests; the final complete 398-line log contained `0` failure-marker matches. The runtime Core assembly declares `noEngineReferences: true` and its sources contain no `UnityEngine` references.
- 2026-07-14 Task 3 RED: the focused `ScenarioValidatorTests` run produced no XML and reached the expected missing-production-types compiler boundary (`CS0234` for the absent Dialogue namespace and `CS0246` for `ScenarioDefinitionDto`, `DialogueNodeDto` and `ResponseOptionDto`); the complete 720-line log ended with `Scripts have compiler errors.`
- 2026-07-14 Task 3 focused GREEN: `ScenarioValidatorTests` reported `13/13` passed with `0` failures, skips or inconclusive tests, and the complete 420-line Unity log contained `0` matches for `error CS\d+|Compilation failed|Unhandled Exception`.
- 2026-07-14 Task 3 all Edit Mode GREEN: the canonical wrapper reported `18/18` passed with `0` failures, skips or inconclusive tests; the complete 366-line log contained `0` failure-marker matches.
- 2026-07-14 Task 3 structural checks: the runtime Dialogue assembly declares `noEngineReferences: true`, references only Core and contains `0` `UnityEngine`, `MonoBehaviour` or `ScriptableObject` references. Validator coverage confirms every required issue code, inclusive confidence bounds, complete issue collection and stable node/response ordering; `ValidationIssue` exposes only `Code`, `Path` and `Severity`.
- 2026-07-14 Task 4 RED: the focused `DialogueSessionTests` run produced no XML and reached the expected missing-production-type boundary (`CS0246` for the absent `DialogueSession`); the complete 699-line log ended with `Scripts have compiler errors.`
- 2026-07-14 Task 4 focused GREEN: `DialogueSessionTests` reported `11/11` passed with `0` failures, skips or inconclusive tests; the complete 402-line Unity log contained `0` matches for `error CS\d+|Compilation failed|Unhandled Exception`.
- 2026-07-14 Task 4 all Edit Mode GREEN: the canonical wrapper reported `29/29` passed with `0` failures, skips or inconclusive tests; independent XML parsing confirmed `Passed`, and the complete 365-line log contained `0` failure-marker matches.
- 2026-07-14 Task 4 structural checks: runtime scenario data is copied into get-only objects backed by read-only collections and an ordinal dictionary; session response IDs and flags use ordinal case-sensitive identity. Rejected unknown and unavailable selections preserve both current node and flags, and the recovery test proves the flag is authored by the weak-answer then evidence-response sequence. The Dialogue runtime and its Task 4 tests contain `0` `UnityEngine`, `MonoBehaviour` or `ScriptableObject` references.
- 2026-07-14 Task 4 review RED: the focused fixture reported `15/19` passed and exactly four expected failures: terminal responses remained available, and compile accepted null, empty and exact-duplicate response IDs. The complete 386-line log contained `0` compile-failure markers.
- 2026-07-14 Task 4 review GREEN: the focused fixture reported `19/19` passed and the canonical full Edit Mode suite reported `37/37` passed, both with `0` failures, skips or inconclusive tests. Independent XML parsing confirmed `Passed`; the complete focused/full logs were 381/365 lines with `0` matches for `error CS\d+|Compilation failed|Unhandled Exception`.
- 2026-07-14 Task 5 RED: the focused `ScoringTests` run produced no XML and reached the expected missing-production-types compiler boundary (`CS0234` for the absent Scoring namespace plus `CS0246`/`CS0103` for its contracts); the complete log ended with `Scripts have compiler errors.`
- 2026-07-14 Task 5 runtime seam RED: after the baseline scoring suite was green, the new immutable runtime-delta integration case reached the expected `CS1503` boundary because only the authored DTO overload existed; restoring the mapped `RuntimeResponseScoreDelta` overload made the case pass without introducing a second score model.
- 2026-07-14 Task 5 focused GREEN: `ScoringTests` reported `31/31` passed with `0` failures, skips or inconclusive tests; independent XML parsing confirmed `Passed`, and the complete 392-line Unity log contained `0` matches for `error CS\d+|Compilation failed|Unhandled Exception`.
- 2026-07-14 Task 5 all Edit Mode GREEN: the final canonical wrapper reported `68/68` passed with `0` failures, skips or inconclusive tests; independent XML parsing confirmed `Passed`, and the complete 393-line log contained `0` failure-marker matches.
- 2026-07-14 Task 5 scoring checks: all seven authored deltas clamp to explicit 20/15/15/15/15/10/10 caps and floor at zero; overall caps at 100, confidence at 0…100, level boundaries are inclusive at 0/40/60/80, category strength thresholds are 12/9/9/9/9/6/6, and normalized strength/improvement ties use stable rubric order. Recovery produces the localized `result.strength.recovery` key; all result copy remains localization keys. Scoring declares `noEngineReferences: true` and the Scoring sources/tests contain `0` `UnityEngine`, `MonoBehaviour` or `ScriptableObject` references.
- 2026-07-14 Task 6 RED: the focused timer/settings run produced no XML and reached the expected missing-production-types compiler boundary (`CS0234`/`CS0246`/`CS0103` for the absent Accessibility namespace, `AccessibilitySettings` and `TimerMode`); the 719-line log ended with `Scripts have compiler errors.`
- 2026-07-14 Task 6 focused GREEN: the final post-refactor fixture run reported `35/35` passed with `0` failures, skips or inconclusive tests; independent XML parsing confirmed `Passed`, and the complete 388-line log contained `0` matches for `error CS\d+|Compilation failed|Unhandled Exception`.
- 2026-07-14 Task 6 all Edit Mode GREEN: the canonical wrapper reported `103/103` passed with `0` failures, skips or inconclusive tests; independent XML parsing confirmed `Passed`, and the complete 365-line log contained `0` failure-marker matches.
- 2026-07-14 Task 6 timer/accessibility checks: Core consumes only an effective duration and contains `0` Accessibility/`TimerMode` references; the Accessibility assembly points only to Core and both runtime assemblies declare `noEngineReferences: true`. Zero duration is an explicitly disabled timer that never expires, while positive timers clamp at zero and emit `Expired` once. Negative/non-finite deltas throw before mutation, and a 1,000-tick allocation test measured no managed allocation. Settings cover Normal/Extended/Off, tutorial zero duration, reduced motion, finite-safe `0…1` audio volumes and exact initial `en`/`ms` locale support with English fallback.
- 2026-07-14 Task 6 review fix: focused RED reported `39/40` passed with the sole expected failure that Extended duration overflow returned instead of throwing; safe-boundary and timer reentrancy/lifecycle characterization cases already passed. After guarding the computed result, focused GREEN reported `40/40` and full Edit Mode reported `108/108`, each with `0` failures, skips or inconclusive tests and `0` log failure markers. Extended mode accepts `1.1984620899082103E+308` (finite result `1.7976931348623155E+308`) and rejects the first overflowing authored value `1.1984620899082105E+308` with `ArgumentOutOfRangeException`.

- 2026-07-14 Task 7 RED: the LMS fixture reached the expected missing-production-types compiler boundary (`CS0234` for the absent LMS namespace and `CS0246`/`CS0103` for its contracts and mock modes); no XML was produced and the complete Unity log ended with `Scripts have compiler errors.`
- 2026-07-14 Task 7 focused GREEN: `LmsPayloadTests` reported `36/36` passed with `0` failures, skips or inconclusive tests; independent XML parsing confirmed `Passed`, and the complete 368-line Unity log contained `0` matches for `error CS\d+|Compilation failed|Unhandled Exception`.
- 2026-07-14 Task 7 all Edit Mode GREEN: the final canonical wrapper reported `144/144` passed with `0` failures, skips or inconclusive tests; independent XML parsing confirmed `Passed`, and the complete 366-line Unity log contained `0` failure-marker matches.
- 2026-07-14 Task 7 contract/privacy checks: completion JSON round-trips public-field/array DTOs through isolated Unity `JsonUtility`; validation collects structured codes for required IDs, supported content version `1`, inclusive `0-100` overall/confidence/competency scores, UTC timestamp format/order, finite nonnegative duration and nonnegative timeout/attempt counts. The completion field inventory is pseudonymous learner/session/course/module/lesson/scenario IDs plus game/content version, status/timing, result scores, selected response IDs, timeout/attempt and follow-up lesson ID only; it has no name, email, school, credential, answer text or open-ended learner-content field. Launch configuration carries only `LaunchReference`, an opaque short-lived reference outside the completion payload. Mock Success/Failure/Expired/MissingConfiguration modes are synchronous, callback-null-safe, attempt-preserving and return typed sanitized error keys without payload values.
- 2026-07-14 Task 7 review RED: against commit `c118b6d`, the expanded focused LMS fixture reported `51/88` passed with exactly `37` expected failures: `15` serializer-gating/non-finite cases, `10` launch-volume validation cases, `9` launch-reference validation cases and `3` permissive timestamp cases; the complete 391-line log had `0` compile/exception markers. A follow-up upper-bound RED reported `88/89` passed with the sole expected failure that a 61-character launch-reference identifier was accepted.
- 2026-07-14 Task 7 review GREEN: the final focused fixture reported `89/89` passed with `0` failures, skips or inconclusive tests and the complete 386-line log contained `0` failure markers. The final canonical wrapper reported `197/197` EditMode tests passed; independent XML parsing confirmed `Passed` with `0` failures, skips or inconclusive tests, and the complete 366-line log contained `0` failure markers. Serialization now gates on structured validation before `JsonUtility`, all serialized floating values must be finite and in range, launch references require `lref_` plus 12-60 URL-safe identifier characters, timestamps use exact invariant RFC3339 UTC forms with optional 1-7 digit fractional seconds, and completion arrays normalize to non-null empty arrays after JSON deserialization.
- 2026-07-14 Task 7 timestamp review cycle 3: focused RED against `8ba1127` reported `89/90` passed with the sole expected failure that bare-decimal timestamp `2026-07-14T02:00:00.Z` was accepted; the complete 388-line log had `0` failure markers. Replacing optional uppercase-`F` parsing with exact lowercase-`f` formats for 1-7 fractional digits produced focused GREEN `90/90` and canonical full EditMode GREEN `198/198`, both with `0` failures, skips or inconclusive tests; their complete 387/364-line logs contained `0` failure markers.
- 2026-07-14 Task 8 RED: the focused localization fixture produced no XML and reached the expected missing-production-type compiler boundary (`CS0246` for `LocalizationCatalog`); the complete 707-line Unity log ended with `Scripts have compiler errors.` The compiler response included Unity's `System.Runtime.Serialization.dll`, confirming the planned BCL JSON API was available before implementation.
- 2026-07-14 Task 8 focused GREEN: `LocalizationTests` reported `12/12` and `SaveDataTests` reported `18/18` passed with `0` failures, skips or inconclusive tests; independent XML parsing confirmed `Passed`, and their complete 419/366-line logs contained `0` matches for `error CS\d+|Compilation failed|Unhandled Exception`.
- 2026-07-14 Task 8 all Edit Mode GREEN: the canonical wrapper reported `228/228` passed with `0` failures, skips or inconclusive tests; independent XML parsing confirmed `Passed`, and the complete 363-line log contained `0` failure-marker matches.
- 2026-07-14 Task 8 catalog/versioning checks: `en.json` is the canonical `reviewed` 28-key catalog; `ms.json` has exactly the same 28 ordinal keys, no duplicates or omissions, and status `pending_human_review`, with reviewed English fallback copy rather than fabricated Malay. All 19 Task 5 result keys are present. `DataContractJsonSerializer` parses the deterministic locale/status/entries schema and rejects malformed/null/duplicate entries. Save version 1 contains exactly version, timer mode, reduced motion, music volume, SFX volume and locale primitives; v0 deterministically migrates to v1 defaults, negative/future versions are rejected, and Core contains `0` Accessibility or `UnityEngine` references.
- 2026-07-14 Task 8 review RED against `5990133`: the expanded localization fixture reported `14/24` passed with exactly 10 expected failures (four empty/whitespace value cases, four unsafe-key escaping cases, the null-key placeholder and concatenated JSON); the save fixture reported `19/21` passed with the two expected failures for concatenated JSON and trailing non-whitespace. Their complete 391/367-line logs contained `0` compile/exception markers; trailing-whitespace characterization already passed in both fixtures.
- 2026-07-14 Task 8 review GREEN: `LocalizationTests` reported `24/24` and controller-run `SaveDataTests` reported `21/21` passed with `0` failures, skips or inconclusive tests; their complete 390/366-line logs contained `0` failure markers. The controller-run canonical wrapper reported `243/243` EditMode tests passed; independent XML parsing confirmed `Passed` with `0` failures, skips or inconclusive tests, and the complete 365-line log contained `0` failure markers. Catalog values now reject empty/whitespace copy, both BCL readers enforce one top-level object plus whitespace-only EOF, and missing-key diagnostics escape every character outside ASCII letters/digits/dot/underscore/hyphen as fixed-width uppercase `\uXXXX` while using an escaped stable null placeholder.
- 2026-07-14 Task 9 RED: the initial real-content fixture reported `0/7` with exactly seven expected failures because `Assets/Content/Scenarios/smart-school-garden.en.json` was absent; its complete 398-line log had `0` compile/exception markers. The loader fixture then produced no XML at the expected missing-production-type boundary, with five `CS0103` references to `ScenarioJsonLoader` in the complete 723-line log before `Scripts have compiler errors.`
- 2026-07-14 Task 9 focused GREEN: the final aggregate Task 9 filter reported `19/19` passed with `0` failures, skips or inconclusive tests; independent XML parsing confirmed `Passed`, and the complete 393-line log contained `0` matches for `error CS\d+|Compilation failed|Unhandled Exception`. This covers real authored content, sanitized loader/error behavior, Unity `JsonUtility` DTO compatibility and the thin `ScenarioAsset` wrapper.
- 2026-07-14 Task 9 all Edit Mode GREEN: the canonical wrapper reported `262/262` passed with `0` failures, skips or inconclusive tests; independent XML parsing confirmed `Passed`, and the complete 366-line log contained `0` failure markers.
- 2026-07-14 Task 9 content checks: the scenario has 10 nodes and 8 authored scored-question nodes; condition-aware traversal enumerated 729 playable paths, all with exactly 6 scored questions. Every scored question has exactly 3 responses, timers cover `0/12/15/20`, strongest positions span all three slots, 4 nodes author conditional availability, and the single `recovered_after_weak_answer` setter requires `weak_claim_made`. Required competency/special tags, reaction cues, quality tiers and capped score/confidence deltas pass validation. A review RED (`8/9`) caught answer length predicting quality; copy rebalance made at least two strong choices non-longest and one strong choice shortest.
- 2026-07-14 Task 9 loader/localization checks: `DataContractJsonSerializer` deserializes the existing public mutable DTO schema, validation runs against supplied localization keys, and valid content compiles to immutable runtime data. Missing, malformed, concatenated and compile-invalid input returns code/path/severity-only structured issues without raw JSON. English and Malay catalogs each contain 109 unique keys with exact ordinal parity; Malay remains `pending_human_review` and intentionally retains English fallback copy.
- 2026-07-14 Task 9 assembly adaptation: the approved engine-free `Agrovator.PitchSimulator.Dialogue` assembly remains `noEngineReferences: true`, references only Core and contains `0` Unity engine type references in its root sources. The required `ScenarioAsset` lives at `Assets/Scripts/Dialogue/Unity/ScenarioAsset.cs` in child `Agrovator.PitchSimulator.Dialogue.Unity`; it stores only serialized JSON, judge and audio Unity object references and no session state.
- 2026-07-14 Task 9 review-fix RED against `c34a633`: the strengthened content fixture reported `8/10` passed with exactly two failures. Authored question nodes had 6 Strong-longest choices against the new maximum of 4, and every playable route exceeded the maximum of 2 Strong-longest prompts. The complete 392-line log contained `0` compile/exception markers. New path hardening already passed with 729 terminal paths, exactly 6 scored questions each, 3 available responses at every visited scored question and zero dead ends, missing/unavailable destinations or choice-loss events; exact pending-review Malay-to-English fallback also passed.
- 2026-07-14 Task 9 review-fix GREEN: response copy was rebalanced without changing scores, flags, routing, loader or assembly code. Longest-choice tiers across the 8 authored question nodes are now `Developing, Developing, Developing, Strong, NeedsWork, NeedsWork, NeedsWork, NeedsWork`; only 1 node is Strong-longest. Independent traversal reports 0…1 Strong-longest prompts on every 6-question route, 729/729 terminal paths, and zero dead ends, missing destinations, unavailable selected destinations or choice loss. English and pending-review Malay values remain exact matches for all 109 keys.
- 2026-07-14 Task 9 review-fix verification: the final Task 9 aggregate reported `20/20` passed with `0` failures, skips or inconclusive tests and a 368-line log with `0` failure markers. The canonical full EditMode wrapper reported `263/263` passed; independent XML parsing confirmed `Passed` with `0` failures, skips or inconclusive tests, and the complete 365-line log contained `0` failure markers.
- 2026-07-14 Task 9 second-review RED against `120271a`: after correcting one test-harness assertion that could not count a lazy sequence, the valid focused content fixture reported `8/11` passed with exactly three intended failures. Every route had 3 Strong answers in the shortest position instead of 2, authored nodes had 4 middle-ranked Strong answers instead of the allowed 2–3, and the authored tier-mean length spread was `20.625` characters against the `≤12` guard. The complete 389-line log contained `0` compile/exception markers.
- 2026-07-14 Task 9 second-review GREEN: authored Strong-answer length ranks are now exactly 2 shortest, 3 middle and 3 longest. Mean response lengths are Strong `119.125`, Developing `125.75` and NeedsWork `123.125`, a `6.625`-character spread. All 486 standard and 243 recovery routes contain exactly 2 Strong answers at each rank; standard route tier means are `117.333/125.333/123.5` (spread `8.0`) and recovery means are `121.167/125.333/129.833` (spread `8.667`). All remain within the route-level `≤12` guard, with existing 729-path, six-question, three-choice, zero-dead-end/destination-loss checks unchanged and exact 109-key pending Malay fallback parity preserved.
- 2026-07-14 Task 9 second-review verification: the Task 9 aggregate reported `21/21` passed with `0` failures, skips or inconclusive tests and a complete 367-line log with `0` failure markers. The canonical full EditMode wrapper reported `264/264` passed; independent XML parsing confirmed `Passed` with `0` failures, skips or inconclusive tests, and the complete 365-line log contained `0` failure markers.
- 2026-07-14 Task 10 RED: the focused `PitchSessionControllerTests` run produced no XML and reached the expected missing-production-type boundary with `CS0246` for `PitchSessionController`; the complete 727-line log ended with `Scripts have compiler errors.`
- 2026-07-14 Task 10 focused GREEN: the orchestration fixture reported `10/10` passed with `0` failures, skips or inconclusive tests; independent XML parsing confirmed `Passed`, and the complete 431-line Unity log contained `0` matches for `error CS\d+|Compilation failed|Unhandled Exception`.
- 2026-07-14 Task 10 all Edit Mode GREEN: the canonical wrapper reported `274/274` passed with `0` failures, skips or inconclusive tests; independent XML parsing confirmed `Passed`, and the complete 366-line log contained `0` failure markers.
- 2026-07-14 Task 10 orchestration checks: launch enters Title only with a valid matching LMS configuration; start creates a fresh Briefing run; tutorial traversal is unscored; authored responses update capped rubric scores and confidence; reaction is published before feedback; conditional destinations are respected; terminal feedback builds a validator-clean completion payload; synchronous submission success reaches Complete; retry increments the attempt and clears dialogue flags, scores, confidence, timer, timeouts, selected IDs, presentation state, result and payload.
- 2026-07-14 Task 10 timeout contract: expiry chooses the first available `Developing` response (or stable first available response) solely as a deterministic graph traversal seam. It applies no authored score/confidence, records no learner-selected response ID, increments the timeout count and publishes a typed neutral timeout reaction/feedback event.
- 2026-07-14 Task 10 assembly adaptation: orchestration sources remain under `Assets/Scripts/Core/Session` and namespace `Agrovator.PitchSimulator.Core`, but compile in child `Agrovator.PitchSimulator.Session` with explicit Core, Dialogue, Scoring, Accessibility and LMS references. The foundational Core assembly remains acyclic and engine-free; the Session sources and asmdef contain `0` Unity engine type references and declare `noEngineReferences: true`.
- 2026-07-14 Task 10 review RED: the missing disposal API first produced no XML and `CS1061` in a 707-line log. Adding only the no-op seam exposed the behavioral baseline at `7/12` passed with exactly five failures: timeout response flags leaked into availability/recovery and broke the existing retry route, a stale submission callback repopulated error after retry, `FeedbackReady` observed `ShowingReaction`, and a disposed controller still reacted to its shared timer. The complete 392-line behavioral RED log contained `0` compile/exception markers.
- 2026-07-14 Task 10 review GREEN: the focused orchestration fixture reported `12/12` passed with `0` failures, skips or inconclusive tests; independent XML parsing confirmed `Passed`, and its complete 391-line log contained `0` failure markers. `DialogueSession.SelectForTimeout` advances only when the response and destination are available under existing flags and never applies response `SetFlags`; submission callbacks are one-shot, generation/attempt scoped and accepted only in Submitting; snapshots refresh before feedback events; and idempotent controller disposal unsubscribes the timer and rejects later commands.
- 2026-07-14 Task 10 review full GREEN: the canonical wrapper reported `276/276` passed with `0` failures, skips or inconclusive tests; independent XML parsing confirmed `Passed`, and the complete 364-line log contained `0` matches for `error CS\d+|Compilation failed|Unhandled Exception`.
- 2026-07-14 Task 10 final-review RED: the expanded controller fixture reported `12/14` passed with exactly two failures. A timed node whose only response required projected response flags remained stuck in AwaitingResponse, and a delayed `onFailure(null)` callback was misclassified as success and reached Complete. The complete 389-line log contained `0` compile/exception markers.
- 2026-07-14 Task 10 final-review GREEN: the focused controller fixture reported `14/14` passed with `0` failures, skips or inconclusive tests; the complete 386-line log contained `0` failure markers. When no flag-free timeout traversal exists, expiry now publishes the neutral timeout reaction without advancing dialogue; reaction/feedback then return to the same answerable node and reset its timer. LMS success and failure callbacks use explicit handlers, and null failure details synthesize a sanitized `SubmissionFailed` error for the active attempt.
- 2026-07-14 Task 10 final-review full GREEN: the canonical wrapper reported `278/278` passed with `0` failures, skips or inconclusive tests; independent XML parsing confirmed `Passed`, and the complete 364-line log contained `0` matches for `error CS\d+|Compilation failed|Unhandled Exception`. Smart School Garden content and its mutually exclusive node conditions remain unchanged.

- 2026-07-14 Task 11 PlayMode RED: the initial bootstrap fixture reported `0/1` with the expected scene-contract failure because `Bootstrap` was absent from the active Build Settings; the complete Unity log contained `0` compile/exception failure-marker matches.
- 2026-07-14 Task 11 builder GREEN: `PitchSimulatorProjectBuilder.BuildProjectFoundation` compiled and completed in batch mode twice with `0` failure markers. Each run replaces the three generated scene hierarchies, explicitly rewires serialized references, and leaves Build Settings with exactly enabled `Bootstrap`, then enabled `Game`; `WebIntegrationTest` remains excluded from default launch order.
- 2026-07-14 Task 11 PlayMode GREEN: the bootstrap fixture reported `1/1` passed with `0` failures, skips or inconclusive tests. It verifies one persistent `Bootstrapper`, additive `Game` loading, one Canvas, one EventSystem, one router, all five deterministic screens and Title-only initial visibility; the complete 503-line log contained `0` failure markers.
- 2026-07-14 Task 11 EditMode regression GREEN: the canonical wrapper reported `278/278` passed with `0` failures, skips or inconclusive tests; independent XML parsing confirmed `Passed`, and the complete 364-line log contained `0` failure markers.
- 2026-07-14 Task 11 UI contract: `Bootstrapper` is the sole `DefaultExecutionOrder(-10000)` entry point and performs bounded loaded-scene validation before controller/router initialization. Runtime UI sources contain no `UnityEditor` dependency. Presenters are guarded, idempotently initialized MonoBehaviour bridges that remove only their own listeners and delegate all session commands to `PitchSessionController`; scoring, branching and timeout behavior stay in the pure session/domain layers.
- 2026-07-14 Task 11 review RED: the new Editor ownership fixture reached the intended missing-production-API boundary with exactly two `CS0117` errors for the absent noninteractive batch entry and no test XML; the log ended at `Scripts have compiler errors.` The PlayMode review assertions were authored in the same RED cycle and directly reference the UI assembly, initialization flags, focus state and isolated teardown behavior.
- 2026-07-14 Task 11 review Editor GREEN: the focused builder fixture reported `1/1` passed with `0` failures, skips or inconclusive tests; its complete 589-line log had `0` failure markers. A saved unowned sentinel root survives two builder calls while the generated root, Canvas, EventSystem and router remain singular. The interactive menu now saves or cancels modified scenes before work; the separate batch entry is noninteractive, opens targets additively, replaces only exact owned roots, preserves unrelated roots/open scenes and restores the original active scene.
- 2026-07-14 Task 11 review PlayMode GREEN: the strengthened fixture reported `1/1` passed with `0` failures, skips or inconclusive tests; its complete 502-line log had `0` failure markers. It waits for and asserts both `Bootstrapper.IsInitialized` and `GameScreenRouter.IsInitialized`, verifies Title default focus, Settings focus, focus restoration back to Title and Briefing focus after routing, then explicitly destroys the persistent bootstrap object and unloads foundation scenes in Unity teardown.
- 2026-07-14 Task 11 review contract/focus hardening: bootstrap success now requires a bounded Game-scene scan finding exactly one Canvas, EventSystem and router. Router validation requires distinct panels plus correctly nested presenter/default-selectable references for all five screens. Every route restores its screen default selectable, and the EventSystem serializes Title Start as `firstSelectedGameObject`; invalid contracts stop initialization safely.
- 2026-07-14 Task 11 review full GREEN: the noninteractive batch builder completed twice with exit `0` and `0` failure markers each. The canonical EditMode wrapper reported `279/279` passed with `0` failures, skips or inconclusive tests; independent XML parsing confirmed `Passed`, and the complete 546-line log contained `0` failure markers.

## Next action

Preserve the Task 20 evidence and close the documented human/environment gates before any production release. Generated development build, test, and browser artifacts remain ignored and must be reproduced after code, scene, content, toolchain, or deployment changes.

- 2026-07-14 Task 14 RED: the focused `AudioServiceTests` run produced no XML and reached the expected missing-production-API boundary with `CS0234` for the absent Audio namespace plus `CS0246` for `AudioCueBinding`, `AudioService`, `AudioCue`, `IAudioPlaybackChannel` and `IAudioDiagnostics`; the complete log ended with `Scripts have compiler errors.`
- 2026-07-14 Task 14 focused GREEN: the device-free audio fixture reported `7/7` passed and the Title user-gesture PlayMode assertion reported `1/1` passed, each with zero failures or compile/exception markers. The exact nine-cue inventory routes only `MusicLoop` to the looping music channel and the other eight cues to one-shot SFX. Pre-unlock calls are inert and unqueued; unlock is idempotent; mute preserves independent finite-safe clamped volumes; null, missing and unknown cues warn at most once through the injected development diagnostic seam.
- 2026-07-14 Task 14 generated-scene GREEN: the Editor builder fixture reported `1/1` passed with zero failure markers while invoking the builder twice. The generated Bootstrap owns exactly two distinct 2D `AudioSource` components with `playOnAwake=false`, explicit music/SFX loop policies and nine intentionally null clip bindings. `Bootstrapper` explicitly constructs the plain service, and Title Start/Settings callbacks invoke the idempotent gesture unlock synchronously before the first `ButtonPress` attempt; there is no `Awake`, `Start` or `Update` autoplay.
- 2026-07-14 Task 14 asset/licensing boundary: `Assets/Audio/PLACEHOLDERS.md` maps every cue to a reserved filename, channel, loop policy, recommended source/import treatment, provenance evidence and child-safe loudness review. It explicitly states that no final audio is included, licensed or claimed.
- 2026-07-14 Task 14 full regression GREEN: persistent project-builder runs 1 and 2 each exited `0` with one success marker and zero failure markers. Canonical EditMode reported `296/296` and canonical PlayMode `19/19`, both Passed with zero failures, skips, inconclusive tests or compile/exception markers. Incidental Unity 6.5 ProjectSettings/SceneTemplate output and unrelated Game/WebIntegrationTest serialization churn were removed; final scene hashes are Bootstrap `8BD9567B...`, Game `6FA0A5B0...` and WebIntegrationTest `2B0546A1...`.

- 2026-07-14 Task 15 RED: the initial focused PlayMode run produced no XML and stopped at the expected missing transport contract with `CS0246` for `IWebGlLmsTransport` and `WebGlLmsTransportFailure`; the late-launch refinement RED stopped at the expected missing `WebGlLmsBridgeHost.Initialize` seam. Both logs ended with `Scripts have compiler errors.`
- 2026-07-14 Task 15 focused GREEN: the review-hardened `LmsBridgePlayModeTests` reported `10/10` passed with zero failures, skips or inconclusive tests. It covers validated launch JSON, success/failure/expired/missing-config behavior, explicit attempt-preserving missing-configuration mapping, duplicate callback suppression, throttled Missing Config to resent Success recovery, JavaScript cancellation, interop-throw cleanup and allowlisted harness status rendering. Native WebGL submissions have a 30-second bounded pending lifetime.
- 2026-07-14 Task 15 scene contract: the repeatable Editor builder fixture reported `1/1` passed while restoring generated scenes byte-for-byte. Its WebIntegrationTest builder owns one generated root with a focused bridge host and explicit serialized diagnostics label, preserves unrelated roots through the shared ownership helper, and leaves default Build Settings as Bootstrap then Game only.
- 2026-07-14 Task 15 harness/security checks: both JavaScript sources parsed successfully; the versioned bridge and harness validate both source window and exact same origin, contain zero query-string credential patterns and zero console calls. The harness exposes Success, Failure, Expired and Missing Config modes, resends launch data, and renders only allowlisted completion summaries.
- 2026-07-14 Task 15 Bootstrap integration: the WebGL player constructs the real bridge and waits recoverably for asynchronous launch data at a throttled 0.2-second unscaled cadence, so Missing Config can later recover through Success plus Resend without repeated per-frame UTF-8 allocations. It then builds localization, accessibility and audio settings from the validated launch configuration; Editor and non-WebGL players retain the synchronous local mock. This justified scope expansion is required because Task 18 builds only Bootstrap/Game and Task 19 drives harness modes against that build.
- 2026-07-14 Task 15 review scene/callback hardening: the bridge host and callback receiver now live in matching MonoBehaviour filenames. The strengthened double-builder fixture reports `1/1`, verifies exactly one generated WebIntegration bridge host, a non-null serialized diagnostics label, zero missing scripts and exact enabled Bootstrap/Game-only Build Settings. Timed-out or interop-failed native submissions clear C# ownership and call a JavaScript cancellation export that deletes the browser pending entry, so stale result messages cannot dispatch callbacks. Harness completion status renders only four normalized allowlisted values or `Unknown`.
- 2026-07-14 Task 15 full regression GREEN: final review-fix canonical PlayMode reported `29/29` and EditMode `296/296`, both Passed with zero failures, skips, inconclusive tests or compile/exception markers. Incidental Unity ProjectSettings normalization and SceneTemplateSettings output were removed after verification.

- 2026-07-14 Task 12 PlayMode RED: the interaction fixture reached the expected missing-production-type boundary with `CS0246` for `ResponseListView`, `TimerView` and `ConfidenceView`; no XML was produced and the log ended at `Scripts have compiler errors.`
- 2026-07-14 Task 12 focused GREEN: `PitchInteractionPlayModeTests` reported `13/13` passed with zero failures, skips or inconclusive tests; the complete log contained zero compile/exception markers. It covers the fixed three-slot response pool, numbered display text, visible-only explicit navigation and focus, synchronous click/Submit one-shot locking, timer number/fill/pulse behavior, reduced-motion suppression, exact five-state confidence boundaries and controller-owned neutral timeout rendering.
- 2026-07-14 Task 12 generated-scene GREEN: the project builder completed twice with exit `0` and zero failure markers. The generated PitchRoom owns exactly three serialized response views plus timer and confidence views, while `Bootstrapper` remains the sole production `controller.Tick(Time.unscaledDeltaTime)` bridge and UI views only render immutable snapshot data.
- 2026-07-14 Task 12 full regression GREEN: canonical PlayMode reported `14/14` and canonical EditMode reported `280/280`, each with zero failures, skips, inconclusive tests or compile/exception markers. The Bootstrap PlayMode flow now traverses the tutorial into a real three-choice scored question and verifies localized English response text, numbering, focus, timer/confidence presence and duplicate-event locking in the generated scene. Five confidence-state keys preserve exact 114-key English/pending-Malay fallback parity.
- 2026-07-14 Task 12 scene ownership hygiene: the Editor builder test snapshots and restores all three generated scene assets byte-for-byte in `finally`. SHA-256 values captured before and after the full EditMode run matched exactly for Bootstrap, Game and WebIntegrationTest; incidental Unity 6.5 ProjectSettings normalization and SceneTemplateSettings output were removed.
- 2026-07-14 Task 12 review RED: the generated-scene fixture reported `0/2` with exactly the two intended failures and zero compile/exception markers. The visible countdown remained at `20` across real frames because controller ticks emitted no event, and response selection left EventSystem focus on disabled Response 1 instead of active Continue.
- 2026-07-14 Task 12 review GREEN: focused generated-scene PlayMode reported `2/2`; canonical PlayMode reported `15/15` and canonical EditMode reported `280/280`, all Passed with zero failure markers. `Bootstrapper` remains the sole clock owner and now forwards each refreshed immutable snapshot through a timer-only router/presenter path. `TimerView` updates text only when the displayed integer changes, while fill/pulse can animate each frame; pulse resets synchronously outside AwaitingResponse. Router focus now uses serialized Response 1 only while awaiting an answer and serialized Continue for reaction, feedback and other pitch-room continuation states. Submit on focused Continue advances exactly one state. Builder review runs passed twice and all three scene SHA-256 hashes again matched exactly before/after EditMode.
- 2026-07-14 Task 13 reaction RED: the focused PlayMode run produced no XML and reached the intended missing-production-type boundary with `CS0246` for `JudgeReactionSpriteSet`; its Unity log ended at `Scripts have compiler errors.`
- 2026-07-14 Task 13 focused GREEN: `JudgeReactionPlayModeTests` reported `2/2` passed with zero failures, skips or inconclusive tests and no compile/exception markers. The typed map contains exactly 11 states; unknown cues and missing sprites fall back to Encouraging. Talk alternates only while question text is visible, idle blink defaults to a low frequency, authored reactions play once then settle, and reduced motion keeps the semantic reaction static while suppressing talk/blink loops. The view's per-frame path allocates no collections or strings.
- 2026-07-14 Task 13 import GREEN: `PixelArtImportTests` reported `6/6` passed. Source dimensions are Judge `1408x160`, room `1280x720`, panel `768x384` and confidence `480x96`; all import as Sprite/Point/no mipmaps/uncompressed/32 PPU/clamp. Judge Aya slices into 11 named `128x160` cells, confidence into 5 named `96x96` cells, and the dialogue panel is Single with a 24-pixel nine-slice border. The postprocessor lives in an Editor-only child assembly; runtime UI sources contain no `UnityEditor` reference.
- 2026-07-14 Task 13 generated-scene GREEN: the final project builder completed twice with exit `0`, success markers and zero failure markers. The generated PitchRoom includes the environment, sliced dialogue panel, confidence artwork and a fully serialized Judge Aya image/map while retaining the existing response/focus contracts. Focused generated-scene PlayMode reported `2/2` passed, including question Talk and authored-reaction rendering.
- 2026-07-14 Task 13 full regression GREEN: final canonical PlayMode reported `17/17` and canonical EditMode reported `286/286`, both Passed with zero failures, skips, inconclusive tests or compile/exception markers. Pre/post-suite SHA-256 hashes matched exactly for Bootstrap (`226ED848...`), Game (`DC0AA474...`) and WebIntegrationTest (`356C6C0B...`); incidental Unity 6.5 ProjectSettings normalization and SceneTemplateSettings output were removed after verification.
- 2026-07-14 Task 13 art provenance: the four visually approved project-bound outputs remain at their planned `Assets/Art` paths. `CREDITS.md` and machine-readable `manifest.json` record the built-in OpenAI image-generation prompts, dimensions, crop/downscale/chroma processing, project-use license statement and `thirdPartyDerivatives: false`.
- 2026-07-14 Task 13 review RED: canonical PlayMode reported `16/18` with exactly two intended failures: authored `Curious` resolved to generic Encouraging, and the confidence glyph remained active when artwork existed. The valid Editor UI fixture reported `7/10` with exactly three intended failures: missing contrast-backing hierarchy, `Curious` content mapping, and obsolete importer source. The corrected clean-reimport test already passed and restored both sprite metas exactly in `finally`.
- 2026-07-14 Task 13 review visual GREEN: the builder fixture reported `1/1` passed. Ink `#0E171F` against cream `#F4EAD5` measures `15.139:1` for the prompt; the reversed pair gives the same ratio for status, confidence and timer copy. Opaque, compact, raycast-free backings protect text from variable artwork without obscuring the room; the `1280x720` reference, preferred-height budget and direct-child rect containment all pass.
- 2026-07-14 Task 13 review runtime/importer GREEN: PlayMode reported `18/18`; `PixelArtImportTests` reported `9/9`; generated-scene PlayMode reported `2/2`. Confidence artwork and glyph are mutually exclusive with runtime missing-sprite fallback. The existing authored `Curious` cue maps explicitly to typed Interested while truly unknown cues still map to Encouraging. Sprite slicing now uses the Unity 6 built-in `com.unity.2d.sprite` `1.0.0` package and `Unity.2D.Sprite.Editor` data-provider API (`ISpriteEditorDataProvider`, `SpriteRect`, name/file-ID provider), with zero `CS0618` markers.
- 2026-07-14 Task 13 review full GREEN: final canonical PlayMode reported `18/18` and EditMode `289/289`; final builder runs both exited `0` with success markers and zero failure markers. Scene and sprite-meta SHA-256 values matched before/after tests exactly: Bootstrap `372ACAA1...`, Game `6FA0A5B0...`, WebIntegrationTest `09BCF104...`, Judge meta `F20D01B0...`, Confidence meta `8ACD53DB...`. Incidental ProjectSettings/SceneTemplate output and unrelated scene file-ID churn were removed after verification.

- 2026-07-14 Task 16 RED: the focused `ResultsPlayModeTests` run produced no XML and stopped only at the expected missing-production-type boundary, with `CS0246` for `QuestionReviewItemView`; the log ended with `Scripts have compiler errors.`
- 2026-07-14 Task 16 focused GREEN: `ResultsPlayModeTests` reported `4/4` passed with zero failures or compile/exception markers. Strong and recovery paths assert overall/final confidence, pitching/communications rollups, two strengths, two improvements and immutable ordered response/feedback/explanation review history. Retry clears the complete attempt state. Success, Submitting, generic Failure, Expired and Missing Configuration render localized statuses; failed submissions retain review and can be submitted again.
- 2026-07-14 Task 16 generated-scene GREEN: the project builder completed twice with exit `0`; the strengthened Editor fixture reported `1/1`. The Results screen owns a fixed six-item review pool under a bounded vertical `ScrollRect`, explicit serialized presenter references, localized headings/action labels, explicit Submit/Retry navigation and 1280x720-contained non-scrolling chrome.
- 2026-07-14 Task 16 full regression GREEN: canonical PlayMode reported `33/33` and EditMode `296/296`, both Passed with zero failures, skips, inconclusive tests or compile/exception markers. Bootstrap and WebIntegrationTest scene churn plus Unity ProjectSettings/SceneTemplate normalization were removed; only the required generated Game results hierarchy remains in scope.
- 2026-07-14 Task 16 review-fix GREEN: focused Results PlayMode reported `6/6`, the builder contract reported `1/1` across two successful runs, and fresh canonical PlayMode/EditMode reported `35/35` and `296/296` with zero failures. Keyboard review scrolling, review focus, and retry focus contracts are covered.
- 2026-07-14 Task 17 documentation scope: added the numbered `docs/00-...` through `docs/18-...` handoff set, four ADRs, ten constrained Claude prompts, two executable Codex prompts, a 16-phase production roadmap, and accurate README/CHANGELOG status. Documentation validation checks names/counts, required headings, placeholder markers, relative links, empty headings, roadmap phase field coverage, diff whitespace, and docs-only scope; Unity suites were not rerun because no runtime/project file changed.
- 2026-07-14 Task 18 RED/GREEN: the focused EditMode fixture first stopped at the intended missing `WebGlBuild` compiler boundary, then reported `4/4` Passed with zero failures or compile/exception markers. It covers exact enabled Bootstrap/Game order, deterministic Development WebGL options, Unity-API Player settings, current template tokens, responsive 16:9/accessibility behavior and absence of template audio/console calls.
- 2026-07-14 Task 18 full regression: canonical PlayMode reported `35/35` and EditMode `300/300`, both Passed with zero failures, skips, inconclusive tests or compile/exception markers. Known Unity ProjectSettings/SceneTemplate normalization was removed while retaining intentional AGROVATOR WebGL settings.
- 2026-07-14 Task 18 review fix: focused Unity RED reported `3/4` with the sole expected initial-warning failure, while the Node layout RED reported `0/3` for the missing hidden state, pure calculator and measured-chrome/coalescing contracts. GREEN reported Unity `4/4` and Node `3/3`. The warning alert now starts hidden; layout reserves computed body padding, shell row gap, button height/margins and visual-viewport height. ResizeObserver work is animation-frame coalesced and unchanged width writes are skipped.
- 2026-07-14 Task 18 corrected build: fresh canonical PlayMode reported `35/35` and EditMode `300/300`. The final approved warm-cache wrapper completed in `50.886` seconds; BuildReport reported `Succeeded`, `92,357,339` bytes, `00:00:04.4515450`, zero build warnings and zero errors. Seven files were emitted, including the `1,083`-byte layout calculator, with `92,349,401` plain payload bytes and no `.gz`, `.br` or `.unityweb` artifact. The full failure scan was clean; two clean-shutdown `Curl error 42: Callback aborted` diagnostics were non-build messages. `Build/` remains ignored and excluded from source control.
- 2026-07-15 Task 19 server RED/GREEN: the initial self-test exited `1` because `tools/Serve-WebGL.ps1` did not exist. The final loopback-only self-test passed hosted harness/build HTML, CSS, JavaScript, data, WebAssembly, favicon, HEAD, traversal, missing-file and method checks; every temporary child listener was stopped and its port verified closed.
- 2026-07-15 Task 19 automated browser matrix: Playwright `1.61.1` passed Chrome `150.0.7871.115` in `6.957 s` and Edge `150.0.4078.65` in `7.452 s`; Firefox was absent at both standard Windows paths and Safari is unavailable on Windows. Each passing run completed tutorial plus six scored questions with pointer and keyboard responses, Failure-to-success resubmission, Retry, Missing Config-to-Success recovery, desktop/mobile 16:9 containment, zero console errors and zero page errors. Ignored evidence is under `artifacts/smoke/`; the server PID was stopped in `finally`.
- 2026-07-15 Task 19 warning boundary: headless runs recorded the intentional null-`ButtonPress` audio warning and the existing non-root `DontDestroyOnLoad` Bootstrapper warning. No claim is made for human hearing, real touch hardware, unrestricted fullscreen, Firefox or Safari.
- 2026-07-15 Task 19 in-app Browser evidence: default and `390x844` override layouts had no horizontal overflow and contained the 16:9 canvas; reload restored launch/bridge/frame progress plus a fresh Title. No app audio warning appeared before Start; pointer Start reached Briefing and only then emitted the expected null-`ButtonPress` warning, proving user-gesture/no-autoplay semantics without claiming audible content. Touch-sized pointer Start passed, while native touch events were unavailable. The fullscreen control was uniquely found and exercised, but the controlled browser denied permission and left `fullscreenElement=false`; unrestricted-browser fullscreen remains unverified. A URL-less MutationObserver injection error was tool-side and absent from automated product page-error evidence. The manual server was stopped and port `4173` verified closed.
- 2026-07-15 Task 20 fresh suites/build: canonical EditMode passed `300/300` in `53 s` and PlayMode passed `35/35` in `50.6 s`. The WebGL wrapper exited `0` in approximately `50 s`; BuildReport was `Succeeded`, `92,357,339` bytes in `00:00:03.7963558`, with zero warnings/errors and the exact expected seven-file inventory (`5,367`, `2,571`, `1,083`, `58,622`, `711,897`, `9,288,984`, and `82,288,815` bytes).
- 2026-07-15 Task 20 browser matrix: generated `2026-07-14T16:27:06.205Z` (`00:27:06.205` local) with Playwright `1.61.1`; Chrome `150.0.7871.115` passed in `7,462 ms` and Edge `150.0.4078.65` passed in `7,172 ms`, both with zero console/page errors and launch/Success/Failure/Missing Configuration present. The server stopped cleanly with zero stderr. Firefox was unavailable at standard Windows paths; Safari remains unavailable on Windows.
- 2026-07-15 Task 20 audit: all 18 requested implementation capabilities have direct automated/authored evidence. Privacy scanning found no production secret, email, credential query, secret-named tracked file, direct identity field (name, email, school, date of birth, or address), credential, raw answer text, or open-ended learner input; deliberate mock/pseudonymous and malformed negative-test fixtures remain documented. Pseudonymous IDs, scores, timestamps, and selected response IDs remain learning records requiring retention/privacy approval. All `13` non-code assets are consolidated in `docs/16-ASSET-MANIFEST.md`; four PNGs match provenance and dimensions, while the missing repository licence and unarchived OpenAI terms/account authority remain human release gates.
- 2026-07-15 Task 20 boundaries: the intentional null-clip and existing non-root `DontDestroyOnLoad` warnings remain. No claim is made for final audio, approved Malay, real LMS submission, classroom usability, human accessibility review, native touch, unrestricted fullscreen, Firefox, Safari, production hosting, or legal/release approval.
- 2026-07-15 final whole-branch review repair: focused RED reproduced raw/advanced pitch-room prompt text (`14/16` PlayMode), missing Complete-to-Retry transitions (`20/22` EditMode), and the absent audio director (`CS0246`). GREEN passed pitch-room `16/16`, retry `22/22`, audio-director `6/6`, and Bootstrap integration `2/2`. Learner-facing question/feedback/explanation text is localized and post-response choices are hidden; Retry now starts a fresh attempt from successful completion; all nine audio cues are connected to user, session, or timer events while null clips remain non-blocking.
- 2026-07-15 final post-review acceptance: canonical EditMode passed `308/308` in `27.5 s` and PlayMode passed `36/36` in `48.7 s`. The WebGL wrapper exited `0` in `81.1 s`; BuildReport was `Succeeded`, `92,361,436` bytes in `00:00:53.9826352`, with zero warnings/errors and seven files (`5,367`, `2,571`, `1,083`, `58,622`, `711,897`, `9,289,560`, and `82,292,336` bytes).
- 2026-07-15 final browser RED/GREEN: the first post-fix matrix exposed instantaneous canvas clicks being mistaken for focus-color changes while Unity missed the frame-polled press. Holding every pointer press for `120 ms` then passed Chrome `150.0.7871.116` in `6,566 ms` and Edge `150.0.4078.65` in `6,316 ms`, with zero console/page errors and Success, Failure, Missing Configuration, successful-completion Retry through fresh Question 1, desktop/mobile containment, and clean server shutdown. Firefox remained unavailable.
- 2026-07-15 UI-polish Task 1 RED: the isolated EditMode catalog assertion reported exactly `1/308` failure because the 13 tutorial/UI keys were absent, and PlayMode reached the intended missing-production-type boundary with `CS0246` for `TutorialPresenter`.
- 2026-07-15 UI-polish Task 1 GREEN: canonical EditMode passed `308/308` and PlayMode passed `39/39`; all three new tutorial presenter cases passed, both logs had zero compilation/Unhandled Exception markers, and the 145-key reviewed-English/pending-Malay catalogs retain exact key and value parity.
- 2026-07-15 UI-polish Task 2 RED: canonical EditMode reported `307/308` with the sole failure at the missing generated `Canvas/Tutorial`; PlayMode reported `37/39`, with both failures caused by the absent Tutorial screen/path. Both logs had zero compilation/Unhandled Exception markers.
- 2026-07-15 UI-polish Task 2 GREEN: the project builder exited `0` with one success marker and zero failure markers, generated the dedicated Tutorial hierarchy plus serialized router ownership in `Game.unity`, and final canonical EditMode/PlayMode passed `308/308` and `39/39` with zero failures, skips, inconclusive tests or compile/exception markers. Unity ProjectSettings/SceneTemplate normalization and unrelated Bootstrap/WebIntegrationTest file-ID churn were removed after verification.
- 2026-07-15 UI-polish Task 2 diff-check fix RED/GREEN: `git diff --check 5ee02dd..HEAD` initially exited `2` with `600` diagnostics, all from Unity's standard blank `m_Name:`/`m_Text:` scalar serialization in generated `Game.unity`; the scoped `*.unity` attribute now preserves LF text handling while disabling only `trailing-space`, and the same command exits `0` with no output. `git check-attr whitespace -- Assets/Scenes/Game.unity` reports exactly `whitespace: -trailing-space`; C#, JSON and Markdown probes remain `unspecified`.
- 2026-07-15 UI-polish Task 3 RED: after normalizing the generated Canvas to its declared 1280x720 reference size inside the EditMode fixture, the canonical suite reported `307/308` with the sole expected failure `Title must own a Content Frame`; the 500-line log contained zero compile/exception failure markers.
- 2026-07-15 UI-polish Task 3 builder GREEN: `PitchSimulatorProjectBuilder` exited `0` with one success marker and zero failure markers, persisted exactly four generated `Content Frame` objects, and migrated all Title, Briefing and Settings bootstrap/test paths beneath those frames. Known ProjectSettings/SceneTemplate normalization plus unrelated Bootstrap/WebIntegrationTest file-ID churn was removed.
- 2026-07-15 UI-polish Task 3 final GREEN: canonical EditMode reported `308/308` and PlayMode `39/39`, both Passed with zero failures, skips or inconclusive tests; their complete 609/920-line logs contained zero `error CS`, compilation-failure or unhandled-exception markers. `git diff --check` exited `0`, stale direct simple-screen paths were absent, and Pitch Room/Results retained their prior layout contract for Tasks 4/5.
- 2026-07-15 UI-polish Task 3 layout-review RED: the builder fixture now activates each simple screen before forcing layout, restores and asserts its prior active state, checks all exact frame sizes and 64px minimum targets, and reported `307/308` with one aggregated expected failure. Effective widths were Tutorial `243.353/243.353/301.294` instead of `180/180/420` and Settings labels `648/648` instead of `680/680`; the 500-line log had zero compile/exception markers.
- 2026-07-15 UI-polish Task 3 layout-review GREEN: the approved Tutorial-specific override sets Back/Skip/Next to `180/180/420` inside the existing 820px row, while Settings uses 20px horizontal card padding so both labels render at 680px. The regenerated builder log had one success marker and zero failure markers; final canonical EditMode/PlayMode passed `308/308` and `39/39`, with zero failures, skips or inconclusive tests and zero failure markers in the complete 617/918-line logs.
- 2026-07-15 UI-polish Task 4 RED: the active-scene, forced-layout Pitch Room fixture reported `308/309` with the sole expected failure that `PitchRoom/Content Frame` was absent; the complete log contained zero compile/exception failure markers.
- 2026-07-15 UI-polish Task 4 layout RED/GREEN: the first generated frame run reported `307/309` because Metrics rendered at `716px`; focused layout diagnostics traced this to uncontrolled default 100px indicator rects (`Confidence 460px + Timer 236px + 20px spacing`). After controlling indicator child widths, the next `307/309` run exposed Responses expanding to the frame's `912px` inner width; disabling response-row flexible expansion produced the required effective `680px` cap while retaining 64px response targets.
- 2026-07-15 UI-polish Task 4 builder GREEN: `PitchSimulatorProjectBuilder` regenerated the owned scenes with exit `0`, one success marker and zero compiler/exception markers. `Game.unity` now keeps Environment full-screen beside a centered `960x680` Content Frame with 24px padding, 8px spacing, a `680px` Metrics row, capped gameplay controls and preserved presenter/router references; unrelated Bootstrap/WebIntegrationTest file-ID churn plus ProjectSettings/SceneTemplate normalization was removed.
- 2026-07-15 UI-polish Task 4 final GREEN: the strengthened focused containment fixture passed `1/1`; canonical EditMode passed `309/309` and PlayMode passed `39/39`, with zero failures, skips, inconclusive tests or compile/exception markers. Coverage activates PitchRoom at the 1280x720 reference, forces nested layouts, verifies the exact hierarchy, rendered caps, full-screen Environment, descendant containment, visible metrics, 64px targets and 4.5:1 contrast while existing PlayMode paths retain focus, timer, response-locking, reaction and localized presenter behavior.
- 2026-07-15 UI-polish Task 5 RED: active-screen, forced-layout Results assertions raised canonical EditMode to 310 tests and reported `308/310`; both expected failures stopped at the missing direct `Results/Content Frame`, while the complete 533-line log contained zero compile/exception failure markers.
- 2026-07-15 UI-polish Task 5 builder GREEN: `PitchSimulatorProjectBuilder` exited `0` with one success marker and zero failure markers. `Game.unity` now owns `Results/Content Frame` at `960x680` with 32px horizontal/20px vertical padding, an effective `<=860px` flexible review scroll, and centered 260x64 Submit/Retry actions; the six-item pool, presenter references, and explicit keyboard scrollbar contract remain intact. Known Bootstrap/WebIntegrationTest/ProjectSettings/SceneTemplate normalization was removed.
- 2026-07-15 UI-polish Task 5 final GREEN: canonical EditMode passed `310/310` and PlayMode passed `39/39`, including `6/6` Results tests. Both XML roots reported Passed with zero failures, skips or inconclusive tests, and their complete 638/920-line logs contained zero compile/exception failure markers. Active forced layouts verify effective frame, scroll and button sizes plus frame/footer/viewport/scrollbar containment at the 1280x720 reference.
- 2026-07-15 UI-polish Task 5 accessibility-review RED: focused EditMode reported `0/1` with an effective `32px` scrollbar target, no separate visual track, and only `1.09:1` normal-to-selected focus contrast; focused PlayMode reported `0/1` because the rendered selection change was `1.0856:1`. Their complete 404/488-line logs contained zero compile/exception failure markers.
- 2026-07-15 UI-polish Task 5 accessibility-review focused GREEN: the regenerated Results scrollbar preserves its path, `KeyboardReviewScrollbar`, handle, permanent visibility and explicit navigation while using a raycast-enabled 64px target, 48px cream focus rail and centered 32px track. Builder exit was `0` with one success marker and zero failure markers; focused EditMode/PlayMode passed `1/1` and `1/1` with zero failure markers in 375/487 lines. Selected focus contrast against the scroll background is `14.09:1`.
- 2026-07-15 UI-polish Task 5 accessibility-review canonical GREEN: EditMode passed `310/310` and PlayMode passed `39/39`, including all `6/6` Results tests; both XML roots reported Passed with zero failures, skips or inconclusive tests, and their complete 605/916-line logs contained zero compile/exception failure markers. Known unrelated Unity scene/settings normalization was removed.
- 2026-07-15 UI-polish Task 6 RED: the exact Node source-contract command reported `3/9` passed and `6/9` failed, with all six expected failures caused by the absent three-page Tutorial labels, retry Skip label, and Chrome tutorial/pitch checkpoint paths.
- 2026-07-15 UI-polish Task 6 GREEN: `node --check tools/smoke-webgl.mjs` exited `0`, and `node --test tools/tests/*.test.js` passed `9/9` with zero failures. The main attempt now captures Chrome-only Tutorial/Q1 checkpoints and completes all three Tutorial pages with frame-held Enter; Retry uses the normalized `(0.50, 0.82)` pointer Skip with `120ms` dwell before the existing pointer-only practice flow proves a fresh Question 1 reveal. Browser error gates, lifecycle cleanup, sanitized bridge behavior and final smoke/failure screenshots remain unchanged.
- 2026-07-15 UI-polish Task 6 boundary: this source-contract task did not build WebGL or launch a browser; browser execution evidence belongs to Task 7.
- 2026-07-15 UI-polish Task 6 test-review RED: the formatting-tolerant structural fixture reported `3/5` passed and `2/5` failed. Its comment/dead-string rejection, Chrome guard/path/order and Q1 reveal checks passed; the expected failures exposed zero required `250ms` Tutorial settles and no executable retry `pointerAction` Skip.
- 2026-07-15 UI-polish Task 6 test-review GREEN: the compact no-dependency call extractor passes its adversarial comment/dead-string fixture and scopes assertions to executable `playAttempt` calls. The focused fixture passes `4/4`; it requires exactly three ordered Tutorial Enter calls plus three `250ms` settles, both exact Chrome checkpoint paths inside Chrome guards, pitch capture after the Q1 reveal gate, and a normalized `(0.50, 0.82)` retry `pointerAction` with `120ms` dwell followed by a keyboard-free pointer proof through fresh Q1.
- 2026-07-15 UI-polish Task 6 second-review mutation RED: the focused fixture reported `4/6` passed and `2/6` failed with `Missing expected exception`. In-memory source copies proved the prior analyzer accepted both an extra unlabeled executable Enter inside the Tutorial window and an unawaited `setTimeout(..., 250)` replacing a required settle.
- 2026-07-15 UI-polish Task 6 second-review GREEN: the Tutorial window is now bounded by its executable Chrome checkpoint and Judge-introduction calls. It requires exactly three total executable Enter actions with ordered page labels and exactly three alternating awaited Promise-based `250ms` settles; both mutations are rejected while formatting tolerance and comment/dead-string masking remain. Production smoke behavior was unchanged. The separate `extractFunction` boundary Minor remains intentionally deferred per review scope.
- 2026-07-15 UI-polish Task 7 canonical suites: fresh EditMode passed `310/310` and PlayMode `39/39`, with zero failures, skips, inconclusive tests, or configured compile/exception markers. NUnit durations were `2.3198379 s` and `2.3781601 s`; wrapper wall times were approximately `51 s` and `49.4 s`.
- 2026-07-15 UI-polish Task 7 build/server: `tools/Build-WebGL.ps1` exited `0` in `395.3 s`; BuildReport was `Succeeded`, `92,374,202` bytes and `00:06:03.3210118`, with zero warnings/errors and the expected seven files. `tools/Serve-WebGL.ps1 -SelfTest` passed on temporary loopback port `55900`, covering GET/HEAD, MIME, traversal, missing-path and method behavior.
- 2026-07-15 UI-polish Task 7 browser RED diagnostics: the first Chrome/Edge matrix failed at Tutorial response reveal because Continue rendered at normalized `y=0.8183…0.9040` while smoke clicked `0.91`; the second reached the response state but exposed response bounds `y=0.6779…0.7764` versus click `0.78`; the third reached successful completion/Retry but proved `(0.50,0.82)` hit Tutorial Next rather than Skip, whose measured center was `(0.4059,0.7928)`. Each defect was deterministic in both browsers and caught by the downstream stable-region proof rather than accepted as a focus-tint change.
- 2026-07-15 UI-polish Task 7 initial browser flow GREEN: three focused source-contract tests observed RED before the minimum coordinate corrections, then Node passed `11/11`. The initial matrix completed the required Chrome/Edge state/input paths with zero console/page errors, but its final screenshot followed only the first recovered canvas hash change. Review therefore invalidated the initial claim that the Edge final was a complete readable checkpoint; those visual timings/captures are superseded below.
- 2026-07-15 UI-polish Task 7 review RED: the focused Node command reported `2/5` passed and `3/5` failed. Comment/dead-string and `delay: 0` mutations produced `Missing expected exception`, proving both coordinate assertions accepted non-executable/incomplete contracts; the recovery assertion found zero post-recovery stable-ready calls, proving final capture could follow a partial first repaint.
- 2026-07-15 UI-polish Task 7 review GREEN: coordinate assertions now use the executable-call extractor, require exactly one `canvas.click`, exact measured coordinates, and `{ delay: 120 }`. Before final capture, recovered Briefing upper content (`20%-64%` canvas) and lower controls (`68%-98%`) must both change from Title and remain hash-identical for three consecutive `100ms`-polled samples within the existing timeout. Focused passed `5/5`, Node syntax exited `0`, and full Node passed `14/14`.
- 2026-07-15 UI-polish Task 7 repaired browser/visual GREEN: Playwright `1.61.1` passed Chrome `150.0.7871.116` (`6,716 ms` ready, `33.438 s` total) and Edge `150.0.4078.65` (`6,542 ms` ready, `32.951 s` total), each with zero console/page errors; Firefox remained unavailable and the server stopped with zero stderr. The regenerated Chrome/Edge finals are byte-identical (`SHA-256 5D85498F78B73CAF12CACA001385C013FEC73019B2B3256922589BAFC965ADE8`) and were opened individually: both show the complete readable harness, progress/completion panels, centered Briefing heading/body/card, action, fullscreen control, spacing, and gold focus outline, with no black partial repaint. Literal Edge tutorial/pitch files remain absent and are not claimed.
- 2026-07-15 UI-polish Task 7 records: at `390x844`, both browsers recorded a `324x218` iframe containing a `280x156.625` canvas. Fresh reconciliation found `109` logical Assets plus `145` metas (`109` file/`36` folder), zero missing/orphaned metas, four dimension-matched manifested PNGs, 145/145 localization parity, 46 Markdown files with zero broken relative links, and zero unexpected secret shapes.
- Next unchecked UI-polish action: controller rerun of the independent whole-branch review for `3271adf..HEAD`; Task 7 did not perform that review.
- 2026-07-15 UI-polish Task 7 branch-wide whitespace review RED: controller verification with `git diff --check 3271adf..HEAD` identified only trailing spaces on the design record's Date and Branch lines. The cleanup is limited to removing those two spaces; no runtime, test, scene, project-setting, build, or browser input changed, so heavy gates are not rerun. Final GREEN: the exact `git diff --check 3271adf..HEAD` exited `0` with no output after the cleanup commit.
- 2026-07-15 final Pitch Room text review RED: retained `artifacts/smoke/chrome-pitch.png` visibly ended the prompt at `garden`, response 1 at `timing is`, and confidence at `Curio`. Temporary generated-scene instrumentation measured the active `1280x720` layout at prompt `804x60`, response `680x64`, and confidence-label height `40`; UGUI preferred heights were `67…101`, up to `83`, and `50` respectively while `VerticalWrapMode.Truncate` was active. The focused generated-layout test then failed `0/1` with representative TextGenerator counts `54/69` for the introduction prompt, `116/130` for prefixed response 1, and `4/7` for `Curious`; the complete log had zero compile/exception markers.
- 2026-07-15 final Pitch Room text review GREEN: the single builder/layout repair bounds prompt best-fit to readable `22…24px`, renders response labels at `22px`/`0.9` line spacing inside `72px` targets with `240px` row allocation, and reserves `160px` for confidence labels while reducing only the decorative fill to `72px`. `Game.unity` was regenerated through `PitchSimulatorProjectBuilder`; unrelated generated-scene churn and known Unity settings noise were removed. Focused EditMode passed `1/1`; canonical EditMode passed `311/311` and PlayMode `39/39`, with zero failures, skips, inconclusive tests or compile/exception markers. Node syntax exited zero and Node passed `14/14`.
- 2026-07-15 final Pitch Room build/browser/visual GREEN: `tools/Build-WebGL.ps1` exited `0` in `48.8 s`; BuildReport was `Succeeded`, `92,374,282` bytes and `00:00:07.2820626`, with zero warnings/errors and the expected seven files. Server self-test passed on temporary loopback port `59732`. The final unchanged matrix rerun passed Chrome `150.0.7871.116` (`7,293ms` ready, `36.210s` total) and Edge `150.0.4078.65` (`7,549ms` ready, `36.559s` total), each with zero console/page errors; Firefox remained unavailable and server shutdown had zero stderr. A prior Edge attempt sampled stale desktop canvas bounds before the asynchronous mobile resize, while its later capture visibly showed containment; no product change was made for that one-off harness race.
- 2026-07-15 final Pitch Room image inspection: replacement `chrome-pitch.png` was opened directly and shows the complete prompt ending `system?`, response 1 ending `inconsistent.`, and confidence label `Curious`, with all three responses readable inside the contained card. Final Chrome and Edge PNGs were opened individually and are byte-identical (`SHA-256 5D85498F78B73CAF12CACA001385C013FEC73019B2B3256922589BAFC965ADE8`), showing complete recovered Briefing/harness content with no black partial repaint or overflow. Literal Edge tutorial/pitch files remain absent and are not claimed.
- 2026-07-15 public-demo publish RED: the first GitHub Pages Chrome run loaded the hosted WebGL player in `7,442 ms` and completed the full tutorial, scored flow, Results, submission modes, Retry, desktop/mobile containment and screenshot gates, but correctly failed the zero-console-error requirement. Chrome made one implicit request to `https://mkwebarchitects.github.io/favicon.ico`, outside the repository Pages subpath, and received `404`; the local server had masked the missing harness declaration with its explicit origin-root favicon response.
- 2026-07-15 public-demo favicon TDD GREEN: a focused Node test first failed `0/1` because `WebHarness/index.html` had no deployment-safe favicon declaration. The minimal inline `data:,` favicon then passed focused `1/1` and the full Node suite `15/15`, with zero failures, skips or todos. No Unity runtime, scene, content, generated WebGL binary or LMS payload behavior changed.
- 2026-07-15 public-demo final GREEN: public source `main` points to the tested favicon fix, isolated deployment commit `bd425cd` reached `gh-pages`, and GitHub Pages workflow `29415864996` completed successfully across build, deploy and status jobs. The unchanged hosted game then passed the complete Chrome `150.0.7871.116` smoke flow in `33.1 s`, reached readiness in `7,558 ms`, exercised tutorial, six questions, Results, Failure/Success submission, Retry, Missing Configuration recovery and desktop/mobile containment, and recorded zero console or page errors.
- Next unchecked UI-polish action: create the approved guided-pitch-builder implementation plan in a fresh Codex task, preserving the standalone repository boundary and public-demo baseline.

## Guided Pitch Builder

- [x] Planning: approved Primary/Secondary guided-builder TDD implementation plan
- [x] Task 1: Engine-independent pitch draft, revision, and assessment
- [x] Task 2: Validated Primary/Secondary guided content and localization
- [x] Task 3: Guided session phases, LMS compatibility, and Retry
- [x] Task 4: Reusable four-part board, cards, coaching, and responsive rules
- [x] Task 5: Guided presenter, mode selection, flow routing, and recovery
- [x] Task 6: Constructive four-dimension Results and resubmission
- [x] Task 7: Deterministic guided scenes and generated layout contracts
- [x] Task 8: DPR-sharp WebGL sizing and Chrome/Edge guided smoke
- [x] Task 9: Documentation, acceptance, privacy, and educator-review boundary

## Guided Pitch Builder verification evidence

- 2026-07-15 planning baseline: the worktree was clean and detached at exact source commit `ce7f8ac4f69aaf0b3645277041234938d6caaaa4`; `ce7f8ac` is also the `codex/pitch-simulator-ui-polish` and `origin/main` baseline named in the handoff.
- 2026-07-15 planning scope: `AGENTS.md` and the approved `docs/plans/2026-07-15-guided-pitch-builder-design.md` were read before repository mapping. No AGROVATOR LMS repository path was accessed, and no production source, content, scene, package, build, or test artifact was changed.
- 2026-07-15 plan artifact: `docs/superpowers/plans/2026-07-15-guided-pitch-builder.md` contains `9` independently reviewable TDD tasks, `53` checkbox steps, `9` planned task commits, exact file/interface/RED/GREEN/verification instructions, stable Primary/Secondary content IDs and copy, and the required execution sub-skill header.
- 2026-07-15 design self-review: all approved sections—Purpose, Core Learning Flow, modes, feedback/assessment, visual direction, technical boundary, errors, verification, and non-goals—map to explicit plan tasks. The review repaired the guided audio-event adapter, exact Node path, generated-builder command, viewport-metrics ownership, asset/meta/privacy commands, and pre-commit whole-diff boundary.
- 2026-07-15 plan quality gates: the placeholder scan returned `NO_PLACEHOLDERS`; the command scan returned `NO_BAD_COMMANDS`; all required design terms were present; `106` Markdown fence markers were balanced; and `git diff --check` exited `0` with no output before this TASKS update.
- 2026-07-15 planning verification boundary: Unity, Node, WebGL build, browser smoke, and visual suites were not rerun because this commit changes planning records only and intentionally implements no production code. Their exact RED/GREEN gates are specified task-by-task in the plan.
- 2026-07-15 guided-builder Task 1 RED: the sandboxed canonical EditMode attempt stopped before import with Unity licensing exit `198` and was rejected as evidence. The approved escalated `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tools\Run-UnityTests.ps1 -Platform EditMode` invocation then produced no XML and reached the expected missing-production boundary: the complete 734-line log contained `CS0234` for the absent `Agrovator.PitchSimulator.GuidedPitch` namespace and `CS0246` for the absent draft/mastery types, then ended with `Scripts have compiler errors.`
- 2026-07-15 guided-builder Task 1 GREEN: the canonical EditMode wrapper reported `327/327` passed. Independent XML parsing confirmed `Passed` with zero failures, skips, or inconclusive tests; the complete 678-line log contained zero `error CS\d+|Compilation failed|Unhandled Exception` markers.
- 2026-07-15 guided-builder Task 1 formula/behavior checks: all four slots, immutable copied snapshots, initial-response preservation, revision/reset behavior, blank IDs, unknown enums, and read-only assessment mastery output are covered. Empty/Needs Practice/Developing/Clear readiness contributions are `0/10/20/25`; competency values are `0/40/70/100`; `Clear/Developing/Needs Practice/Clear` produces `80` readiness and rounded populated means of `78`; only current-over-initial revised mastery increments `ImprovedPartCount`.
- 2026-07-15 guided-builder Task 1 boundary: Unity generated the two folder metas and five file metas. `rg -n "UnityEngine|MonoBehaviour|ScriptableObject" Assets/Scripts/GuidedPitch` returned no matches; the new asmdef has no references and declares `noEngineReferences: true`. Existing unrelated `ProjectSettings/ProjectSettings.asset` and `ProjectSettings/SceneTemplateSettings.json` workspace changes were preserved and excluded.
- 2026-07-15 guided-builder Task 2 RED: the sandboxed canonical attempt was rejected as evidence after Unity licensing exit `198`. The approved `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tools\Run-UnityTests.ps1 -Platform EditMode` rerun produced no XML and reached the expected missing-content compiler boundary with eight distinct `CS0246` diagnostics for the absent guided DTO/runtime types; the complete log ended with `Scripts have compiler errors.`
- 2026-07-15 guided-builder Task 2 API review RED/GREEN: a test-first public-contract review produced four expected `CS0117` diagnostics for the not-yet-named value-aware loader/validator seam. The minimal rename leaves `Load(string, IEnumerable<string>)` as the sole unambiguous required `Load` API while reading-level tests use explicit `LoadWithLocalizationValues`/`ValidateWithLocalizationValues` methods.
- 2026-07-15 guided-builder Task 2 GREEN: the final canonical wrapper reported `347/347` EditMode tests passed. Independent XML parsing confirmed `Passed` with zero failures, skips, or inconclusive tests; the complete 651-line log contained zero `error CS\d+|Compilation failed|Unhandled Exception` markers and one successful test-run completion marker.
- 2026-07-15 guided-builder Task 2 content checks: version `2` and checksum `guided-pitch-builder-v2-authored` compile into two terminating Primary/Secondary routes, each with four ordered pitch parts and one follow-up. All `30/30` option IDs and `90/90` per-option feedback keys are ordinal-unique; Primary sentences span `12-14` whitespace-delimited words and the Secondary maximum is `26` against its `32`-word cap.
- 2026-07-15 guided-builder Task 2 localization checks: English and pending-review Malay each contain `300` keys, including `155` guided keys, with zero key or value parity differences. All exact labels and transfer copy resolve, the learner-judging scan found zero forbidden phrases, and Malay retains `pending_human_review` English fallback values.
- 2026-07-15 guided-builder Task 2 boundary: validation returns document-ordered immutable issues exposing only `Code`, `Path`, and `Severity`; loaders reject errors before compilation and runtime dictionaries/lists are defensive read-only copies. Unity generated all six required file metas with six unique GUIDs; guided runtime sources contain zero `UnityEngine`, `MonoBehaviour`, or `ScriptableObject` references. Unity-generated ProjectSettings normalization was removed from the scoped diff.
- 2026-07-15 guided-builder Task 2 review-fix RED: a focused 33-word Secondary localization-value test raised the canonical suite to `348` and reported `347/348` passed with the sole expected failure `Validate_SecondarySentenceOver32WordsReturnsStructuredError` (`Expected: not null`, `But was: null`). Independent XML parsing confirmed one failure with zero skips or inconclusive tests; the complete 653-line log contained zero compile/exception markers.
- 2026-07-15 guided-builder Task 2 review-fix GREEN: the minimum value-aware validator branch now emits structured `guided.secondary_word_count_invalid` at the option `TextKey` path above 32 words while preserving Primary's `12-16` rule and all public APIs. The canonical wrapper reported `348/348` passed; independent XML parsing confirmed `Passed` with zero failures, skips, or inconclusive tests and duration `1.9955983 s`; the complete 642-line log contained zero compile/exception markers and one successful completion marker.
- 2026-07-15 guided-builder Task 3 RED: the sandboxed canonical EditMode attempt was rejected after Unity licensing exit `198`. The approved `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tools\Run-UnityTests.ps1 -Platform EditMode` rerun reached the intended missing-production compiler boundary: no XML was created, the complete `744`-line log reported only `CS0246` for the absent `GuidedPitchSessionController`, and ended with `Scripts have compiler errors.` The focused `node --test .\tools\tests\web-harness.test.js` run passed `1/2`; the sole expected failure showed the harness still declared `ContentVersion: 1` instead of `2`.
- 2026-07-15 guided-builder Task 3 GREEN: the final canonical wrapper reported `359/359` EditMode tests passed. Independent XML parsing confirmed `Passed` with zero failures, skips, or inconclusive tests and duration `2.1899152 s`; the complete `660`-line log contained zero `error CS\d+|Compilation failed|Unhandled Exception` markers and one successful completion marker. The focused harness suite passed `2/2`, and `node --test .\tools\tests\*.test.js` passed `16/16` with zero failures, skips, or todos.
- 2026-07-15 guided-builder Task 3 state and revision proof: the separate active-ready controller covers `Booting -> Title -> Briefing -> ModeSelection -> Learn -> Build/BuildFeedback x4 -> Improve -> Present -> FollowUp/FollowUpFeedback -> Results`, with every phase, active part, option ID set, feedback key, immutable board snapshot, readiness value, and event order asserted. Wrong-phase, wrong-part, wrong-mode, missing-ID, unknown-enum, premature-Present, and Clear-part revision commands return `false` without mutation; explicitly selected weaker revision remains allowed and preserves honest initial/current mastery.
- 2026-07-15 guided-builder Task 3 payload and duration proof: the exercised revised Primary result is readiness/overall score `100`, six ordered competencies `problem,evidence,solution,audience,clear_explanation,communication`, legacy confidence `70`, chronological selected IDs `primary-problem-needs-practice,primary-evidence-clear,primary-solution-clear,primary-value-clear,primary-problem-clear,primary-cost-clear`, duration `12.5`, and timeout count `0`. Source scans found no `QuestionTimer`, timeout increment, timer UI field, or response `TextKey` use in the new session files.
- 2026-07-15 guided-builder Task 3 Retry/submission proof: sanitized submission failure returns to Results while preserving revised draft, assessment, payload, and history for resubmission; duplicate callbacks, callbacks after completion, and prior-attempt callbacks after Retry are ignored. Retry increments attempt `2 -> 3`, returns to Briefing, and explicitly clears learner mode, active revision part, all initial/current IDs and `WasRevised` flags, readiness, feedback, follow-up, selection history, payload, and submission error.
- 2026-07-15 guided-builder Task 3 LMS/privacy boundary: supported content is exactly version `2` and version `1` launch/completion payloads are rejected. Reflection tests still confirm the approved `14`-field launch and `19`-field completion DTO sets; neither DTO file changed, completion privacy validation is empty, snapshots deep-copy competency/selection arrays, and serialized completion contains response IDs but no response sentence text. The harness fixture now advertises version `2`; the legacy controller remains available with only valid test fixtures advanced to the supported version.
- 2026-07-15 guided-builder Task 3 boundary review: Unity generated four unique `.meta` GUIDs for the three production files and controller test. `git diff --check` is clean; Unity-generated `ProjectSettings` normalization was removed; no Task 4 presenter, UI, scene, package, content, or transport-field work entered the diff.
- 2026-07-15 guided-builder Task 3 review-fix RED: after adding only the allocation and synchronous-listener regression tests, the canonical `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tools\Run-UnityTests.ps1 -Platform EditMode` run reported `359/362` passed. The sole three expected failures proved `Tick(1.25)` replaced the observable snapshot, Build feedback listeners observed stale `BuildFeedback`, and follow-up feedback listeners observed stale `FollowUpFeedback`; XML had zero skips/inconclusive tests and the complete `650`-line log had zero compile/exception markers.
- 2026-07-15 guided-builder Task 3 review-fix GREEN: the minimum fix removed `RefreshSnapshot()` from duration-only `Tick`, captured feedback event data before clearing it, then transitioned, rebuilt the snapshot, and only then published. The canonical wrapper passed `362/362`; XML was `Passed` with zero failures, skips, or inconclusive tests and duration `2.2782468 s`, while the complete `643`-line log had zero compile/exception markers and one successful completion marker. Tick now preserves snapshot identity while the eventual payload retains `1.25` seconds; synchronous Build/Results listeners see the advanced snapshot, recursive `Continue()` safely returns `false`, and ResultsReady publishes once. Node files were untouched, so Node suites were not rerun.
- 2026-07-15 guided-builder Task 4 RED: the direct canonical wrapper was blocked before Unity by local PowerShell execution policy, and the first bypassed sandbox attempt exited `198`; neither was accepted as test evidence. The approved escalated `powershell.exe -NoProfile -ExecutionPolicy Bypass -File .\tools\Run-UnityTests.ps1 -Platform PlayMode` run produced no XML and reached the intended compiler boundary: the complete log reported only `CS0246` for the absent rail, board, card-list, feedback, and responsive-layout types and ended with `Scripts have compiler errors.`
- 2026-07-15 guided-builder Task 4 intermediate PlayMode: the first compiled implementation reported `34/49` passed with zero skips/inconclusive tests and zero compiler/exception markers. One Task 4 failure showed the initial card was implicitly keyboard-focused rather than remaining Normal; the other `14` failures were the exact Task 3 v2 compatibility falloutâ€”valid legacy PlayMode scenarios/launches plus the authored Smart School Garden content remained v1 while `LmsPayloadValidator` correctly accepted only v2. The minimum repair removed the unrequested auto-focus side effect and advanced only those valid legacy fixtures/content plus the matching EditMode assertion to v2; LMS DTO shape, validator semantics, fields, and later-task behavior were unchanged.
- 2026-07-15 guided-builder Task 4 GREEN: final canonical PlayMode passed `49/49`, including `10/10` hand-built `GuidedPitchViewTests`; XML reported `Passed`, zero failures/skips/inconclusive tests, and duration `2.2008349 s`. The complete `950`-line log contained zero `error CS\d+`, `Compilation failed`, or `Unhandled Exception` markers and one `Test run completed` marker. The relevant full EditMode regression passed `362/362`; XML reported `Passed`, zero failures/skips/inconclusive tests, and duration `2.5230052 s`, while its complete `617`-line log had zero configured failure markers and one completion marker.
- 2026-07-15 guided-builder Task 4 view evidence: rail and board render the fixed Problem/Evidence/Solution/Value pools with identical `!/#F28C6F`, `?/#67B7D1`, `>/#7BC47F`, and `*/#E5B95C` mappings. Three prebuilt sentence cards cover Normal, Hover, Selected, Disabled, and KeyboardFocus states; stable IDs publish once then lock until Render, explicit vertical navigation stops at both ends, and hidden cards clear labels, listeners, interactability, and navigation targets. Feedback renders exactly the three constructive labelled rows with no numeric rubric.
- 2026-07-15 guided-builder Task 4 layout/accessibility evidence: `1280x720` applies one-row board/cards wide rules; `720x960` applies a two-by-two board, one-column stacked cards, and enabled scroll. Same-mode reapplication returns false, pool identities remain stable, the production source scan found zero `new GameObject`/`Instantiate` calls, and Unity generated `8/8` unique required file meta GUIDs. Card targets are `96px` (minimum `64px`); computed contrast is `15.14:1` dark text/card cream, `17.09:1` light text/deep navy, and `12.54:1` gold focus/deep navy. Exact `#F4EAD5` remains selectable-card backing only.
- 2026-07-15 guided-builder Task 4 boundary: no scene, package, LMS DTO/validator, transport field, guided presenter, or Task 5+ production behavior changed. Unity-generated `ProjectSettings.asset` and `SceneTemplateSettings.json` normalization was removed after each final run; `git diff --check` is clean.
- 2026-07-15 guided-builder Task 4 review-fix RED: after adding only the three view-contract tests, canonical PlayMode reported `50/52` passed with the two expected failures—`Cards_ConfigureAssignsBackgroundAsButtonTargetGraphic` (`Expected: same as <Inactive Card (UnityEngine.UI.Image)>`, `But was: null`) and `ResponsiveLayout_RecomputesCompactCellSizesWhenViewportShrinksInSameMode` (board cell `324` versus the required `<=264` after `720x960 -> 600x900`). The board seam test passed on arrival: the authored v2 content plus English catalog through a composite response ID -> option `TextKey` -> catalog localizer rendered the exact authored sentences, never raw IDs. XML had zero skips/inconclusive tests; the complete 906-line log had zero compile/exception markers.
- 2026-07-15 guided-builder Task 4 review-fix GREEN: the minimum fixes documented `PitchBoardView.Render`'s response-ID localize contract, recomputed width-dependent compact/wide cell sizes whenever the viewport changes while `Apply` still returns true only on a wide/compact mode change and identical inputs remain no-ops, and assigned plus validated `Button.targetGraphic = background` in `SentenceCardView.Configure`. Canonical PlayMode passed `52/52`; XML reported `Passed`, zero failures/skips/inconclusive tests, and duration `2.7479287 s`, while the complete 901-line log had zero compile/exception markers and one completion marker. Canonical EditMode passed `362/362` with duration `3.7724056 s` and a 597-line log containing zero markers.
- 2026-07-15 guided-builder Task 4 review-fix boundary: only the three guided view sources and the PlayMode view test changed; pool identities and Transform counts remained stable across same-mode reapplication. `git diff --check` is clean and Unity-generated `ProjectSettings.asset`/`SceneTemplateSettings.json` normalization was removed.
- 2026-07-17 guided-builder Task 5 RED: the new guided flow, keyboard, scroll, and bootstrap recovery PlayMode tests were written against the not-yet-existing production seams and reached the intended missing-production compiler boundary for the absent guided presenter, screen router, mode-selection, learn, and safe-fallback recovery types before any XML was produced.
- 2026-07-17 guided-builder Task 5 intermediate PlayMode: the first compiled implementation reported `60/61` passed with zero skips/inconclusive tests and zero compile/exception markers. The sole failure, `ConstrainedLayout_KeepsTheFocusedCardVisibleInsideTheGuidedScrollRect` ("Focusing the first card must scroll back up. Expected: True But was: False"), was diagnosed as a frame-timing race: the rig's `160`px viewport over `288`px content with `96`px cards whose default `100x100` Label/Focus children overhang each card rect by `2`px means the child-inclusive keep-visible targets (`content.y ~ 130` for the last card, `~ -2` for the first) sit ~`2`px outside the ScrollRect's valid elastic range `[0, 128]`; the default-Elastic `ScrollRect.LateUpdate` springs out-of-range content back by a frame-delta-dependent SmoothDamp step, and the undefined LateUpdate order between `GuidedPitchPresenter` and `ScrollRect` decides whether the one-frame-later `0.5`px assert samples the presenter's exact write. An unchanged-code canonical rerun passing `61/61` confirmed the nondeterminism.
- 2026-07-18 guided-builder Task 5 fix: `[DefaultExecutionOrder(100)]` on `GuidedPitchPresenter` guarantees its LateUpdate runs after `ScrollRect`'s (default order `0`) so the ensure-visible adjustment is the frame's final content position, and `StopMovement()` zeroes residual elastic/inertia velocity before each adjustment. No test was weakened; the temporary diagnostic string was trimmed back to its plain message.
- 2026-07-18 guided-builder Task 5 GREEN: canonical PlayMode passed `61/61`; XML reported `Passed` with zero failures, skips, or inconclusive tests and duration `1.0312721 s`, while the complete `786`-line log had zero `error CS\d+|Compilation failed|Unhandled Exception` markers and one `Test run completed` marker. Canonical EditMode passed `364/364` (the prior `362` plus the two new guided audio-cue tests); XML reported `Passed` with zero failures, skips, or inconclusive tests and duration `1.7937331 s`, while the complete `616`-line log had zero failure markers and one completion marker.
- 2026-07-18 guided-builder Task 5 route checkpoints: the Primary end-to-end PlayMode flow asserts all nine visible checkpoints - briefing copy naming Judge Aya, the Smart School Garden, individual practice for the team pitch, and no timer; exactly two equally styled cream Primary/Secondary cards; the mode-specific part framings; Learn's incomplete-pitch example with four persistent rail parts, a single Continue, and no Timer/Confidence views; Build's per-part question with exactly three localized cards filling the matching board slot; the worked/missing/improve feedback rows; Improve labeling the Developing part as an opportunity to strengthen while revision preserves the initial response ID; Present rendering the four sentences in order; and the follow-up cost question whose Results stay hidden until the coaching is reviewed. Secondary mode reuses the same mechanics, rail labels, and card-cream theme.
- 2026-07-18 guided-builder Task 5 keyboard/focus: each phase focuses its default selectable (Start, briefing Continue, first mode card, Continue, first sentence card, first strengthen button); explicit arrow navigation stops at both list ends; Enter submits the focused card; focus is restored to Continue after feedback, to the first card for the next Build round, and to Present after the revised card list hides; `MoveFocus` cycles Tab forward and Shift+Tab backward with wrapping at both ends; and the constrained `320x160` ScrollRect keeps the focused card fully visible when focus moves to the last card and back to the first.
- 2026-07-18 guided-builder Task 5 audio mapping: `HandleGuidedSessionEvent` maps ResponseSelected to `ResponseSelected` plus `JudgeReaction`, FeedbackReady to `FeedbackOpen`, ResultsReady to `ResultsReveal`, and SubmissionSucceeded/SubmissionFailed to `CompletionSuccess`/`CompletionFailure`; null events play nothing, and both the EditMode mapping tests and the PlayMode flow assert the untimed guided path never plays `TimerWarning`.
- 2026-07-18 guided-builder Task 5 recovery/privacy boundary: the recomposed guided `Bootstrapper` loads localization first, then version-checked guided content, and blocks the attempt on the SafeFallback screen for missing/malformed/invalid-route content, localization mismatch or parse failure, and rejected launches. Failure logs are asserted to equal only the stable diagnostic codes `guided_content_invalid`, `guided_localization_invalid`, `guided_launch_invalid`, or `guided_scene_contract_invalid` - never JSON braces, learner IDs, launch references, or content text - and `SafeFallbackPresenter` falls back to the exact English recovery sentence whenever the localized key is unavailable.
- 2026-07-18 guided-builder Task 5 mode-selection boundary: the learner mode is chosen only by an in-game card click; a reflection assertion proves `LmsLaunchConfig` exposes no category or learner-mode field, so no LMS category inference exists. Title/Briefing presenters were generalized to callback seams shared by the legacy `GameScreenRouter` and the new `GuidedPitchScreenRouter`, whose contract validation requires distinct panels, correctly parented presenters and default selectables.
- 2026-07-18 guided-builder Task 5 localization: `13` keys were added per catalog - `guided.board.add.problem/evidence/solution/value`, `guided.mode.primary/secondary.title/parts`, `guided.briefing.judge/practice/untimed`, `guided.improve.instruction`, and `guided.recovery.message` - bringing English and pending-review Malay to `313` keys each with zero key-parity differences.
- 2026-07-18 guided-builder Task 5 boundary: Unity generated `6/6` unique new file-meta GUIDs; the generated `Game` scene intentionally still hosts the legacy `GameScreenRouter` until Task 7 regenerates the owned scenes, so the guided `Bootstrapper` keeps the retained `scenarioJson` serialized field and would report `guided_scene_contract_invalid` against today's scene. Unity-generated `ProjectSettings.asset` normalization was reverted, untracked `SceneTemplateSettings.json` churn was removed, and `git diff --check` is clean.
- 2026-07-18 guided-builder Task 5 review-fix RED: the behavioral Settings-overlay test raised canonical PlayMode to `62` and reported `61/62` with the sole expected failure `Settings_StaysOpenWhenAnAsyncRefreshChangesPhase_ThenClosesOntoTheNewPanel` ("An asynchronous session refresh must not close the Settings overlay. Expected: True But was: False") in a `779`-line log with zero compile/exception markers, proving `Refresh()` unconditionally slammed the overlay shut on any async controller event. Adding the router-Tab and preloaded-composition seam tests then reached the intended compiler boundary: no XML, only `CS1061` for the absent `GuidedPitchScreenRouter.MoveFocus` and `Bootstrapper.PresentLoadedGuidedSession`, and one `Scripts have compiler errors.` marker.
- 2026-07-18 guided-builder Task 5 review-fix GREEN: three minimal changes - (1) Tab/Shift+Tab polling moved from the sometimes-inactive presenter (whose Update never ran during ModeSelection because the router deactivates the guided panel) to the always-active router, which delegates through a public `MoveFocus` seam to the presenter; the regression test asserts the presenter's panel is inactive while router Tab cycles and wraps the two mode cards both directions. (2) `Bootstrapper` gained `PresentLoadedGuidedSession(router, content, catalog, bridge)` so the WebGL launch-poll branch loads content exactly once and can never double-log `guided_content_invalid`; the shared overload is covered by a rejected-launch test asserting the captured errors equal exactly `["guided_launch_invalid"]`, every blocked-attempt assertion now requires exactly one logged code, and the WebGL-only branch itself remains compile-stripped in editor tests (inspection-verified, exercised by the Task 8 browser smoke). (3) `Refresh()` while the Settings overlay is open now retargets `panelBeforeSettings` to the new phase panel and returns instead of showing it; an explicit `settingsOpen` flag (set by Open/CloseSettings, cleared by `ShowSafeFallback`) tracks the overlay because rig/scene panels may start active before the first `Show`, which an intermediate `59/64` run exposed when the guard naively read `settingsPanel.activeSelf`. Final canonical PlayMode passed `64/64` (XML `Passed`, zero failures/skips/inconclusive, duration `1.1495299 s`; `838`-line log, zero failure markers, one completion marker) and canonical EditMode passed `364/364` (XML `Passed`, zero failures/skips/inconclusive, duration `1.6007249 s`; `617`-line log, zero failure markers, one completion marker). Unity-generated `ProjectSettings.asset` normalization was reverted, `SceneTemplateSettings.json` churn removed, and `git diff --check` is clean.
- 2026-07-18 guided-builder Task 6 RED (EditMode): adding only the six new expected `guided.results.*` values to `LocalizationTests` reported `363/364` with the sole intended failure `GuidedCatalog_ContainsExactLearningLabelsAndTransferPrompt` (`Expected: "Your final pitch" But was: "[[missing:guided.results.final_pitch]]"`), zero skips/inconclusive tests, and a `649`-line log with zero compile/exception markers.
- 2026-07-18 guided-builder Task 6 RED (PlayMode): the new `GuidedPitchResultsPlayModeTests` plus the `GuidedRigFactory` results-screen extension reached the intended missing-production compiler boundary: no XML was produced, and the complete `700`-line log's only errors are `6` x `CS0246` naming exactly the absent `GuidedPitchResultsPresenter` and `PitchResultPartView` types, with one `Scripts have compiler errors.` marker.
- 2026-07-18 guided-builder Task 6 GREEN: canonical EditMode passed `364/364` (XML `Passed`, zero failures/skips/inconclusive, duration `1.5382206 s`; `672`-line log, zero failure markers, one completion marker) and canonical PlayMode passed `67/67` - the prior `64` plus the three new guided Results tests (XML `Passed`, zero failures/skips/inconclusive, duration `1.2451404 s`; `800`-line log, zero failure markers, one completion marker).
- 2026-07-18 guided-builder Task 6 status/readiness examples: the revised Primary path renders the four cards in `Problem/Evidence/Solution/Value` order with icon/colour/label identity from `PitchPartVisuals` and statuses `Clear/Clear/Developing/Needs Practice` with `Pitch Readiness: 80%`; the all-clear second attempt shows `Pitch Readiness: 100%`; and the all-needs-practice path (`Pitch Readiness: 40%`) still keeps Submit interactable, proving completion is available for every complete, reviewed pitch regardless of readiness.
- 2026-07-18 guided-builder Task 6 improvement wording: `You strengthened 1 part of this pitch.` renders only from the domain `ImprovedPartCount`; per-card notes show `Strengthened after feedback` only when the revised mastery outranks the initial one, `Revised after feedback` for revision without growth, no note on unrevised cards, and the recognition line is hidden entirely when nothing was strengthened.
- 2026-07-18 guided-builder Task 6 forbidden-label scan: every `Text` under the Results panel is asserted to contain none of `Score`, `Confidence`, `Final confidence`, `Seedling`, `Sprouting`, `Growing`, or `Thriving`; the final pitch joins the localized CURRENT response sentences (response ID -> option TextKey -> catalog) with paragraph breaks in framework order, contains the revised Clear problem sentence but never the replaced Developing one, and shows no `[[missing:` token or `FinalConfidence` value.
- 2026-07-18 guided-builder Task 6 submission preservation: with a deferred bridge, Submitting shows `lms.submission.submitting` and disables Submit and Retry; generic failure, session expiry, and missing configuration each render their sanitized localized status keys (`lms.submission.failed`, `lms.session.expired`, `lms.configuration.missing`), keep all four populated cards and the byte-identical final pitch, relabel the action `Resubmit results`, and allow resubmission; success renders `lms.submission.success`, disables Submit, keeps Retry usable, and moves focus onto Retry.
- 2026-07-18 guided-builder Task 6 Retry proof: Retry from a failed-submission Results returns to Briefing with attempt `2`, a null learner mode, an empty draft, a cleared submission error, and focus on the briefing Continue; the second attempt must re-choose a mode at ModeSelection, and its Secondary all-clear Results shows only attempt-2 sentences, `Ready to submit.` status, the plain `Submit results` label, no revision notes, and the scroll reset to the top - while submission refreshes within one attempt preserve the scroll position.
- 2026-07-18 guided-builder Task 6 router boundary: the Results presenter owns its default selectable behind the router's focus restoration (`Submit` on Results, `Retry` on Complete, none while Submitting) through a `GetDefaultSelectable` seam replacing the `resultsDefault` placeholder; `GuidedPitchPresenter.GetDefaultSelectable` was not extended, the Settings-overlay `settingsOpen` retarget behavior is untouched, and the router now validates, initializes, and refreshes the results presenter alongside the guided presenter.
- 2026-07-18 guided-builder Task 6 localization/boundary: `6` new keys per catalog (`guided.results.final_pitch`, `guided.results.strengthened.one/many`, `guided.results.part.strengthened/revised`, `guided.results.resubmit`) bring English and pending-review Malay to `319` keys each with exact key and guided-value parity; Unity generated `3/3` unique new file-meta GUIDs, no scene or package changed, Unity-generated `ProjectSettings.asset` normalization was reverted, untracked `SceneTemplateSettings.json` churn was removed, and `git diff --check` is clean.
- 2026-07-18 guided-builder Task 6 review-fix RED: the new `Keyboard_RouterTab_CyclesSubmitAndRetryOnResultsAndIsSafeWhileSubmitting` PlayMode test raised the suite to `68` and reported `67/68` with the sole intended failure ("Tab must cycle the Results screen's own selectables. Expected: True But was: False") in an `831`-line log with zero compile/exception markers, proving router `MoveFocus` delegated only to the guided presenter whose cycle root is inactive on Results, so Tab silently no-opped on a screen with two real actions.
- 2026-07-18 guided-builder Task 6 review-fix GREEN: the router now routes Tab to the results presenter whenever the Results panel is shown, mirroring the `GetDefaultSelectable` seam; both presenters share one extracted `GuidedFocusCycle` helper, so Submit <-> Tab-cycles <-> Retry with wrap in both directions while the guided panel is inactive, and Submitting (both actions disabled) is a safe focus-preserving no-op. The two review minors were folded in: the duplicated mastery-key mapping now lives in a single throwing `PitchPartVisuals.MasteryLabelKey`, the Present/Results paragraph join shares `PitchPartVisuals.ComposeCurrentSentences`, and `GuidedPitchResultsPresenter.Initialize` resets `renderedAttempt`/`hasRenderedResult` so re-initialization against a fresh controller with an equal attempt number still resets the scroll. Canonical PlayMode passed `68/68` (XML `Passed`, zero failures/skips/inconclusive, duration `1.3345954 s`; `838`-line log, zero failure markers, one completion marker) and canonical EditMode passed `364/364` (XML `Passed`, zero failures/skips/inconclusive, duration `1.7256365 s`; `616`-line log, zero failure markers, one completion marker). `git diff --check` is clean and Unity-generated `ProjectSettings.asset` normalization was reverted.
- 2026-07-19 guided-builder Task 7 RED: the rewritten generated-scene EditMode suite reported `360/365` with exactly the five intended failures against the legacy scenes - the singular `GuidedPitchScreenRouter` count was `0`, three fixtures stopped at the missing `Guided Pitch` canvas child, and the legacy `Continue Button` still used `Automatic` navigation; the complete `552`-line log had zero `error CS\d+|Compilation failed|Unhandled Exception` markers. The restored end-to-end boot PlayMode test reported `43/44` with the sole intended failure `Unhandled log message: '[Error] guided_scene_contract_invalid'`, proving `Bootstrapper.Start()` still rejected the legacy `Game` scene; the three superseded legacy files (`TutorialPlayModeTests`, `PitchInteractionPlayModeTests`, `ResultsPlayModeTests`, `24` tests) were deleted with their metas in the same RED cycle because Tasks 4-6 guided suites replaced their coverage.
- 2026-07-19 guided-builder Task 7 builder: `GuidedPitchSceneBuilder.Create(Transform, Sprite, Sprite[])` now constructs all six screens plus the router; `PitchSimulatorProjectBuilder` retains only bootstrap/web-test composition and the Game entry point, wires `guidedPitchContentJson` to `Assets/Content/Scenarios/guided-pitch-builder.en.json`, and the legacy `scenarioJson` field was removed from `Bootstrapper`. The waited batch builder exited `0` with one `built successfully` marker and zero `error CS`/compilation/exception markers in every complete log. Two consecutive waited runs produced structurally identical scenes: identical line counts (`422`/`20298`/`611`) and `0` differing lines after masking Unity's session-random local file IDs, which are the only byte-level variance (the known file-ID normalization). Unrelated `ProjectSettings.asset`, untracked `SceneTemplateSettings.json`, and side-effect `WebIntegrationTest.unity` file-ID churn were reverted after verification; the Bootstrap/Game guided changes were kept.
- 2026-07-19 guided-builder Task 7 hierarchy: the generated Canvas keeps `ScaleWithScreenSize` at `1280x720` and owns exactly `Title, Briefing, Guided Pitch, Results, Settings, Safe Fallback` in order. `Guided Pitch` owns `Environment Frame` (full-screen point-filtered `pitch-room`) and `Content Frame` with `Progress Rail`, `Aya Row` (native `128x160` point-filtered Judge Aya beside the opaque-navy gold-accented Dialogue Card with light text), `Pitch Board`, `Phase Scroll/Viewport/Content` hosting `Mode Selection, Learn, Sentence Cards, Feedback, Improve Actions, Present Pitch, Follow Up`, and `Primary Action`. The serialized `modeSelectionPanel` is the nested Phase Scroll section, so `GuidedPitchScreenRouter.Show` gained a minimal ancestor-activation rule (a panel stays active when the selected panel is its child); the rig-based suites are unaffected because their panels are siblings, and the scene end-to-end test covers the nested route.
- 2026-07-19 guided-builder Task 7 wide/compact measurements: at `1280x720` the frame renders `980x672` (bounds `960-1000` / `<=680`), centered, opaque `#0E171F`, with `300`px of environment visible beside it; the board is one row of four equal `224x140` slots and the three cream (`#F4EAD5`) sentence cards render as contained `288x96` cards. At `960x720`/`720x960` (with `GuidedPitchResponsiveLayout.Apply`, whose compact threshold now includes exactly `960` wide) the frame renders `912x672`/`672x672` inside the 24px safe padding, the board reflows to two-by-two, cards stack one per column, the Phase Scroll enables with every active descendant inside viewport/content bounds, and every content selectable fits the viewport height so focused targets can scroll fully visible; Results reuses the four `PitchResultPartView` visuals stacked vertically.
- 2026-07-19 guided-builder Task 7 target/contrast/truncation: every generated selectable uses explicit navigation and all active targets across Title, Briefing, Guided Pitch, Results, and Settings measure at least `64x64`px. Verified pairs include light `#FFF8E8` on navy `#0E171F`/`#16242F` (about `17:1`/`15:1`), ink on cream (`15.1:1`), labels on the darkened action teal (about `6:1` >= `4.5:1`), and gold `#FFD166` focus/revision outlines against navy (about `12:1` >= `3:1`). Representative longest authored copy - the `159`-character sentence in cards/board/results, longest question/hint, longest worked/missing/improve feedback values, the composed four-sentence final pitch, mode cards, learn, briefing, readiness/improvement/transfer, submission statuses, and the recovery sentence - renders with zero truncated characters via TextGenerator at all three fixtures.
- 2026-07-19 guided-builder Task 7 GREEN: against the exact committed scene bytes, canonical EditMode passed `365/365` (XML `Passed`, zero failures/skips/inconclusive; complete `641`-line log with zero failure markers) and canonical PlayMode passed `44/44` (XML `Passed`, zero failures/skips/inconclusive; complete `849`-line log with zero failure markers), including the restored `BootstrapScene_BootsTheGuidedComposition_ThroughTitleIntoModeSelection` end-to-end test: additive Bootstrap load, `Bootstrapper.IsInitialized`, one Canvas/EventSystem/router in `Game`, Title with focused Start, localized briefing naming Judge Aya and `no timer`, and the nested Mode Selection visible with its first card focused and zero logged errors. Scene SHA-256 values were byte-identical before and after both suites (Bootstrap `825CF369...`, Game `5C00AD60...`).
- 2026-07-19 guided-builder Task 7 disclosures: `Agrovator.PitchSimulator.Editor.asmdef` gained the `GuidedPitch` reference the scene builder needs; `GuidedPitchResponsiveLayout`'s compact width comparison became inclusive (`<= 960`) so the plan's `960x720` fixture stacks, with no existing expectation touched; the `Follow Up` phase section is an empty reserved container because the presenter contract carries no follow-up-only view. Legacy dialogue presenters/router source files remain compiled but hold no reference in any owned scene.
- 2026-07-19 guided-builder Task 7 independent-review root causes/RED: (1) `GuidedPitchResponsiveLayout` exposed only manually-invoked board/card rules, held no live viewport or fixed control-group references, and had no dimension-change lifecycle; canonical PlayMode reported `44/45`, with the sole new live generated-Canvas resize test failing because compact mode remained false (`930`-line log, zero compiler/exception markers). (2) every builder call unconditionally destroyed each owned root; after the raw unmasked byte assertion was added, the decisive root-identity assertion reported canonical EditMode `364/365` because the unchanged `Generated UI` object had been destroyed (`680`-line log, zero failure markers). (3) ordinary `ColorTint` was the only generated focus state outside sentence/revision visuals; canonical EditMode reported `364/365`, solely because Start had no reusable indicator (`761`-line log, zero failure markers). (4) the garden `Image` filled an arbitrary rect with neither aspect preservation nor crop behavior; canonical EditMode reported `365/366`, solely because the environment had no crop-to-fill fitter (`760`-line log, zero failure markers).
- 2026-07-19 guided-builder Task 7 review-fix layout/focus/aspect GREEN: the generated guided panel now serializes its actual Canvas viewport plus the fixed mode, improve, and primary-action layout groups. `OnEnable`/`OnRectTransformDimensionsChange` reapply board/card rules, enable compact scrolling, and toggle the same reusable `GuidedPitchFlowLayout` instances between row and stack without creating hierarchy objects; the live `720x960` PlayMode fixture proves all five families reflow. Every generated `Selectable` now owns one gold `Outline`-backed `SelectableFocusIndicator`; all remain explicit-navigation targets of at least `64px`, gold/navy adjacent contrast remains >=`3:1`, and real EventSystem movement transfers the indicator between mode cards. `Environment Frame` now uses `AspectRatioFitter.EnvelopeParent` at the original sprite ratio; `1280x720` and `720x960` assertions prove crop-to-fill coverage and equal X/Y source scale.
- 2026-07-19 guided-builder Task 7 review-fix determinism GREEN: `PitchSimulatorProjectBuilder` now validates each owned Bootstrap/Game/Web contract and reuses the single current root without saving; missing, duplicate, or stale contracts still take the existing full migration/rebuild path. The strengthened build-twice test compares raw scene bytes with no ID masking, requires the same owned Game root/serialized IDs on the unchanged second run, then deletes its Canvas and proves a third run rebuilds a valid contract while preserving the unowned sentinel and exact enabled Bootstrap/Game build settings. Two separate final batch Unity sessions logged one success/quit marker apiece and zero configured failure markers in complete `354`/`324`-line logs; both produced byte-identical SHA-256 values: Bootstrap `825CF36910F32B6C8128404239D0FD1A18279DBBDDAB9F6FE569493D77A747B7`, Game `38EAB485A257E061729C0B01BDA1D8BD910913A8F68F6C6E1490E200DB36C4AD`, WebIntegrationTest `601066A3572FD4AACB252D547AFEB1D1B3D50876BB5ABDBFF770C2B9E7DAC0DE`.
- 2026-07-19 guided-builder Task 7 independent-review final GREEN: canonical EditMode passed `366/366` (XML `Passed`, zero failures/skips/inconclusive, duration `4.75679 s`; complete `748`-line log, zero configured failure markers, one completion marker) and canonical PlayMode passed `45/45` (XML `Passed`, zero failures/skips/inconclusive, duration `2.4543793 s`; complete `892`-line log, zero configured failure markers, one completion marker). Unity generated both new script metas with distinct GUIDs; `ProjectSettings.asset`, `SceneTemplateSettings.json`, and Bootstrap file-ID-only normalization were removed after proof, final scene hashes remained unchanged through both suites, and `git diff --check` is clean.
- 2026-07-19 guided-builder Task 7 re-review RED/GREEN: the compact-size test activates mode/cards/improve plus both primary actions at `960x720` and `720x960`, asserts every control stays inside its flow root, and asserts every active phase root stays inside scroll content. The authoritative-size RED reported EditMode `365/366` solely because Mode Selection still carried a competing `LayoutElement` (`762`-line log, zero configured failure markers); removing the companion size sources from mode, improve, and primary and regenerating through a successful `358`-line builder run restored `366/366`. The physical-breakpoint RED reported PlayMode `44/45` solely because the generated layout had no serialized physical Canvas source (`925`-line log, zero configured failure markers); generated ScreenSpace canvases now classify with `Canvas.pixelRect`, poll physical changes without direct `Apply`, and a deterministic physical-source seam proves compact mode at both `960x720` and `720x960` while leaving the Canvas in `ScreenSpaceOverlay`, restoring `45/45` after a successful `376`-line builder run.
- 2026-07-19 guided-builder Task 7 re-review partial-stale contract: nulling only serialized `modeSelectionControls` produced the intended EditMode `365/366` RED because the builder reused the same incomplete owned root (`772`-line log, zero configured failure markers). `GuidedPitchResponsiveLayout.ValidateContract` now covers Canvas/rect/core references, all mode/improve/primary groups, viewport consistency, and distinct groups; `HasCurrentGameContract` delegates to it. GREEN `366/366` proves the partial root is replaced/repaired, the repaired responsive contract validates, a subsequently deleted Canvas still triggers structural migration, unchanged second-run bytes/IDs remain stable, the unowned sentinel survives, and build settings remain exactly enabled Bootstrap then Game.
- 2026-07-19 guided-builder Task 7 re-review final determinism/GREEN: two separate final builder processes produced complete `364`/`321`-line logs with one success/quit marker each and zero configured failure markers; raw unmasked hashes matched after both runs: Bootstrap `825CF36910F32B6C8128404239D0FD1A18279DBBDDAB9F6FE569493D77A747B7`, Game `8AE7D6F1179ADC161581E5B77A143A97D2F76F371F7EA9D2E91C49D067EB6F60`, WebIntegrationTest `601066A3572FD4AACB252D547AFEB1D1B3D50876BB5ABDBFF770C2B9E7DAC0DE`. Canonical EditMode passed `366/366` (XML `Passed`, zero failures/skips/inconclusive, duration `2.7752058 s`; complete `855`-line log, zero configured failure markers, one completion marker) and PlayMode passed `45/45` (XML `Passed`, zero failures/skips/inconclusive, duration `1.5979127 s`; complete `889`-line log, zero configured failure markers, one completion marker). Scene hashes remained unchanged after both suites; Unity-only `ProjectSettings.asset`/`SceneTemplateSettings.json` noise was removed and `git diff --check` is clean.
- 2026-07-19 guided-builder Task 8 Node RED/GREEN: syntax stayed valid while the initial source-contract run passed `3/8` and failed the five expected missing variable-stage, DPR, display-metrics, and complete assigned-flow contracts. The final exact commands `node --check tools/smoke-webgl.mjs` and `node --test tools/tests/*.test.js` passed `12/12` with zero failures, skips, or todos. The template now consumes measured body/safe-area/control chrome, sizes both stage dimensions, coalesces ResizeObserver/window/visualViewport changes, caps DPR render scale at `2`, prevents desktop inline sizing from becoming mobile min-content overflow, and leaves Canvas CSS at `100% x 100%`.
- 2026-07-19 guided-builder Task 8 viewport/letterbox TDD: the first canonical PlayMode RED produced no XML at the expected absent `WebViewportMetrics` compiler boundary; after implementation, a lifecycle fixture exposed the stale serialized-viewport guard before the minimal repair. The display-only `.jslib` functions report exact Canvas CSS width/height and DPR-times-100 without LMS, learner, or content fields; native/editor fallback is `Screen.width`, `Screen.height`, DPR `1`. `GuidedPitchResponsiveLayout` observes the CSS width/height/DPR tuple and reapplies only when it changes. The letterbox EditMode RED reported `364/366`, including the intended `EnvelopeParent` versus `FitInParent` failure and the obsolete browser-aspect assertion; the generated guided screen now centers an exact 16:9 point-filtered garden inside a deep-navy backing. Two separate post-generation builder runs logged success with zero failure markers and kept raw scene hashes byte-identical: Bootstrap `825CF36910F32B6C8128404239D0FD1A18279DBBDDAB9F6FE569493D77A747B7`, Game `0F0A68B1664EA108AFF0A2C17D6370F949E88DFB912C3390FE48F72562D124B0`, WebIntegrationTest `601066A3572FD4AACB252D547AFEB1D1B3D50876BB5ABDBFF770C2B9E7DAC0DE`.
- 2026-07-19 guided-builder Task 8 screenshot-review RED/GREEN: the first rebuilt Chrome Present capture exposed retained keyboard sentence-card scroll, with the fourth pitch sentence pushed beneath the action boundary. The focused canonical PlayMode fixture reported `47/48`, solely because Improve-to-Present retained content Y=`96` instead of top Y=`0` (`925`-line log, zero compile/exception markers). The presenter now stops and resets the shared phase scroll on phase changes; final PlayMode passed `48/48` (XML `Passed`, zero failures/skips/inconclusive, duration `1.9297993 s`; `916`-line log, zero failure markers). A tall-desktop generated contract also proves the Primary presentation text remains inside the masked viewport with bottom clearance. Final EditMode passed `367/367` (XML `Passed`, zero failures/skips/inconclusive, duration `3.0207199 s`; `860`-line log, zero failure markers).
- 2026-07-19 guided-builder Task 8 build/server: the final `tools/Build-WebGL.ps1` exited `0`; BuildReport was `Succeeded`, `92,630,677` bytes and `00:00:26.7397145`, with zero warnings/errors and zero configured failure markers in the complete `649`-line log. `tools/Serve-WebGL.ps1 -SelfTest` passed on temporary loopback port `52538`, covering GET/HEAD, MIME, traversal, missing-path, and method checks.
- 2026-07-19 guided-builder Task 8 browser matrix: Chrome `150.0.7871.127` ran the complete Primary keyboard-only path from `2026-07-19T11:10:47.462Z` to `11:11:20.428Z` (`6,770 ms` load) and completed with score `100`; Edge `150.0.4078.83` ran the complete Secondary pointer-only path from `11:11:20.429Z` to `11:12:01.691Z` (`6,651 ms` load) and completed with score `95`. Both exercised Learn, all four selections/feedback rounds, one revision, Present, cost follow-up, Results, submission failure then success, Retry, and fresh Mode Selection through changed hashes stable across three samples. Each recorded zero console errors, zero page errors, one failure recovery, one successful completion, canvas focus, containment, and zero inner/outer horizontal overflow; the smoke server stopped with zero stderr. Firefox is unavailable because no executable exists at the standard Windows installation paths, so no Firefox coverage is claimed.
- 2026-07-19 guided-builder Task 8 initial sizing/image evidence (superseded by independent review): both browsers measured desktop viewport `1440x1000`, CSS/backing `1276x918`, DPR/render scale `1/1`, ratios `1.00/1.00`; mobile viewport `390x844`, CSS/backing `380x783`, DPR/render scale `1/1`, ratios `1.00/1.00`. The original visual verdict was incorrect: both Present captures hid the fourth sentence below Continue, and both compact captures exposed only the top of Secondary. Those eleven PNGs are not accepted as final evidence; the corrected review-fix evidence below supersedes them.
- 2026-07-19 guided-builder Task 8 boundary: Web viewport integration is read-only and transmits no learner/content data; smoke/result summaries contain only pseudonymous harness state and no LMS payloads. Build, server, logs, XML, JSON, and PNG evidence remain ignored. Unity-generated `ProjectSettings.asset` normalization and `SceneTemplateSettings.json` churn were removed; no AGROVATOR LMS repository path was accessed.
- 2026-07-19 guided-builder Task 8 independent-review visual RED/GREEN: actual mask/action/card tests first reported canonical EditMode `366/369`, with exactly three failures: the compact phone viewport was `104.10px` high against `150px` mode cards, while Primary/Secondary Present roots were `112/128px` against required `140/156px`; the complete `885`-line log had zero compile/exception markers. `GuidedPitchFlowLayout` now owns a serialized compact item height (`96px` for mode cards), Present reserves explicit vertical padding, and the stale-scene contract includes both values. After original-detail review caught that runtime Present still used seven visual lines, canonical PlayMode reported the decisive `47/48` RED (expected four lines, actual seven); Present alone now compacts shared paragraph text to exactly four newline-separated sentences while Results retains paragraph spacing.
- 2026-07-19 guided-builder Task 8 independent-review smoke/source hardening: the reusable lexical extractor masks comments and strings, preserves executable duplicates, and structurally extracts balanced functions/calls. Contracts require the exact ordered Chrome keys, Edge coordinates, state-gate class, failure/success harness actions, eleven captures, forbidden mixed-input absence, executable observer -> `requestLayout` -> `fitCanvas` flow, DPR -> Unity config flow, and exactly three unique display-only viewport exports. Runtime gates hash localized phase-content and lower-control regions separately, require state-specific stable combinations across three samples, settle Unity composition after responsive resize, and corrected Edge's real revision centers from the non-actionable `0.62/0.70` pair to `0.70/0.60`. Final syntax plus `node --test tools/tests/*.test.js` passed `17/17` with zero failures/skips/todos.
- 2026-07-19 guided-builder Task 8 independent-review final Unity/determinism: canonical EditMode XML is `Passed`, `369/369`, zero failures/skips/inconclusive, duration `3.1738018 s`; its complete `887`-line log has zero configured failure markers and one completion marker. PlayMode XML is `Passed`, `48/48`, zero failures/skips/inconclusive, duration `1.734111 s`; its complete `917`-line log has zero configured failure markers and one completion marker. Two final builder processes exited `0`; raw scene hashes remained byte-identical before/after both: Bootstrap `825CF36910F32B6C8128404239D0FD1A18279DBBDDAB9F6FE569493D77A747B7`, Game `52CA1EA6BB54A6B345D0790093F8869FB0AC94F013E236E88FF4A834A97CE40C`, WebIntegrationTest `601066A3572FD4AACB252D547AFEB1D1B3D50876BB5ABDBFF770C2B9E7DAC0DE`.
- 2026-07-19 guided-builder Task 8 independent-review final build/browser: the accepted same-input WebGL build reported `Succeeded`, `92,631,285` bytes, `00:00:22.4415828`, zero warnings/errors, and zero configured failure markers in its complete `638`-line log; a prior attempt hit a transient IL2CPP metadata-sort `AccessViolationException`, which did not recur. Server self-test passed on temporary port `54460`. The final matrix passed Chrome `150.0.7871.127` Primary keyboard (`6,708ms` load, score `100`) and Edge `150.0.4078.83` Secondary pointer (`6,574ms`, score `100`) with zero console/page errors, failure -> success submission, Retry, fresh Mode Selection, settled desktop/mobile composition, containment/focus, and zero overflow; server shutdown had zero stderr. Firefox remained unavailable and is not claimed.
- 2026-07-19 guided-builder Task 8 independent-review initial final images (superseded by re-review): all eleven regenerated PNGs were opened at original detail, and a metadata-only rerun produced byte-identical hashes. The Present and compact fixes were valid, but the conclusion that Results copy was complete was too broad: `edge-secondary-results.png` clipped the lower portion of the wrapped final word `beds.` at the Results boundary. The re-review evidence below supersedes the Results verdict while retaining the verified Present/mobile findings.
- 2026-07-19 guided-builder Task 8 independent-review hygiene: Unity-only `ProjectSettings.asset` normalization and untracked `SceneTemplateSettings.json` were removed, `git diff --check` is clean, build/test/browser evidence remains ignored, and no AGROVATOR LMS repository path was accessed.
- 2026-07-19 guided-builder Task 8 Results re-review RED/root cause: the exact `1276x918` Secondary capture state uses all four long current sentences, one strengthened Problem note, double-newline final-pitch paragraphs, the generated Results `RectMask2D`, and the fixed status/footer. After an ancillary unsupported `Assert.Multiple` compile attempt was corrected in test code, canonical EditMode reported the decisive `369/370` RED (duration `4.4584785 s`; complete `902`-line log, zero compile/exception markers): the final-pitch rectangle/glyph bottoms were `-242/-243` against fixed status top `-226`, so TextGenerator reported every character while the last `beds.` glyphs occupied the clipped boundary. The default Results scroll remained correctly reset to top; the root cause was the seven pre-pitch `8px` content gaps consuming the last `16/17px` of reserved clearance.
- 2026-07-19 guided-builder Task 8 Results re-review GREEN/determinism: generated Results content alone now uses `5px` vertical gaps, recovering `21px` before the final pitch and leaving paragraph spacing, scroll/reset behavior, `64px` actions, submission, and Retry untouched; the stale-scene contract validates the spacing. A first implementation run stayed red when an identical guided-content call site was changed instead, proving both clipping and root-identity contracts detected the mistake before it was corrected. Final canonical EditMode passed `370/370` (duration `3.379202 s`; `871`-line log, zero failure markers) and PlayMode passed `48/48` (duration `1.7141382 s`; `889`-line log, zero failure markers). Two unchanged final builder processes exited `0` with zero configured failure markers and preserved raw hashes before/after both: Bootstrap `825CF36910F32B6C8128404239D0FD1A18279DBBDDAB9F6FE569493D77A747B7`, Game `D41C1538493E4D73628D4A7E58B5087D38DF3890258509E7E2D1AE8C64A9A8EB`, WebIntegrationTest `601066A3572FD4AACB252D547AFEB1D1B3D50876BB5ABDBFF770C2B9E7DAC0DE`; full Node syntax/contracts remained `17/17`.
- 2026-07-19 guided-builder Task 8 Results re-review build/browser: the fresh warning-free WebGL BuildReport was `Succeeded`, `92,631,312` bytes, `00:00:06.6876192`, zero warnings/errors, with zero configured failure markers in the complete `1,852`-line log; server self-test passed on temporary port `57023`. The final matrix passed Chrome `150.0.7871.127` Primary keyboard from `13:13:33.998Z` to `13:14:07.753Z` (`6,885ms` load, score `100`) and Edge `150.0.4078.83` Secondary pointer from `13:14:07.754Z` to `13:14:50.779Z` (`7,525ms`, score `100`), both with zero console/page errors, failure -> success, Retry/fresh mode, contained desktop/mobile composition, and zero overflow; server shutdown was clean. Firefox remained unavailable and is not claimed.
- 2026-07-19 guided-builder Task 8 Results re-review final images/hygiene: all eleven required PNGs were reopened at original detail. Edge Results now renders the complete Secondary final word `beds.` and period above the fixed status/footer; Chrome Results remains readable, both Present captures retain four complete sentences, and both compact captures retain complete Primary/Secondary cards. The two updated Results hashes are Chrome `B15B2D005F4B3206CB1EAF6025813407DCA11C0063B4A21640DF6C5A2134BFAA` and Edge `B19F8D7408BAF6325B7958C42D692798E7018AB72BEA62234599778DD60970A8`; the other nine required captures retained their prior hashes. Each browser recorded only the nine disclosed non-error root-only/audio-placeholder warnings. Proven Unity-only settings/template noise was removed, evidence remains ignored, and no AGROVATOR LMS repository path was accessed.
- 2026-07-19 guided-builder Task 9 canonical suites: fresh EditMode XML was `Passed`, `370/370`, zero failures/skips/inconclusive, duration `2.6572753 s`; its complete `897`-line log had zero configured compile/exception/failure markers. Fresh PlayMode XML was `Passed`, `48/48`, zero failures/skips/inconclusive, duration `1.760119 s`; its complete `890`-line log had zero configured markers. `node --check .\tools\smoke-webgl.mjs` passed and the full Node suite passed `17/17` with zero failures/skips/todos.
- 2026-07-19 guided-builder Task 9 focused acceptance evidence from those fresh XML files: `GuidedPitchContentTests` passed `18/18`, `LocalizationTests` `27/27`, `LmsPayloadTests` `92/92`, `BootstrapPlayModeTests` `9/9`, and `GuidedPitchFlowPlayModeTests` `8/8`, all with zero failures/skips/inconclusive. These assert one active v2 Bootstrap reference, both terminating routes, the exact `30` stable unique option IDs, every content/localization key, reading limits, exact English/pending-Malay parity, unchanged DTO/reflection/privacy shape, both mode flows, keyboard/focus, recovery, submission preservation, and Retry reset.
- 2026-07-19 guided-builder Task 9 content/contract reconciliation: the active asset is `smart-school-garden` version `2`, authored estimate `10` minutes and checksum `guided-pitch-builder-v2-authored`; `31` JSON `Id` lines comprise the scenario ID plus `30` option IDs. English and Malay contain `319/319` entry lines, with English `reviewed`, Malay `pending_human_review`, and exact tested key/guided-fallback parity. The `14`-field `LmsLaunchConfig` and `19`-field `LmsCompletionPayload` files have zero diff from the guided baseline and retain their field names/types; six competency IDs are emitted, Time Management is omitted, `SelectedResponseIds` keeps chronological initial/revision/follow-up history, and hidden `FinalConfidence` retains legacy delta semantics.
- 2026-07-19 guided-builder Task 9 build/server: WebGL BuildReport was `Succeeded`, `92,631,312` bytes, `00:00:02.0019569`, zero warnings/errors; the complete `626`-line log had zero configured failure markers. The ignored artifact contains `7` files totalling `92,631,312` bytes: HTML `5,972`, CSS `2,730`, layout JS `1,077`, loader `58,622`, framework `712,694`, data `9,030,172`, and wasm `82,820,045`. Server self-test passed on temporary loopback port `58382` for GET/HEAD, MIME, traversal, missing-path, and method checks.
- 2026-07-19 guided-builder Task 9 browser matrix: Playwright `1.61.1` on port `58399` passed Chrome `150.0.7871.127` Primary keyboard from `13:31:34.900Z` to `13:32:08.651Z` (`6,860 ms` load, score `100`) and Edge `150.0.4078.83` Secondary pointer from `13:32:08.651Z` to `13:32:50.621Z` (`6,552 ms`, score `100`). Both had zero console/page errors, six competencies, zero timeouts, failure -> success, Retry/fresh mode, focus/containment, and zero inner/outer overflow; the server stopped with zero stderr. Desktop was `1440x1000` with CSS/backing `1276x918`; mobile was `390x844` with CSS/backing `380x783`; DPR/render scale and X/Y ratios were `1`. Firefox was unavailable and is not claimed. Correction: this run did not exercise missing-configuration recovery; the guided path never called `verifyMissingConfigRecovery` and its retained artifacts recorded `missingConfig: false`. The original recovery claim was inaccurate and is superseded by the repair evidence below.
- 2026-07-19 guided-builder Task 9 original-detail visual review: all `11` required guided PNGs were opened at original detail: five Chrome Primary wide captures, four Edge Secondary wide captures, and two compact captures. Mode/build/improve copy, persistent part visuals, cream cards, gold focus, four complete Present sentences, complete final pitches including Secondary `beds.`, fixed actions, and complete compact Primary/Secondary cards remained readable/contained. Each browser recorded only `9` disclosed non-error root-only/audio-placeholder warnings. The longer Secondary Results body remains reachable by its tested vertical scroll.
- 2026-07-19 guided-builder Task 9 asset/link/ignore reconciliation: `142` logical files plus `40` logical directories produced `182` non-meta entries and exactly `182` meta files, with zero missing/orphaned metas. All `48` tracked Markdown files and `78` relative links resolved. Exact ignore checks cover `Library`, `Temp`, `Logs`, `UserSettings`, `Build`, and `artifacts`; the seven build files plus XML/log/JSON/PNG evidence remain ignored, and Unity-only `ProjectSettings.asset`/`SceneTemplateSettings.json` noise was removed.
- 2026-07-19 guided-builder Task 9 privacy audit: the exact required `rg` matches were all reviewed and were limited to explanatory denials, one historical Unity licensing/access-token diagnostic note without a value, the validator's blocked `email` field name, and plan text. Broader scans found zero email-like values, credential-query shapes, secret-named tracked files, free-text input controls, or unexpected AWS/OpenAI/GitHub/private-key shapes. The only bearer/JWT-shaped matches are deliberate malformed rejection fixtures plus explanatory acceptance prose; logging/writing sinks contain stable startup/audio codes or sanitized smoke metrics, not full launch/completion payloads or response text.
- 2026-07-19 guided-builder Task 9 documentation/evidence boundary: README, CHANGELOG, and the 13 named operating/authoring/architecture/LMS/privacy/QA/asset/acceptance documents now describe exact current Primary/Secondary behavior, conceptual-only `Elementary -> Primary`, the four prompts, untimed threshold-free completion, diagnostic/current revision assessment, Retry/recovery, responsive/DPR behavior, and all measured evidence. They explicitly distinguish implementation behavior from learning effectiveness and leave educator/learner, Malay, Firefox/Safari, native touch, real LMS, unrestricted fullscreen, legal, classroom, and human accessibility review unclaimed. Independent Task 9 and whole-branch reviews remain post-commit and are not claimed here.
- 2026-07-19 guided-builder Task 9 branch hygiene: the known six trailing spaces in `Assets/Scripts/UI/GuidedPitch.meta` and `Assets/Tests/PlayMode/GuidedPitch.meta` were removed without GUID/content changes because the exact baseline diff check reported them. `git diff --check ce7f8ac` then exited `0`. The retained Task 8 lexical constant-unreachable-branch Minor was not changed and remains for final review triage.
- 2026-07-19 guided-builder Task 9 recovery-repair RED: the independent Task 9 review confirmed the acceptance defect — `runBrowser` never invoked `verifyMissingConfigRecovery`, so the guided Chrome/Edge path recorded `modes.missingConfig: false` while the acceptance records claimed recovery coverage. A first repair attempt failed live because Playwright `selectOption`/`click` require visible controls and the fullscreen harness presentation hides `#mode` and `#resend`. Two new Node source contracts were added RED-first: one requires exactly one reachable recorded `result.modes.missingConfig = await verifyMissingConfigRecovery(page, options)` step inside `runBrowser` (rejecting an `if (false)` wrap and an unrecorded call), and one requires recovery to drive the hidden controls via `setHarnessMode` plus `#resend` `dispatchEvent("click")` with zero `selectOption` use (rejecting a visible-`click` mutation).
- 2026-07-19 guided-builder Task 9 recovery-repair GREEN: `node --check .\tools\smoke-webgl.mjs` exited zero; the focused smoke-flow suite passed `9/9`; the full Node suite passed `19/19` with zero failures/skips/todos. `verifyMissingConfigRecovery` now sets the harness mode through `page.evaluate` (value plus bubbled `change` event; `harness.js` reads `mode.value` at send time), reloads the frame, waits for `Missing Config`, switches to Success, dispatches `#resend`, and proves the recovered Title responds — matching the harness contract without requiring hidden controls to be visible.
- 2026-07-19 guided-builder Task 9 repaired browser matrix: Playwright `1.61.1` on port `60994` passed Chrome `150.0.7871.127` Primary keyboard from `14:37:53.106Z` to `14:38:34.346Z` (`6,971 ms` load, score `100`) and Edge `150.0.4078.83` Secondary pointer from `14:38:34.346Z` to `14:39:23.694Z` (`6,590 ms`, score `100`). Both recorded `status: passed` with `modes.success`, `modes.failure`, and `modes.missingConfig` all `true`, zero console/page errors, six competencies, zero timeouts, desktop `1440x1000` CSS/backing `1276x918`, mobile `390x844` CSS/backing `380x783`, DPR/render scale and X/Y ratios `1`, containment/focus true with zero inner/outer overflow, and clean server shutdown with zero stderr. Firefox remained unavailable and is not claimed.
- 2026-07-19 guided-builder Task 9 repaired-run warnings and images: each browser now records `12` disclosed non-error warnings because missing-configuration recovery reloads the player for a second boot — root-only `DontDestroyOnLoad`, `MusicLoop`, and `ButtonPress` twice each, plus the six single-shot empty cue warnings; no new warning class appeared. All eleven required captures were regenerated by the repaired run; both re-inspected Results captures are complete and readable and their hashes are unchanged: Chrome `B15B2D005F4B3206CB1EAF6025813407DCA11C0063B4A21640DF6C5A2134BFAA`, Edge `B19F8D7408BAF6325B7958C42D692798E7018AB72BEA62234599778DD60970A8`.
- 2026-07-19 guided-builder whole-branch review: the independent `ce7f8ac..3089016` review against the approved design and plan confirmed all twelve required gates — both learner modes, four-part assembly/revision, current-choice assessment, untimed completion, follow-up adaptability, Retry reset, error preservation/recovery, payload/privacy compatibility (zero DTO diff from `ce7f8ac`), responsive/DPR layout, keyboard/focus, browser smoke, and explicit human-review boundaries — with zero P0-P2 findings. Of the `27` retained Minor findings, `6` were verified already resolved, `18` accepted as recorded debt, and `3` triaged actionable P3 alongside two fresh P3 dead-code/hygiene findings. Verdict: merge-ready after P3 cleanup.
- 2026-07-19 guided-builder P3 cleanup: deleted the orphaned legacy smoke helpers `playAttempt`, `keyboardAction`, `pointerAction`, `mouseResponse`, and `mouseContinue` (`165` dead lines with zero call sites and legacy pitch-room coordinates); added the previously missing `selectOption` mutation demonstration to the hidden-control Node contract; removed the dead `button.gameObject != null` check and the redundant per-`Render` `ApplySharedStyle` call from `ModeSelectionView` (style is applied once by `Configure` and serialized into generated scenes); documented the deliberately dormant legacy cluster (`GameScreenRouter`, `PitchRoomPresenter`, `ResultsPresenter`, `TutorialPresenter`, `PitchSessionController`, and the legacy `smart-school-garden.en.json` fixture) as a post-merge retirement candidate in `docs/05-TECHNICAL-ARCHITECTURE.md` per the plan's untouched-legacy constraint.
- 2026-07-19 guided-builder recovered-Start flake RED: the first post-cleanup matrix run failed Chrome at `Canvas content and controls did not settle after recovered Briefing` with `modes.missingConfig: false` and zero console/page errors, while Edge passed. Root cause: Unity's frame-polled WebGL input missed the single held press on the recovered Title during a relaunch stall, the whole-canvas change gate accepted the focus tint alone, and the app never left Title — the same press-miss class the earlier vertical-slice matrix documented. A RED-first Node contract now requires `verifyMissingConfigRecovery` to call `pressCanvasUntilContentChange(page, canvas, 0.5, 0.61, ...)` gated on the content region, forbids the whole-canvas gate for the recovered Start, and mutation-tests a single-unretried-press replacement with an applied-mutation guard; the focused suite first reported the intended `9/10` RED.
- 2026-07-19 guided-builder final verification: `node --check` exited zero; the full Node suite passed `20/20` with zero failures/skips/todos. Canonical EditMode passed `370/370` (duration `3.6042203 s`; complete `906`-line log, zero configured markers) and PlayMode passed `48/48` (duration `2.3099064 s`; complete `895`-line log, zero configured markers) after the `ModeSelectionView` cleanup. The final matrix on port `63464` passed Chrome `150.0.7871.127` Primary keyboard `15:05:05.793Z`-`15:05:52.988Z` (`7,654 ms` load) and Edge `150.0.4078.83` Secondary pointer `15:05:52.988Z`-`15:06:50.264Z` (`7,647 ms`), both `status: passed` with `modes.success/failure/missingConfig` all `true`, zero console/page errors, `12` disclosed warnings, clean server shutdown with zero stderr, and unchanged Results hashes Chrome `B15B2D00...`/Edge `B19F8D74...`. Firefox remained unavailable and is not claimed.
- 2026-07-20 guided-builder judge-reaction design decision: the 30 authored cues reached `GuidedPitchSessionEvent.ReactionCue` but no guided UI consumed them, so Judge Aya was a frozen `Encouraging` portrait for the whole lesson. Resolved with approach (A): `GuidedPitchSessionSnapshot` gained `LastReactionCue`, set from the selected option and cleared wherever the accompanying feedback clears. Chosen over routing the cue through `GuidedPitchScreenRouter.HandleSessionEvent` because the guided architecture's stated rule is that presenters render snapshots, and event-carried state would latch on `Retry` (no event is published) and on the router's double-refresh path (controller event plus `changed` callback). `LmsLaunchConfig`/`LmsCompletionPayload` are byte-identical to `ce7f8ac` — the cue is presentation state and never enters the payload.
- 2026-07-20 guided-builder judge-reaction RED (domain): the controller fixture produced no XML and reached the expected missing-production-API boundary with `48` `CS1061` matches for `GuidedPitchSessionSnapshot` lacking `LastReactionCue`; the complete `745`-line log ended with `Scripts have compiler errors.`
- 2026-07-20 guided-builder judge-reaction RED (UI compile): the PlayMode run produced no XML and reached the expected boundary with `3` `CS1501` matches for `No overload for method 'Configure' takes 15 arguments`; the complete `676`-line log ended with `Scripts have compiler errors.`
- 2026-07-20 guided-builder judge-reaction RED (behavioral, defect proof): with the presenter seam compiling but no render call, PlayMode reported `47/53` with exactly six failures and a complete `859`-line log carrying `0` compile/exception markers. Five were the new judge assertions failing as `Expected: Concerned But was: Idle` (and `Expected: Talk But was: Idle`), proving the portrait never changed after a selection; the sixth was `BootstrapScene_BootsTheGuidedComposition...` reporting `guided_scene_contract_invalid` because the saved scene predated the judge reference. Every reaction assertion checks both `CurrentReaction` and that the portrait `Image.sprite` swapped to a distinct per-reaction sprite, so a view reporting the right enum over a frozen image still fails.
- 2026-07-20 guided-builder judge-reaction ambient-motion RED (browser): the first implementation kept `JudgeReactionView`'s blink/talk loops, and the browser matrix failed both browsers at `Required stable content/control regions did not change after build` — a perpetually animating portrait can never satisfy the smoke's three-consecutive-identical-region settle gate. Rather than weaken a proven gate, Aya now rests on a still `Encouraging` face and changes only when a statement changes: the design requires that she reacts, never that she idles, and these untimed reading screens should not carry motion that competes with a young learner's reading. The neutral face is held through the same semantic channel, which keeps `semanticLatched` true and is what prevents `Update` from entering the blink/talk branches. New guard `Judge_HoldsAStillPortraitBetweenStatements_WithNoAmbientLoop` watches the sprite for `6 s` — past the `5 s` blink interval — both before a selection and after settling.
- 2026-07-20 guided-builder judge-reaction GREEN: canonical EditMode passed `372/372` (duration `2.8117586 s`; complete `867`-line log) and PlayMode `54/54` (duration `13.9879871 s`; complete `888`-line log), both with `0` failures, skips or inconclusive tests and `0` matches for `error CS\d+|Compilation failed|Unhandled Exception`. Baselines were `370`/`48`; the six new PlayMode cases cover Primary Build, Secondary Improve and follow-up, the settle, reduced motion, Retry, and the still-portrait guard, and the two new EditMode cases cover the cue lifetime and its absence from the payload. The full Node suite passed `20/20` after the capture-sequence contract was updated to `15` captures.
- 2026-07-20 guided-builder judge-reaction scene regeneration: `BuildProjectFoundationBatch` completed with `1` `Pitch Simulator project foundation built successfully.` marker, `Exiting batchmode successfully now!`, and `0` failure-marker matches. Only `Assets/Scenes/Game.unity` changed; the incidental `ProjectSettings.asset` normalization (`targetPixelDensity`, `buildNumber`, iOS/tvOS/visionOS target strings) was verified unrelated and reverted, and untracked `ProjectSettings/SceneTemplateSettings.json` was deleted. `Judge Aya` now carries `JudgeReactionView` with `portraitImage` bound to its own `Image` and all `11` reactions resolved by name to distinct slices of `judge-aya-sheet.png`; a missing slice throws instead of silently falling back. All slices are `128x160`, so the reserved row size holds as the portrait swaps and the art is never stretched. The builder test that previously asserted `JudgeReactionView` was absent — the assertion that locked the defect in — now requires exactly one.
- 2026-07-20 guided-builder judge-reaction browser evidence: Playwright `1.61.1` on `http://127.0.0.1:65443`, generated `2026-07-20T10:36:28.308Z`, passed Chrome `150.0.7871.127` Primary keyboard (`6,948 ms` load) and Edge `150.0.4078.83` Secondary pointer (`6,937 ms`), both `status: passed` with `modes.success/failure/missingConfig` all `true`, zero console errors, zero page errors, `12` disclosed warnings each, and clean server shutdown with zero stderr. Firefox remained unavailable and is not claimed.
- 2026-07-20 guided-builder judge-reaction visual proof: no pre-existing capture could show a reaction, because Chrome captured only build/improve/present and Edge only build/present — all question moments where Aya rests. Four feedback-moment captures were added (`chrome-primary-reaction-developing.png`, `chrome-primary-reaction-clear.png`, `edge-secondary-reaction-first.png`, `edge-secondary-reaction-second.png`). Aya's portrait region was cropped from each PNG and inspected at original detail: the resting face is `Encouraging` (warm smile, thumbs up), a Developing statement renders `Interested` (raised brows, open mouth) via the authored `Curious` cue, and a Clear statement renders `Impressed` (wide eyes, open mouth). Three visually distinct faces confirmed in both browsers and both learner modes; whole-canvas hashes were rejected as proof because feedback text differs independently of the portrait.
- 2026-07-20 guided-builder judge-reaction independent review: an independent review of `5c70bc9` returned one Critical, two Important and three Minor findings; all six were addressed and the design-deviation question was answered. CORRECTION to the entry above as first committed: it claimed the semantic hold "was raised to `2.5 s`", but the committed scene carried `1.6`, so the first browser evidence was captured against a `1.6 s` hold and an `Idle` resting sprite. The reactions observed were real, but that specific claim was false at `5c70bc9` and is retracted here.
- 2026-07-20 guided-builder judge-reaction review fix (Critical, stale scene): the root cause was not a missed regeneration. `PitchSimulatorProjectBuilder.BuildGameScene` returns early whenever `HasCurrentGameContract` passes, and that predicate checked no judge state — so once Judge Aya existed at all, no later change to her tuning or resting sprite could ever reach the saved scene, and three successive builder runs left `Game.unity` byte-identical with `Game.unity` mtime unchanged. `HasCurrentJudgeContract` now makes the view reference, all eleven named sprites, the four tuning values and the `Encouraging` resting sprite part of what makes a scene current; the builder constants became `internal` so the guard compares against the single source of truth. The regenerated scene carries `blinkIntervalSeconds: 5`, `blinkDurationSeconds: 0.12`, `talkFrameSeconds: 0.18`, `semanticHoldSeconds: 2.5`, and `AssertSerializedFloat` in the builder test now fails on any future drift.
- 2026-07-20 guided-builder judge-reaction review fix (Important, resting sprite): `JudgeReactionView.Configure` applies the `Idle` frame as its last statement, silently clobbering the `Encouraging` sprite the builder had just assigned, so the saved scene serialized the Idle portrait — visible to anyone opening the scene and to any frame rendered before the presenter initializes. The builder now reapplies the resting portrait after `Configure`, and the builder test asserts `judgeImage.sprite` is the `Encouraging` slice rather than merely non-null.
- 2026-07-20 guided-builder judge-reaction review fix (Important, evidence fragility): the reaction captures were plain screenshots taken after the settle gate returned, so on a slow machine the `2.5 s` hold could expire first and the run would silently bank four pictures of the resting face while still reporting `passed`. `captureJudgeReaction` now hashes Judge Aya's portrait clip alone and throws when it matches the preceding baseline, converting the visual evidence from a hopeful screenshot into an executable assertion; the Node contract pins the helper, the per-path baseline ordering, and the throw itself.
- 2026-07-20 guided-builder judge-reaction review fix (Minors): `JudgeReactionView` now sets `latchedSemanticReaction` when the settle applies `Encouraging`, so a cue repeating after its predecessor settled is shown again instead of being swallowed as a duplicate — covered RED-first by `View_RepeatsAReactionThatAlreadySettled_InsteadOfSwallowingIt`. The constraint that no `GuidedGate.content` step may immediately follow a reaction (Aya's cell overlaps the gated content region, so her settle could satisfy a content-only gate and mask a missed press) is documented at the portrait-hash helper. Retained as accepted debt: the PlayMode rig exercises a `0.2 s` hold rather than the scene's `2.5 s`, and the still-portrait guard costs ~12 s of wall clock.
- 2026-07-20 guided-builder judge-reaction review-fix verification: canonical EditMode passed `372/372` (duration `2.5693942 s`; complete `871`-line log) and PlayMode `55/55` (duration `13.8072817 s`; complete `888`-line log), both with `0` failures, skips or inconclusive tests and `0` failure-marker matches; the Node suite passed `20/20`. The builder log carried `1` success marker, `Exiting batchmode successfully now!` and `0` failure markers. The re-run matrix on `http://127.0.0.1:49581`, generated `2026-07-20T10:57:58.112Z`, passed Chrome `150.0.7871.127` (`7,315 ms`) and Edge `150.0.4078.83` (`7,965 ms`) with `modes.success/failure/missingConfig` all `true`, zero console and page errors, and the reaction assertion active — so both browsers proved the portrait changed at every feedback moment rather than merely capturing an image. Aya's crops were re-inspected against the corrected scene and still show three distinct faces.
- 2026-07-20 guided-builder judge-reaction retained design question: the independent review argued the timed settle should be removed entirely and the reaction pinned until the learner advances, which would delete the tuning constant, remove the capture race and simplify the gate interactions. This was declined because the task's requirement 4 explicitly asks that Aya "settles back to a neutral/encouraging state rather than latching a `Concerned` face indefinitely"; the fragility it causes was fixed instead. Worth confirming during the educator review, since it is a pedagogy question rather than an engineering one.
- 2026-07-20 guided-builder judge-reaction re-review (round two): a fresh independent review of `5c70bc9..7c6b1da` confirmed all four prior findings resolved and found three new Important issues plus three Minors. All were addressed. (1) `JudgeReactionView.Awake` applied `Idle` as its last act, and because the guided panel is saved inactive while `GuidedPitchScreenRouter.Refresh` refreshes presenters (line `178`) before it activates the panel (line `190`), `Awake` ran *after* the presenter had already applied the resting face and clobbered it — then the semantic latch refused to correct it, leaving a learner on the Idle portrait for a full hold. The builder's post-`Configure` patch had only fixed what an editor sees. The resting face is now the view's own default (`RestingReaction`), `Awake` only applies it when nothing is latched or speaking, and the builder patch was deleted. The PlayMode rig had missed this because it builds the judge on an already-active object, so `Awake` ran before `Configure`; `View_KeepsTheRestingFace_WhenAwakeRunsAfterTheFirstRender` reproduces the real inactive-panel ordering. (2) The second reaction capture on each path compared only against the preceding reaction, so a settled-back portrait would differ from it and pass — the exact silent failure the assertion existed to prevent. `captureJudgeReaction` now rejects both the resting hash and the preceding reaction hash, with both throws pinned by the Node contract. (3) The remaining timing coupling between the `2.5 s` hold and the unbounded settle gate is accepted as fail-loud rather than silent: a slow machine now fails the run instead of banking a resting-face image. Minors: `RequireProperty` makes a renamed serialized field throw a named error instead of an opaque NRE inside the idempotence guard; the settle re-trigger invariant is documented on `RenderJudge`; the portrait clip's reliance on a ~42 px margin from the question text is recorded below.
- 2026-07-20 guided-builder judge-reaction round-two verification: canonical EditMode passed `372/372` (duration `2.4309644 s`; complete `867`-line log) and PlayMode `56/56` (duration `13.9511611 s`; complete `890`-line log), both with `0` failures, skips or inconclusive tests and `0` failure-marker matches; the Node suite passed `20/20`. The builder log carried `1` success marker and `0` failure markers, and the regenerated scene serializes the `Encouraging` resting sprite (fileID `-8798918205640081495`) with `semanticHoldSeconds: 2.5`. The final matrix on `http://127.0.0.1:50863`, generated `2026-07-20T11:14:09.047Z`, passed Chrome `150.0.7871.127` (`6,943 ms`) and Edge `150.0.4078.83` (`7,841 ms`) with `modes.success/failure/missingConfig` all `true`, zero console errors, zero page errors and zero server stderr, under the strengthened two-sided reaction assertion. Firefox remained unavailable and is not claimed.
- 2026-07-20 guided-builder judge-reaction accepted debt: (a) the scene idempotence guard remains a hand-maintained property whitelist, so a future builder change to something it does not check (Aya Row `preferredHeight`, the `LayoutElement` 128x160, `preserveAspect`) can go stale exactly as `semanticHoldSeconds` did; stamping a `GeneratorVersion` on the generated root and comparing it first would make the property checks a safety net rather than the mechanism. (b) The smoke's portrait clip is an unasserted rectangle whose meaning depends on a ~42 px margin between Aya's cell and the question text; if a layout change pushes that text into the clip, the hash would differ for text reasons and the assertion could pass over a frozen portrait — an EditMode assertion pinning Judge Aya's normalized rect would close it. (c) The PlayMode rig exercises a `0.2 s` hold rather than the scene's `2.5 s`, and the still-portrait guard costs ~12 s of wall clock.
- 2026-07-20 generator-version compile RED: `GeneratedScenes_StampGeneratorVersion_AndRegenerateOnlyWhenItChanges` reached the intended missing-production-API boundary. No XML was produced, and the complete `691`-line log contained exactly `9` x `CS0117` naming the absent `PitchSimulatorProjectBuilder.GeneratorVersion` and `9` x `CS0246` naming the absent `GeneratedSceneStamp`, plus one `Scripts have compiler errors.` marker.
- 2026-07-20 generator-version behavioural RED: with `GeneratedSceneStamp` and the `GeneratorVersion` constant declared but no stamping wired into the builder, EditMode compiled and reported `375` total with exactly `1` failure - the new test, failing for the intended reason: `Assets/Scenes/Bootstrap.unity must carry exactly one generator stamp on its owned root. Expected: property Length equal to 1 But was: 0`. The other `374` tests still passed, proving the stamp was the only missing behaviour.
- 2026-07-20 generator-version GREEN: canonical EditMode passed `375/375` (XML `Passed`, `0` failures, skips or inconclusive tests, duration `4.5165859 s`; complete `1492`-line log with `0` failure-marker matches) and canonical PlayMode passed `56/56` (XML `Passed`, `0` failures, skips or inconclusive, duration `14.8141895 s`; complete `858`-line log, `0` failure markers). The Node suite passed `21/21`. The builder log carried `1` success marker and `0` failure markers, and all three owned roots now serialize the stamp script `dfc08b8368e1cee489a8f904557eda16`.
- 2026-07-20 generator-version mechanism: `ReplaceOwnedRoot` stamps `GeneratorVersion` on every owned root, and `HasCurrentBootstrapContract`, `HasCurrentGameContract` and `HasCurrentWebIntegrationContract` each compare it before their property whitelist. The test proves both directions on all three scenes: an unchanged version reuses the identical owned root (`GlobalObjectId` unchanged, no regeneration), and a differing stamped version forces every scene to regenerate and re-stamp regardless of what the whitelist inspects. This closes accepted debt item (a) above - the `semanticHoldSeconds` class of silent no-op can no longer ship.
- 2026-07-20 legacy-retirement reachability: traced outward from `Bootstrapper`, which composes only `GuidedPitchSessionController`, with a case-sensitive whole-word census over every `Assets/**/*.cs`. `GameScreenRouter` had zero production references and was the single dead root; deleting it made `PitchRoomPresenter`, `TutorialPresenter` and the legacy `ResultsPresenter` unreferenced, which in turn freed `ConfidenceView`, `TimerView`, `ResponseListView`, `ResponseButtonView`, `QuestionReviewItemView`, `KeyboardReviewScrollbar`, `FocusNavigator`, `PitchSessionController`, `PitchSessionSnapshot`, `PitchSessionEvents`, the `GameState`/`GameStateMachine`/`GameCommand`/`QuestionTimer` machine, and every type in the `Dialogue`, `Dialogue.Unity` and `Scoring` assemblies. `AudioCueDirector.HandleSessionEvent` and `UpdateTimer` had no production caller (only their own tests) and were the last holders of `PitchSessionEvent` and `GameState`, so both methods went with the cluster; `HandleUserGesture` and `HandleGuidedSessionEvent`, which `Bootstrapper` actually wires, are untouched. An earlier case-insensitive pass falsely implicated `ResultsPresenter`, `TitlePresenter`, `BriefingPresenter` and `SettingsPresenter` through `GuidedPitchResultsPresenter` and lowercase fields; the case-sensitive re-run is what the deletion set is based on.
- 2026-07-20 legacy-retirement survivors: `JudgeReactionView`, `JudgeReactionMapper` and `JudgeReactionSpriteSet` stay - they are referenced by `GuidedPitchPresenter`, `GuidedPitchSceneBuilder` and `PitchSimulatorProjectBuilder`, and the generated-scene guard still asserts exactly one reaction view drives Judge Aya. `AudioCueDirector` stays. `TitlePresenter`, `BriefingPresenter`, `SettingsPresenter` and `GuidedPitchResultsPresenter` stay - all four are wired by `GuidedPitchScreenRouter`. The `AudioCue` inventory keeps all nine cues including the now-unplayed `TimerWarning`, because the nine-cue contract is asserted by `AudioServiceTests` and serialized into the Bootstrap scene bindings. `SaveData`/`SaveDataMigrator` are also unreachable but are persistence utilities outside this cluster and were deliberately left alone.
- 2026-07-20 legacy-retirement RED: the new `LegacyClusterRetirementTests` failed for exactly the intended reason before any deletion - EditMode reported `378` total with `3` failures naming all `31` surviving retired type names, all `3` retired assemblies (`Agrovator.PitchSimulator.Dialogue`, `.Dialogue.Unity`, `.Scoring`), and the surviving `Assets/Content/Scenarios/smart-school-garden.en.json`. The other `375` tests passed, so the retirement was the only unmet condition.
- 2026-07-20 legacy-retirement GREEN: canonical EditMode passed `249/249` (XML `Passed`, `0` failures, skips or inconclusive, duration `3.9870031 s`; complete `1493`-line log, `0` failure-marker matches) and canonical PlayMode passed `56/56` unchanged (XML `Passed`, duration `14.7575502 s`; complete `858`-line log, `0` failure markers). The Node suite passed `21/21`, and a post-deletion builder run reported `1` success marker, `0` failure markers and left all three owned scenes byte-identical, proving the deletion did not disturb generated scene contracts.
- 2026-07-20 legacy-retirement test-count reconciliation: `378 - 129 = 249`, and every removed case is accounted for. Whole deleted fixtures: `ScoringTests` `31`, `DialogueSessionTests` `19`, `QuestionTimerTests` `18`, `PitchSessionControllerTests` `16`, `ScenarioValidatorTests` `13`, `SmartSchoolGardenContentTests` `11`, `ScenarioJsonLoaderTests` `9`, `GameStateMachineTests` `6`, `ScenarioAssetTests` `1` - `124` cases, each covering only deleted production types. `AudioCueDirectorTests` lost `5` more: `SessionEvents_MapToEveryReservedOutcomeCue` (`1`) and `TimerWarning_PlaysOnceInFinalFiveAndResetsForNextQuestion` plus the three `TimerWarning_IgnoresInactiveTimers` cases (`4`), all covering the two deleted director methods. Its guided coverage is untouched, including the assertion that the guided path never plays `TimerWarning`. The `3` new retirement tests offset part of the drop, so the arithmetic is `378 - 124 - 5 = 249`.
- 2026-07-20 legacy-retirement guard integrity: `AssertNoLegacyPresentation` could no longer name the deleted types in a generic argument, so it now matches component type names against the retired-name list and still fails if any of them reappears in an owned scene; the same rewrite was applied to the PlayMode assertion that the guided panel presents neither a timer nor a confidence meter. `PixelArtImportTests.AuthoredReactionCues_AllResolveToExpectedSemanticStates` was repointed from the deleted scenario to the live `guided-pitch-builder.en.json`, whose authored cues are `Concerned`, `Curious` and `Impressed` - `Encouraging` is Aya's resting face rather than an authored cue - and it still pins the unknown-cue fallback to `Encouraging`.
- 2026-07-20 legacy-retirement assembly boundaries: `Agrovator.PitchSimulator.Dialogue`, `.Dialogue.Unity` and `.Scoring` were deleted with their `.asmdef` and `.meta` files, and the four remaining reference lists that named them - `UI`, `Session`, `EditModeTests` and `PlayModeTests` - were trimmed. A repository-wide `.asmdef` scan finds no remaining reference to any removed assembly, and `RetiredLegacyAssemblies_AreNotLoaded` fails if one is ever reintroduced.
- 2026-07-20 release-build RED: `WebGlBuildTests` reached the intended missing-production-API boundary twice. The first RED produced no XML, and the complete `673`-line log carried exactly `3` x `CS0117` for the absent `CreateReleaseBuildOptions` and `3` x `CS0117` for the absent `ApplyReleaseSettings`, with one `Scripts have compiler errors.` marker. After the first release build revealed the real compressed filenames, a second RED for `StampedAssetNames` produced no XML with `3` x `CS0117` in a `667`-line log.
- 2026-07-20 release-build cache-bust trap, caught in the build rather than in production: the first release build succeeded but `StampBuildAssets` threw `BuildFailedException: Cache busting stamped 1 of 4 loader asset URLs. The built filenames do not match the names the stamper knows.` Unity names fallback-compressed assets `.unityweb`, not `.br` or `.gz`, and leaves only `WebGL.loader.js` uncompressed - which is exactly the one URL that matched. Two defects were fixed test-first: the stamping regex now accepts `(\.unityweb|\.br|\.gz)?` and preserves the suffix, and `ComputeContentStamp` hashes those variants. Without the second fix the release stamp would have been derived from `loader.js` alone and would not have changed when the wasm changed - the precise stale-cache failure the stamp exists to prevent. The `stampCount != AssetNames.Length` guard is what converted a silent no-op into a failed build, and it is retained.
- 2026-07-20 release-build measured sizes: the development build tree was `92,636,841` bytes (`88.35` MB) with `WebGL.wasm` at `82,821,956` bytes; the release tree is `8,054,687` bytes (`7.68` MB), a `91.3%` reduction. Per asset: `WebGL.wasm` `82,821,956` -> `WebGL.wasm.unityweb` `4,792,301` (`-94.2%`), `WebGL.data` `9,033,276` -> `WebGL.data.unityweb` `3,066,079` (`-66.1%`), `WebGL.framework.js` `712,694` -> `.unityweb` `67,855` (`-90.5%`), and `WebGL.loader.js` `58,622` -> `118,132` (`+101%`, because it now carries the decompressor). Unity's own report was `result=Succeeded; totalSize=8054627; warnings=0; errors=0`. The post-retirement development build is `92,260,313` bytes, so deleting the legacy cluster removed `376,528` bytes from the development player as well.
- 2026-07-20 release-build load time: measured in one session on the same machine and harness, the release build loaded in Chrome `4,333 ms` and Edge `4,039 ms`, against `11,828 ms` and `10,746 ms` for the development build immediately afterwards. Only the same-session comparison is claimed; the historical `~7 s` development figure was recorded under a different machine load. Both figures come from a loopback server where transfer is effectively free, so they capture decompression and wasm compile time only - the `91.3%` transfer saving that matters on Malaysian school wifi is stated as bytes and was not measured as wall clock from here.
- 2026-07-20 release-build Pages compatibility: GitHub Pages serves bytes verbatim and cannot set `Content-Encoding`, so compression is only correct behind Unity's decompression fallback. `tools/Serve-WebGL.ps1` also sends no `Content-Encoding`, so smoking the release build locally exercises exactly the Pages path rather than inferring it. The release matrix on that server, generated `2026-07-20T16:17:56.237Z`, passed Chrome `150.0.7871.127` and Edge `150.0.4078.83` with `modes.success/failure/missingConfig` all `true`, zero console errors, zero page errors and zero server stderr. All four loader URLs are stamped with the suffix intact (`WebGL.wasm.unityweb?v=c0a2d6b562f7`).
- 2026-07-20 release-build managed stripping, a claim withdrawn under evidence: `ApplyReleaseSettings` initially set `ManagedStrippingLevel.High` and the development path set `Minimal` to restore it. A development-only build from reverted settings still serialized `managedStrippingLevel: WebGL: 4`, and removing the setter removed the key from the diff entirely - so Unity coerces WebGL below-High requests upward while the in-editor getter still reports what was set, which is why the assertion passed while the artifact disagreed. Both setters and both assertions were removed rather than shipping a test that asserts `Minimal` against a `High` artifact. `Il2CppCodeGeneration` is kept because it does persist per flavour (`WebGL: 0` after a development build), and it is the one intentional `ProjectSettings.asset` change committed with this task. The size reduction is therefore attributed to the non-development player plus Brotli, with no per-knob attribution claimed.
- 2026-07-20 release-build shape preservation: `tools/Build-WebGL.ps1` gained a `-Release` switch and defaults to the development build, so `node tools/smoke-webgl.mjs` and the Node contract tests keep the output shape they depend on; release logs to `artifacts/logs/webgl-build-release.log` so a release never overwrites the development log. `WebGlBuildTests` retains both development assertions and adds the release contract, the three compressed-suffix stamping cases and the stamped-filename coverage test.
- 2026-07-20 release-build GREEN: canonical EditMode passed `255/255` (XML `Passed`, `0` failures, skips or inconclusive, duration `4.2803303 s`; complete `1502`-line log, `0` failure markers), canonical PlayMode passed `56/56` (duration `14.3916769 s`; complete `859`-line log, `0` failure markers), and the Node suite passed `21/21`. The final development build reported `result=Succeeded; totalSize=92260253; warnings=0; errors=0` in a complete `1858`-line log with `0` failure markers and stamped token `9ec50140e93f`, and the final development matrix generated `2026-07-20T16:30:38.634Z` passed Chrome and Edge with `modes.missingConfig` `true`, zero console errors, zero page errors and zero server stderr. Firefox remained unavailable and is not claimed.
- 2026-07-20 independent review round one: a fresh reviewer covered all three commits and confirmed the generator stamp is correctly plumbed through the single `ReplaceOwnedRoot` creation point, that the original `semanticHoldSeconds: 2.5` defect is repaired in the saved scene, that Brotli plus `decompressionFallback` is correct for GitHub Pages, and - verified independently by extracting the GUID of every deleted `.cs.meta` at `284b1a6` and grepping all of `Assets/` - that nothing live was deleted, no scene carries a missing-script reference, no orphaned `.meta` remains, and no `.asmdef` reference dangles. One Critical, five Important and five Minor findings were raised.
- 2026-07-20 review Critical, learner-facing and confirmed before fixing: `ApplyReleaseSettings` set `WebGLExceptionSupport.None`, which does not merely drop stack traces - it stops managed exceptions being caught at all. The safe-fallback screen is reached exclusively through `catch` blocks, and `11` were confirmed by inspection: `Bootstrapper.cs:153` (`FormatException` around the catalog, routing to `EnterSafeFallback`), `GuidedPitchContentLoader.cs:54/58/62/82`, and six in `WebGlLmsBridge.cs` including the bare catch whose `null` return is the only path that produces the `guided_launch_invalid` screen. A learner receiving a malformed launch payload or a corrupted content download would have got an aborted player instead of the recovery screen. The smoke never caught it because `missingConfig` returns empty JSON through an `IsNullOrWhiteSpace` branch and `failure` reports through a callback, so neither path throws. Fixed test-first to `ExplicitlyThrownExceptionsOnly` (RED: no XML, `6` x `CS0117` in a `673`-line log), which ships no stack traces and keeps every catch alive.
- 2026-07-20 review Critical cost, reported rather than hidden: exception support is not free. The release tree grew from `8,054,687` to `9,291,199` bytes (`+15.4%`), `WebGL.wasm.unityweb` from `4,792,301` to `5,633,979` and `WebGL.data.unityweb` from `3,066,079` to `3,460,991`. The reduction against the `92,636,841`-byte development baseline is therefore `90.0%`, not the `91.3%` recorded before the fix. Roughly `1.24` MB is the price of the learner keeping a recovery screen, which is the right trade.
- 2026-07-20 review Important fixes: (2) `docs/10-WEB-DEPLOYMENT.md` documented the development build as the deploy procedure - anyone following it would have shipped the `92` MB player - and now leads with `-Release`, records the `.unityweb` output shape and the separate release log, and states why the fallback is mandatory while Pages is the host. (3) `ApplyReleaseSettings` ends in `SaveAssets`, so a release build left version-controlled release knobs behind and silently changed the shape of the next development build; `BuildRelease` now restores development settings in a `finally`, verified by a release build afterwards leaving only the known incidental `ProjectSettings.asset` normalization with no `webGLExceptionSupport`, `webGLCompressionFormat` or `il2cppCodeGeneration` residue. (5) Both rewritten legacy guards filtered null components away, hiding exactly the missing-script failure a mass MonoBehaviour deletion can cause; both now assert no null component before the name check. (6) `LegacyClusterRetirementTests` matched bare type names, so a future legitimate `GuidedPitch.GameState` would have failed a retirement test it has nothing to do with; it now matches `31` fully-qualified names and handles `ReflectionTypeLoadException` by checking the types that did load.
- 2026-07-20 review Important deliberately not implemented: the reviewer correctly observed that bumping `GeneratorVersion` is still manual and unenforced, so a builder change without a bump can still no-op behind the whitelists - the same failure mode one level removed. Deriving the version from a hash of the builder sources would make the invariant self-maintaining, but that is a design change beyond this task, so the obligation is documented on the constant and in `AGENTS.md` and the gap is carried as an open concern rather than silently expanded scope. The suggestion to make `StampedAssetNames` `internal` with `InternalsVisibleTo` was declined: the project has no `InternalsVisibleTo` anywhere and the machinery costs more than the encapsulation buys.
- 2026-07-20 review Minor fixes: `ComputeContentStamp` now throws when it hashes zero files, because hashing nothing yields a constant stamp that every build would share; `CountStampedAssets` counts per asset rather than total occurrences, with a test proving a duplicated asset cannot compensate for a missing one; `GeneratedSceneStamp` records that nothing reads it at runtime; and the UTF-8 BOMs my PowerShell edits introduced into four `.asmdef` files and `tools/Build-WebGL.ps1` were stripped.
- 2026-07-20 review round-one verification: canonical EditMode passed `256/256` (XML `Passed`, `0` failures, skips or inconclusive, duration `4.0698136 s`; complete `1532`-line log, `0` failure markers), canonical PlayMode passed `56/56` (duration `14.4732152 s`; complete `859`-line log, `0` failure markers), and Node passed `21/21`. The post-fix release build reported `result=Succeeded; totalSize=9291139; warnings=0; errors=0` with `0` failure markers in a `1865`-line log and stamped token `06e115502e90`; its matrix generated `2026-07-20T16:58:56.319Z` passed Chrome `150.0.7871.127` (`3,370 ms`) and Edge `150.0.4078.83` (`3,328 ms`). The final development build restored the uncompressed shape at `92,260,313` bytes with stamped token `719e1ae69119`, and its matrix generated `2026-07-20T17:03:08.753Z` passed Chrome (`6,564 ms`) and Edge (`6,661 ms`). Every run had `modes.success/failure/missingConfig` all `true`, zero console errors, zero page errors and zero server stderr. Firefox remained unavailable and is not claimed.
- 2026-07-20 independent review round two: a second review verified all ten round-one fixes at code level and returned `Ready to merge: Yes`. It confirmed the `CountStampedAssets` regex cannot over-count (one boolean per asset, ceiling `4`) and cannot silently under-count (the shared `.js` tail of `WebGL.loader.js` and `WebGL.framework.js` cannot cross-match because the leading `/` and the required `?v=` anchor both ends, and any URL outside a backtick literal would also go unstamped and trip the `stampCount != 4` guard); that the `hashed == 0` throw cannot fire legitimately, since `StampBuildAssets` runs only after `BuildPipeline` reports `Succeeded`; that `component == null` is the correct Unity superset covering both genuinely missing scripts and fake-null destroyed objects, asserted before the later dereference; and that deferring the `GeneratorVersion` enforcement was defensible because source hashing is brittle against formatting-only edits and the property whitelists remain as a second net. It also read `Assets/Plugins/WebGL/PitchSimulatorBridge.jslib` and confirmed no JavaScript throw crosses the interop boundary, so the six bare catches in `WebGlLmsBridge` are not load-bearing against uncatchable JS exceptions.
- 2026-07-20 review round two Important, a false claim in my own commit message: the round-one commit asserted that `ExplicitlyThrownExceptionsOnly` "keeps every catch alive", which is untrue for the `NullReferenceException` arm at `GuidedPitchContentLoader.cs:82`. IL2CPP emits no null checks, so a null dereference in the release player is an unrecoverable wasm trap rather than a managed exception. The two unguarded dereferences were confirmed at `GuidedPitchContent.cs` `Compile` (`dto.Modes`) and `GuidedLearnerModeContent.Compile` (`dto.Parts`). No RED was possible for this: `GuidedPitchContentValidator` coerces null collections and errors with `guided.mode_missing` before `Compile` runs, so the state is unreachable through both public entry points, and `Compile` is `internal` with no `InternalsVisibleTo` in the project. Rather than write a test that would pass identically before and after and present it as TDD, it was fixed by construction - both sites now throw `InvalidOperationException` explicitly, which the release player can catch and turn into `guided.compile_failed` - and the surviving `NullReferenceException` arm is documented as an editor and PlayMode net only.
- 2026-07-20 review round two other fixes: the release settings restore no longer discards the exception that explains a build failure (it logs its own failure instead, which matters because `tools/Build-WebGL.ps1` greps the log specifically for `BuildFailedException`), and it is gated on a flag set after `ApplyReleaseSettings` succeeds so a build that fails in `ValidateScenes` or the target switch no longer rewrites untouched player settings. The template's `symbolsUrl` is documented as knowingly excluded from stamping, since the release player emits no symbols file. Two findings were declined as decisions rather than omissions: the two scene guards keep bare-name matching because a scene-level guard wants the broader net and a false positive lands on a builder-owned scene, while the project-wide assembly guard is where fully-qualified matching belongs; and the reported `9,291,199` and `9,291,139` figures are not a discrepancy but two different measures - the on-disk tree total and the BuildReport `totalSize` - now labelled as such wherever both appear.
- 2026-07-20 review round two verification: canonical EditMode passed `256/256` (XML `Passed`, `0` failures, skips or inconclusive, duration `2.6133719 s`; complete `1510`-line log, `0` failure markers), canonical PlayMode passed `56/56` (duration `13.891694 s`; complete `855`-line log, `0` failure markers), and Node passed `21/21`. A release build exercised the new restore control flow on a path EditMode cannot reach: `result=Succeeded; totalSize=9297033; warnings=0; errors=0`, `0` failure markers in an `1890`-line log, stamped token `8f2e22951256`, on-disk tree `9,297,093` bytes, and afterwards `ProjectSettings.asset` carried no `webGLExceptionSupport`, `webGLCompressionFormat` or `il2cppCodeGeneration` residue and logged no restore failure. The release tree grew `5,894` bytes against round one because the new explicit null guards compile into the player. The final development build restored the uncompressed four-asset shape at `92,260,637` bytes on disk (`totalSize=92260577`) with stamped token `9c13c9c194cc` in a complete `1859`-line log with `0` failure markers, and its matrix generated `2026-07-20T17:20:21.288Z` passed Chrome `150.0.7871.127` (`6,602 ms`) and Edge `150.0.4078.83` (`6,999 ms`) with `modes.success/failure/missingConfig` all `true`, zero console errors, zero page errors and zero server stderr. Firefox remained unavailable and is not claimed.
- 2026-07-20 final size and load summary: development `92,636,841` bytes at the merge base against release `9,297,093` bytes, a `90.0%` reduction; measured on one machine in one session, release loaded in Chrome `3,370 ms` and Edge `3,328 ms` against development `6,602 ms` and `6,999 ms`. Both figures come from a loopback server where transfer is effectively free, so they capture decompression and wasm compile time only; the transfer saving that matters on Malaysian school wifi is claimed as bytes and was not measured as wall clock.
- 2026-07-20 scene-freshness guard, closing the manual-bump gap: `GeneratedSceneFreshnessTests.CommittedScenes_MatchWhatTheCurrentBuilderGenerates` regenerates all three owned scenes and compares the result against what is committed, so a builder change that never reached the saved scenes now fails whether or not anyone remembered to bump `GeneratorVersion`. It forces regeneration by writing a stale stamp rather than relying on the constant, which is the mistake being guarded.
- 2026-07-20 scene-freshness design, decided by experiment rather than assumption: a byte comparison was proposed first and proved unworkable. Bumping `GeneratorVersion` to `2` and regenerating produced `9,225` insertions and `9,225` deletions against `HEAD` - pure `fileID` renumbering, for example `942586762` becoming `1255025008` - so regenerated scenes are never byte-identical even when nothing changed. The guard therefore compares a hierarchy fingerprint: transform path, active state, component type, and every visible serialized property, with `m_ObjectHideFlags`, `m_CorrespondingSourceObject`, `m_PrefabInstance`, `m_PrefabAsset`, `m_GameObject` and `m_Script` excluded because they renumber, and with object references rendered as asset paths or hierarchy paths so they survive renumbering. Sibling order is deliberately preserved rather than sorted, because screen order is part of the generated contract. The experiment's scene churn was discarded with `git checkout`, never hand-edited.
- 2026-07-20 scene-freshness RED, the historical defect reproduced: with `GuidedPitchSceneBuilder`'s Aya Row `preferredHeight` changed from `160f` to `172f` and no version bump - exactly the shape of the `semanticHoldSeconds` regression - EditMode reported `257` total with exactly `1` failure, and it was the new guard: `Assets/Scenes/Game.unity is not what the current builder generates`, differing at index `143792` on `Aya Row|m_PreferredHeight=160`. All `256` other tests passed, including every hand-maintained whitelist contract test, which is precisely why the class of defect could ship before. The experimental change was reverted and EditMode returned to `257/257`.
- 2026-07-20 scene-freshness verification: canonical EditMode passed `257/257` (XML `Passed`, `0` failures, skips or inconclusive, duration `2.8257284 s`; complete `1659`-line log, `0` failure markers), canonical PlayMode passed `56/56` (duration `13.9471224 s`; complete `855`-line log, `0` failure markers), and Node passed `21/21`. The guard restores the original scene bytes in a `finally`, verified by `git status` reporting `Assets/Scenes` clean after the run.
- 2026-07-20 deploy automation: publishing was previously manual and undocumented - no script in `tools/` and no repository reference to `gh-pages` - while the artifact published is whatever sits in `Build/WebGL`, which both the smoke and the Unity suites leave as the ~92 MB development player. `tools/Deploy-Pages.ps1` closes that: it builds the release flavour itself rather than trusting the folder, and refuses to publish unless the output contains at least three `.unityweb` assets, carries a cache-busting stamp, has that stamp attached to the compressed URLs, and totals under `40` MB. Every check throws rather than warns. Publishing is opt-in behind `-Push`, it refuses to publish from a dirty tree so a published build always traces to a commit, it publishes through a temporary git worktree, and it never force-pushes. Five Node contract tests pin those guards (RED: `ENOENT` on the absent script).
- 2026-07-20 deploy automation proven in both directions: pointed at the development build the smoke leaves behind, it refused with `Build/WebGL does not contain the compressed release player (expected .unityweb data, framework and wasm; found: WebGL.data, WebGL.framework.js, WebGL.loader.js, WebGL.wasm). This looks like a development build. Nothing was published.` and exited `1`. Pointed at a real release build it reported `Verified release build: 8.87 MB total, 3 compressed assets.` then `Dry run. Nothing was published.` and exited `0`. A runtime defect was found by running it rather than by reading it: a PowerShell script invoked with `&` never sets `$LASTEXITCODE`, and under `Set-StrictMode -Version Latest` reading it is itself a terminating error, so the first full dry run aborted after a successful build; the exit-code check was replaced with a `try`/`catch`, which is correct because `Build-WebGL.ps1` throws on failure. Two of the Node tests were also wrong and were corrected rather than the script: `--force` matched the routine `git worktree remove --force` cleanup, and an assertion expected a raw `executeMethod` invocation when delegating to `Build-WebGL.ps1 -Release` keeps log scanning and exit handling in one place.
- 2026-07-21 WebGL texture-compression coverage gap closed, and the reported defect refuted rather than fixed. The visual-polish prompt diagnosed the shipping WebGL build as block-compressing the pixel art, citing `textureCompression: 1` under `buildTarget: WebGL` in the committed `.meta` files. That reading is wrong: all four managed textures carry `overridden: 0` on both their `Standalone` and `WebGL` entries, so those entries are disabled and Unity resolves them through `DefaultTexturePlatform`, which is `textureCompression: 0` (Uncompressed). The `1` is Unity's serialized default sitting in an inert block. Nothing overrides it at build time either - `ProjectSettings.asset` has `m_BuildTargetDefaultTextureCompressionFormat: []` and `tools/Build-WebGL.ps1` never sets `webGLBuildSubtarget`. The coverage gap the prompt identified is nevertheless real: `PixelArtImportTests` asserted only `importer.textureCompression`, the default-platform flag, so an accidentally *enabled* override would have shipped unnoticed. `PixelArt_ShipsUncompressedOnWebGLAndStandalone` now pins the ground truth instead of the flag, asserting `GetAutomaticFormat` resolves to `RGBA32`/`RGB24` for each of the four textures on both `WebGL` and `Standalone`, and rejecting any enabled override that reintroduces block or crunched compression. It was written expecting RED and came back GREEN on first run for all `8` assertions, which is the evidence that the diagnosis does not hold; it is retained as a regression guard, not as a fix. EditMode passed `261/261` (XML `Passed`, `0` failures, skips or inconclusive; `257` baseline plus the `4` new cases; complete `1722`-line log, `0` failure markers). Because no texture changed, the release build size is unchanged and the size/quality trade-off the prompt anticipated does not arise.
- 2026-07-21 jagged-art diagnosis B is unreproducible from repository evidence. `artifacts/smoke/matrix.json` records `dpr: 1`, `renderScale: 1` and `ratios {x: 1, y: 1}` for Chrome and Edge on both the desktop `1276x918` and mobile `380x783` viewports, so every committed screenshot was captured at exactly integer scale, where `FilterMode.Point` is correct and cannot produce the reported unevenness. The smoke harness therefore cannot exercise the fractional-DPR path the prompt blames, and no artifact in this repository shows the defect. The remaining plausible cause is a real device at DPR `1.25`/`1.5`, which is unverified. Diagnosis C is confirmed by inspection and is independent of DPR: `pitch-room.png` is a 1280x720 full-resolution illustration displayed at roughly 1:1, so `Point` filtering buys it nothing.
- 2026-07-21 filter-mode policy split per asset (jagged-art diagnosis C). `PixelArtImportPostprocessor` applied one policy to every managed texture, so `pitch-room.png` - a 1280x720 illustration displayed at roughly 1:1 - was imported with `FilterMode.Point` alongside the genuine 128x160 sprite frames of `judge-aya-sheet.png`. Point sampling gains a full-resolution illustration nothing and drops or duplicates source pixels at any non-integer scale. The environment is now `Bilinear`; `judge-aya-sheet.png`, `confidence-icons.png` and `dialogue-panel.png` stay `Point`. RED first, `2` failures out of `261`, both reading `Expected: Bilinear / But was: Point` - the parameterised `PixelArt_UsesCrispDeterministicImportSettings` case for the environment, and the pixel-art clause of `GeneratedScenes_MeetTargetContrastNavigationAndPixelArtContracts`. That second assertion was changed rather than satisfied, which is worth stating plainly: it encoded the single-policy rule this task deliberately replaces, and it is not one of the accessibility clauses. The contrast, focus and navigation assertions around it were not touched. GREEN: EditMode `261/261` (XML `Passed`, `0` failures, skips or inconclusive, duration `6.1743153 s`; complete `1866`-line log, `0` failure markers), PlayMode `56/56` (duration `15.0050534 s`; complete `918`-line log, `0` failure markers), Node `26/26`. `git status` after the run showed exactly one art change, `Assets/Art/Environment/pitch-room.png.meta`, whose entire diff is `filterMode: 0` to `filterMode: 1`; the three sheet metas were untouched, which is the evidence the split landed on the intended asset only. The verified-unrelated `ProjectSettings.asset` normalization (`targetPixelDensity`, `buildNumber`, iOS/tvOS/visionOS target strings) was reverted and the untracked `ProjectSettings/SceneTemplateSettings.json` deleted.
- 2026-07-21 postprocessor import-policy stamp added. Unity only reruns an `AssetPostprocessor` over already-imported assets when its `GetVersion` changes, so editing `OnPreprocessTexture` alone left the committed `.meta` files stale against the new policy with the source and the assets silently disagreeing. `PixelArtImportPostprocessor.GetVersion` now returns `2` and must be bumped with any change to what the importer writes - the same trap `PitchSimulatorProjectBuilder.GeneratorVersion` guards for generated scenes. No `GetVersion` override existed anywhere in the project before this.
- 2026-07-21 presentation item 1, the environment is now visible behind the content. Previously a `#0E171F` frame covered the room and a 16:9 `FitInParent` fit left ~100px dead bands above and below it, so the garden survived only as two ~86px slivers - measured from `chrome-primary-build.png`, where the content region runs `x` `86` to `1189` and the room is letterboxed to `y` `100` to `818`. Two changes, both gated on the existing wide/compact split rather than a new one: `GuidedPitchResponsiveLayout` now drives the environment `AspectRatioFitter` to `EnvelopeParent` on a wide stage and keeps `FitInParent` when compact, and the guided `Content Frame` is `DeepNavy` at alpha `0.82`. The inner cards stay opaque `CardNavy` because they carry the sentence text. Compact deliberately keeps the letterbox: filling a `720x960` stage crops over half the room's width away, taking the windows, shelving and plant beds with it, which was confirmed against a rendered comparison before choosing. `GeneratorVersion` bumped `1` to `2` and all three owned scenes regenerated through `BuildProjectFoundationBatch` (`324`-line log, `0` failure markers, `3` scenes loaded, `Exiting batchmode successfully now!`, exit `0`). RED first, `2` failures out of `261`: `Expected: EnvelopeParent / But was: FitInParent` and `Expected: RGBA(0.055, 0.090, 0.122, 0.820)`. GREEN: EditMode `261/261` (duration `4.7068768 s`; `1716`-line log, `0` markers), PlayMode `56/56` (duration `14.5426127 s`; `891`-line log, `0` markers), Node `26/26`. `Build-WebGL.ps1` succeeded and `smoke-webgl.mjs` reported Chrome and Edge `status: passed`, `modes.missingConfig: true`, `0` console errors and `0` page errors; the regenerated `chrome-primary-build.png` shows the room reading as a place with no dead bands, and `chrome-mobile-compact.png` confirms portrait still letterboxes with its composition intact. The first regeneration attempt produced a `200`-line log that stopped at `Begin MonoManager ReloadAssembly` with `Access token is unavailable` and changed no scene; invoking Unity through `Start-Process -Wait` as `Run-UnityTests.ps1` does rather than the call operator produced the real `324`-line run. This is the licensing class the plan warns about, and it is why a builder run is verified by its log and its diff rather than by its exit code.
- 2026-07-21 contrast measurement hardened before any transparency landed, because the existing helper would have hidden the regression. `RelativeLuminance` ignores alpha, so a translucent panel would have kept satisfying every contrast assertion while real contrast fell away - the exact trap the plan warns about. `ContrastRatio` now fails on any operand with alpha below `1`, forcing the caller to composite first; `Composite` applies source-over, and `WorstCaseRoomColor` decodes `pitch-room.png` and returns its brightest pixel, which is the whiteboard and therefore the worst case for light text. Sampling the whole image rather than the panel's rect is deliberately conservative and survives layout changes. `CheckContrast` accepts an optional declared backdrop and still fails a translucent backing that does not declare one, so this cannot become a general escape hatch. Landing the helper first was worth it: with the frame still opaque the change was a verified no-op at `261/261`, and when the panel went translucent the new guard immediately caught two sites the manual review had missed - `rail label` and `presentation` sit directly on the guided frame and were being measured against its nominal colour. Both now measure against the frame composited over the room and still clear `4.5:1`; the focus and revision outlines still clear `3:1`. No accessibility assertion was relaxed to reach GREEN.
- 2026-07-21 independent review of `2737baa`, `3f37d3e` and `46a4415`, findings applied. The reviewer independently confirmed the compression refutation, confirmed `Composite` is correct source-over and - a point the implementation had not checked - that it is correct specifically because `ProjectSettings.asset:49` is `m_ActiveColorSpace: 0` (Gamma), so the GPU blends UI in the same sRGB space the helper composites in. A Linear project would have made the helper wrong. The reviewer also verified that the brightest room pixel really is the worst case for every pair currently measured, because the composited frame stays darker than both `LightText` and `FocusGold` and brightening the backdrop lowers the ratio monotonically; that invariant only holds while no dark foreground is measured against the frame.
- 2026-07-21 the most serious review finding was an evidence error, not a code defect. The `3f37d3e` entry claimed the `ProjectSettings.asset` normalization was reverted and `SceneTemplateSettings.json` deleted; `git ls-tree` shows the commit contains both. The revert was performed and verified, and then PlayMode and Node were run, which regenerated both files, and `git add -A` swept them into the commit. The verification was true when it was made and stale by the time it was committed. `ProjectSettings.asset` is restored to its `575e77f` content, verified by an empty staged diff against that revision rather than by a status line; `SceneTemplateSettings.json` is removed from the index and added to `.gitignore` so no future batch run can reintroduce it. The durable lesson is that a clean `git status` before a test run is not evidence about what a commit will contain - the check has to be made against the staged tree.
- 2026-07-21 three real defects fixed from review. First, `PitchSimulatorProjectBuilderTests.cs:710` measured every `Selectable`'s focus ring against the hardcoded opaque `DeepNavy`, a surface that no longer exists on the guided screen; it passed at a nominal `12.4:1` while the real composited value is `7.75:1`, so pushing the panel more transparent would have kept reporting `12:1` as the true ratio fell toward `3:1`. This was the exact hole the hardening existed to close and the hardening had missed it. Replaced with `ResolveAdjacentSurface`, which walks to the nearest backing above each control and composites it over the room when translucent, fixing the problem structurally rather than at one call site. Second, `ApplyWideLayoutDefaults` baked every wide-layout property except the new environment fitter, so `Game.unity` shipped `m_AspectMode: 3` and the room snapped from letterbox to full bleed on the first responsive frame; the fitter is now baked with the rest and the committed scene carries `m_AspectMode: 4`. Third, nothing asserted the as-generated aspect mode any more, because the environment test now applies the responsive layout before asserting - a builder change dropping the fitter wiring would not have been caught. An assertion on the committed value now runs before any `Apply`. `GeneratorVersion` bumped `2` to `3` and all three owned scenes regenerated (`369`-line log, `0` failure markers, `3` scenes loaded, exit `0`).
- 2026-07-21 new test found two of its own bugs by being run rather than reasoned about. `GeneratedResponsiveLayout_WithoutEnvironmentFitter_FailsItsContract` covers the `environmentFitter` null clause in `ValidateContract`, which previously had no RED test and would have passed with the clause deleted, and which matters because `SetEnvironmentFill` null-guards so a scene missing the reference degrades silently rather than failing. The test mutates the open scene, and its first version left it dirty, which broke `GeneratedScenes_StampGeneratorVersion_AndRegenerateOnlyWhenItChanges` downstream; its second version cleaned up with `NewScene(EmptyScene, Single)`, which left an untitled empty scene active and broke the same test a different way. Reloading the scene from disk in a `finally` fixed it. Both failures were only visible by running the suite. GREEN: EditMode `262/262` (duration `4.6677358 s`; `1743`-line log, `0` markers), PlayMode `56/56` (duration `13.8233962 s`; `890`-line log, `0` markers), Node `26/26`.
- 2026-07-21 one review finding rejected with reasoning. The reviewer proposed removing the duplicated `TranslucentPanel` constant by having the test read the `internal` field from `GuidedPitchSceneBuilder` directly, on the basis that the test asmdef already references the Editor assembly. An assembly reference does not grant `internal` access in C#; that requires `InternalsVisibleTo`, and `grep` finds none anywhere in `Assets/`. The test also references no `GuidedPitchSceneBuilder` member today. Widening the constant to `public` purely for a test is a worse trade than a documented mirror, so the duplication stays with its "kept in step with" comment. Minor findings applied: `Composite` returns the backdrop instead of dividing by zero when both operands are fully transparent, and `CheckContrast` now fails a text colour with alpha below `1` rather than throwing out of `ContrastRatio` and discarding every failure already accumulated.
- 2026-07-21 the scene-regeneration guard was blind, and the evidence recorded for `a6337b6` was not reproducible. `GeneratedScenes_StampGeneratorVersion_AndRegenerateOnlyWhenItChanges` began failing deterministically, and stashing every uncommitted change proved it fails at `a6337b6` itself - two consecutive runs, clean tree, `261/262`. The `GREEN EditMode 262/262` recorded in that commit and in the entry above was a true observation of one run and a false statement about the commit. That is the second time this session a check was accurate when taken and stale when it mattered. The earlier attribution of this failure to the new scene-mutating test leaving state dirty was also wrong; that cleanup was worth making on its own merits but was not the cause, and the green run that followed it was luck.
- 2026-07-21 root cause and fix for the regeneration guard. The test proved regeneration by asserting the owned root's `GlobalObjectId` changed. Unity derives a scene object's file ID from hierarchy structure, so destroying a root and recreating it structurally unchanged reproduces the same id. The proxy therefore could not see a rebuild at all: the negative assertion was vacuous and the positive one was impossible, and it got worse the more deterministic the builder became - a guard that decays exactly as the thing it guards improves. The builder itself was correct throughout; only the observation was wrong. Regeneration is now observed directly by planting a `Regeneration Sentinel` child under the owned root and checking whether it survives, since the builder destroys the whole root when it rebuilds. Both directions are exercised in one test: sentinel present after a matching-stamp build, absent after a stale-stamp build, with the stamp confirmed rewritten. Proven non-vacuous by mutation rather than by argument - replacing the freshness check with `return stamp != null` produced `Expected: False / But was: True` on the sentinel plus a second failure in `CommittedScenes_MatchWhatTheCurrentBuilderGenerates`; the mutant was reverted and the builder restored byte-identical to `HEAD`. Determinism confirmed by two consecutive clean runs before the mutation and one after.
- 2026-07-21 rotate-to-play orientation rule added as pure C#, ahead of any presentation work. `RotateToPlayRule.ShouldPromptRotate` gates a stage that is portrait and at or below `960` CSS px wide, which is the same threshold `GuidedPitchResponsiveLayout` uses to compact; the layout now references `RotateToPlayRule.HandheldWidthThreshold` so the stage that compacts is exactly the stage that can be gated and the two cannot drift. A tall narrow desktop window is deliberately left playable because rotating a monitor is not a fix and the prompt would be a dead end the learner cannot clear. Device pixel ratio is excluded so a high-density phone is still a phone and a DPR-only change can never flip the gate. `11` cases cover phone and tablet in both orientations, desktop, the two wide-portrait exclusions, the square boundary, and DPR independence. RED first at compile - `error CS0103: The name 'RotateToPlayRule' does not exist in the current context`. GREEN: EditMode `273/273` (XML `Passed`, `0` failures, skips or inconclusive, duration `3.3712424 s`; complete `1725`-line log, `0` failure markers), PlayMode `56/56` (duration `13.8595722 s`; `920`-line log, `0` markers), Node `26/26`. The overlay, its localization, the scene wiring and the smoke-harness changes are not built yet.
- 2026-07-21 known blocker for the rotate-to-play overlay, recorded before it is built. `tools/tests/smoke-webgl-flow.test.js:247-264` pins the mobile smoke contract at `390x844`, which is portrait, and pins the `chrome-mobile-compact.png` and `edge-mobile-compact.png` capture names. A rotate gate makes that viewport render the overlay instead of gameplay, so the mobile contract has to move to landscape and gain a separate portrait assertion that the overlay appears. There is also no true orientation lock on mobile web: `screen.orientation.lock()` requires fullscreen on Chrome for Android and is unsupported on iOS Safari, so the deliverable is a gate the learner clears by turning the device, not an API call.
- 2026-07-21 rotate-to-play copy and presentation bridge landed; scene wiring still outstanding. `ui.rotate.title` and `ui.rotate.body` were added to both catalogs, keeping the `en`/`ms` key-parity contract green and following the existing convention that every `ui.*` key carries the English value in the `pending_human_review` Malay catalog - all `37` existing `ui.*` entries already do. The copy avoids `landscape`, `portrait` and `orientation`, asserted by test, because Primary learners are the audience and the instruction has to be actionable without jargon; it reads `Turn your device sideways` and `This game needs a wider screen. Turn your device to keep playing.` RED first with `Expected: "Turn your device sideways" / But was: "[[missing:ui.rotate.title]]"`. A first attempt to add the keys with `json.dumps` reformatted both catalogs, `1603` changed lines each, and was reverted in favour of a surgical text insert that produced a two-line diff per file; the reformat would have passed every test while making the change unreviewable. `RotateToPlayOverlay` is the thin bridge over the pure rule, mirroring `GuidedPitchResponsiveLayout`'s injected metrics reader, memoized `Apply`, and `ValidateContract`. Its resting state is hidden so a playable stage can never flash the prompt, and `Apply` returns true only when visibility actually changed, which keeps per-frame polling free. `7` behaviour cases cover show, hide once turned, desktop untouched, the hidden default, contract failure and pass, and idempotence. GREEN: EditMode `281/281` (XML `Passed`, `0` failures, skips or inconclusive, duration `3.6116726 s`; complete `1783`-line log, `0` failure markers), PlayMode `56/56` (duration `13.9718765 s`; `891`-line log, `0` markers), Node `26/26`.
- 2026-07-21 remaining rotate-to-play work, not started: building the overlay into the generated canvas as the topmost child, wiring `RotateToPlayOverlay.Configure` and its localization through `Bootstrapper` the way `router.Initialize` already receives its resolver, extending the generated-screen whitelist in `PitchSimulatorProjectBuilder` which currently asserts exactly six screens by `SequenceEqual`, bumping `GeneratorVersion` and regenerating, then moving the mobile smoke contract at `tools/tests/smoke-webgl-flow.test.js:247-264` from `390x844` portrait to landscape and adding a portrait assertion that the overlay appears.
- 2026-07-21 rotate-to-play prompt wired into the generated canvas. It is built last so it is the canvas's last child and draws over whichever screen is current, and it is deliberately not a screen: the router never shows or hides it, and the six-screen `SequenceEqual` whitelists in `PitchSimulatorProjectBuilder` and the builder tests were extended to seven entries with the prompt pinned last rather than loosened. The host `RectTransform` stays enabled so it can poll the viewport every frame while only the panel beneath it toggles, so a playable stage never flashes the prompt. The panel is opaque `DeepNavy` and a raycast target, which is what stops a tap landing on the controls it is covering. `Bootstrapper` hands it the launch locale directly after `router.Initialize`, and again from `EnterSafeFallback`, because a learner on a phone still needs the turn instruction while recovering. `ApplyLocalization` keeps the baked English when a key resolves to a `[[missing:...]]` token or when no resolver is supplied, since blanking the one screen whose entire job is to say what to do would strand the learner; `3` cases cover the replacement, the missing-key fallback and the null resolver. RED first: `Missing 'Rotate To Play/Panel' under 'Canvas'` and `Expected: "Rotate To Play" / But was: "Safe Fallback"`. Two follow-on failures were self-inflicted and worth naming - a second six-screen assertion at `PitchSimulatorProjectBuilderTests.cs:1386` also needed the new child, and the prompt's texts sit under `Panel/Content Frame`, not `Panel`, so the first assertions used the wrong paths. `GeneratorVersion` bumped `3` to `4` and all three owned scenes regenerated (`381`-line log, `0` failure markers, `3` scenes loaded, exit `0`). GREEN: EditMode `286/286` (duration `3.9 s` band; `0` failure markers), PlayMode `56/56`, Node `26/26`, and a full `Build-WebGL.ps1` plus `smoke-webgl.mjs` run with Chrome and Edge `status: passed`, `modes.missingConfig: true`, `0` console errors, `0` page errors, canvas contained with `0` horizontal overflow on both viewports.
- 2026-07-21 the predicted smoke collision did not happen, and the real problem is quieter. The expectation recorded above was that a rotate gate would break the mobile smoke contract because that contract runs at `390x844`, which is portrait. It did not: `verifyCanvasContract` requires the canvas to stay contained and the composition to change when the viewport changes, and the prompt appearing satisfies both, so Chrome and Edge still pass. The actual cost is coverage, not failure. `chrome-mobile-compact.png` and `edge-mobile-compact.png` previously exercised the compact guided layout - stacked board, single-column cards, enabled scroll - and now show only the rotate prompt, so nothing in the suite renders the compact gameplay path any more. Moving the mobile contract to landscape and adding a separate portrait assertion is therefore still worth doing, but to restore lost coverage rather than to repair a break. Recording this because the earlier entry stated the opposite with more confidence than the evidence supported.
- 2026-07-21 mobile smoke now covers both orientations, restoring the coverage the rotate prompt silently took. The single `390x844` check became two: `verifyRotatePrompt` at `390x844` proves the gate, and `verifyPlayableStage` at `844x390` proves the compact layout behind it - stacked board, single-column cards, enabled scroll - which nothing had rendered since the prompt shipped. Both are asserted by sampling four widely separated corner patches of the canvas and comparing their hashes: the prompt is a flat opaque field so its corners hash identically, while gameplay never does. Hashing alone could only have proven "the canvas changed"; this proves which of the two states is on screen, and the two verifiers assert opposite conditions so neither can quietly stop asserting. The live run confirms both directions rather than only the happy one - `mobilePortrait` reports `rotatePromptCovering: true` at `380x783` and `mobile` reports `false` at `834x329`, for Chrome and Edge alike. A Node test pins that opposition (`size !== 1` against `size === 1`) plus the ordering, so portrait is always checked before the stage is turned. Captures grew from `12` to `14` with `chrome-mobile-portrait.png` and `edge-mobile-portrait.png`, and the expected-capture lists were updated rather than loosened. Node `27/27`. Full `smoke-webgl.mjs` run: Chrome and Edge `status: passed`, `modes.missingConfig: true`, `0` console errors, `0` page errors. EditMode `286/286` and PlayMode `56/56` carry over unchanged from `f3030fe`; this change touches only `tools/`, so no Unity code was rebuilt or re-run and the numbers are cited as inherited rather than as a fresh result.
- 2026-07-21 restoring the landscape capture immediately exposed the presentation prompt's item 6, which is now visible but still unguarded. `chrome-mobile-compact.png` at `834x329` renders the room and the four part cards, but the rail labels and the card labels are reduced to bare coloured bars and tiny glyphs with no readable text, which is the same collapse the visual-polish prompt described for the compact stage - the four-part concept, the whole point of the game, is invisible on a landscape phone. Nothing in the suite asserts those labels are legible, so this is caught by eye only. Worth a contrast-and-legibility assertion on the compact rail before item 6 is attempted, so the fix has a failing test to satisfy rather than a screenshot to eyeball.
- 2026-07-21 Malay removed on the project owner's instruction; the game ships in English only. This was raised before acting rather than performed as cleanup, because `ms` was not a stray file but a documented product property - `docs/08-ACCESSIBILITY-LOCALIZATION.md` stated the supported locale codes were `en` and `ms`, `docs/04-CONTENT-AUTHORING.md` required every key in both catalogs, and `319/319` parity appeared in the product overview and QA plan. `CLAUDE.md` directs work to follow the approved design under `docs/`, so removing it is a product decision and the docs had to move with the code or the two would disagree. What was removed: `ms.json` and its meta, the `malayCatalogJson` serialized field and its bootstrap-contract check, the builder reference, the `MalayLocale` constants in `AccessibilitySettings` and `SaveDataMigrator`, and `ms` from the content asset's `SupportedLocales`. `AccessibilitySettings.NormalizeLocale` now resolves every launch locale to English rather than accepting `ms`, so an LMS launch carrying `ms` gets readable English instead of being rejected or showing missing tokens; `GuidedPitchContentValidator` now requires exactly one supported locale. The two Malay parity tests were replaced by `UnsupportedLocale_ResolvesEnglishRatherThanMissingTokens`, which asserts every English key resolves identically under `ms` - the removal is covered by a test rather than by the absence of one. Fallout was found by running rather than by reading: the first EditMode run failed `34/285` and the first PlayMode run `25/56`, almost all a single `FileNotFoundException` for `ms.json` across test rigs, plus a `Locale is invalid` from the migrator and one missing serialized field. `GeneratorVersion` bumped `4` to `5` and all three owned scenes regenerated. GREEN: EditMode `285/285`, PlayMode `56/56`, Node `27/27`, and a full rebuild plus smoke with Chrome and Edge `status: passed`, `modes.missingConfig: true`, `0` console errors, `0` page errors. Six living reference docs were updated. Historical records were deliberately left alone - `docs/18-VERTICAL-SLICE-ACCEPTANCE.md`, `docs/plans/`, `docs/superpowers/plans/` and `docs/superpowers/specs/` describe what was true when they were written, and editing them would falsify the record rather than update it.
- 2026-07-21 rotate prompt given presentation. It was legible but read as an error page, which is the wrong tone for a screen a learner is stuck on until they act. Judge Aya now carries the instruction at `176x220`, which costs no new art and no licensing question because her sheet already ships; the title is `44pt` bold in `FocusGold` and the body `24pt` in `LightText`, both centre-aligned, replacing the previous `30`/`20` pairing that read as fine print on the one screen with nothing else on it. The gold headline clears the contrast bar against `DeepNavy` at roughly `7.6:1` and is asserted by the same `CheckContrast` calls as the body rather than exempted. `GeneratedRotatePrompt_IsPresentedWithJudgeAyaAndReadableHierarchy` pins Aya's presence, her source texture, `preserveAspect`, the title outranking the body, a `40pt` floor on the title, centre alignment and both contrast checks, so the presentation cannot quietly regress to fine print. RED first on the missing Aya child. `GeneratorVersion` bumped `5` to `6` and all three owned scenes regenerated. Content stays centred deliberately: the smoke's corner-uniformity check reads the four canvas corners, so a prompt that reached into them would break `verifyRotatePrompt` - the live run still reports `rotatePromptCovering: true` at `380x783` and `false` at `834x329`. GREEN: EditMode `286/286` (duration `3.8925276 s`; `1761`-line log, `0` markers), PlayMode `56/56` (duration `14.2291587 s`; `892`-line log, `0` markers), Node `27/27`, plus a full rebuild and smoke with Chrome and Edge `passed` and `0` console and page errors.
- 2026-07-21 presentation item 2, the four parts now carry real icons. The ASCII placeholders `!`, `?`, `>` and `*` were the most repeated visual element in the game - they appeared in the progress rail, on all four board slots, on the sentence chips and again on every Results row, four sites in the builder and three views. `Assets/Art/UI/part-icons.png` is a first-party `384x96` sheet of four named `96x96` cells drawn at `24x24` and nearest-upscaled, so every edge lands on a whole pixel and it is genuine pixel art rather than a smooth shape shrunk: a magnifier for Problem, a bar chart for Evidence, a wrench for Solution and a star for Value, each in that part's existing colour. It is recorded in `docs/16-ASSET-MANIFEST.md` as first-party with no third-party source and therefore no redistribution restriction. The postprocessor manages it with the same uncompressed, `Point`-filtered, named-cell policy as the other sheets, its `GetVersion` was bumped `2` to `3` so the new policy actually reran, and both `PixelArtImportTests` contracts were extended to cover it rather than left at four assets. Cells are named after `PitchPart` so the builder resolves an icon by part rather than by index, which no rename can silently reorder. `PitchPartVisual.IconGlyph` was deleted rather than left dangling, because nothing renders it any more. The icon element changed from `Text` to `Image` across three views and the builder; a part's icon never changes at runtime, so the sprite is serialized once by the builder and no view assigns it, which removed the per-`Apply` glyph write entirely. Compilation caught the fallout in three rounds - the `PitchResultPartView.Configure` signature, then five PlayMode rig call sites still constructing icons as `Text` - and each was a real call site rather than a cast to silence. `GeneratorVersion` bumped `6` to `7` and all three owned scenes regenerated (`365`-line log, `0` failure markers, `3` scenes loaded, exit `0`). GREEN: EditMode `288/288`, PlayMode `56/56`, Node `27/27`, plus a full rebuild and smoke with Chrome and Edge `passed`, `modes.missingConfig: true`, `0` console errors and `0` page errors; the regenerated `chrome-primary-build.png` shows the four icons in the rail and on every card.
- Next unchecked guided-builder action: Primary/Secondary educator or representative-learner review.
