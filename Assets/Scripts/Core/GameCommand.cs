namespace Agrovator.PitchSimulator.Core
{
    public enum GameCommand
    {
        FinishBooting,
        StartScenario,
        Continue,
        SelectResponse,
        FinishScenario,
        SubmitResults,
        SubmissionSucceeded,
        SubmissionFailed,
        Retry,
    }
}
